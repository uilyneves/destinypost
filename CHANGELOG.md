# Changelog

Formato baseado em [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/).
Versionamento segue [SemVer](https://semver.org/lang/pt-BR/).

Fork do [Postiz](https://github.com/gitroomhq/postiz-app) (AGPL-3.0).

## [Unreleased]

### Marca

- **Início da transformação para MultiPost.** A interface, títulos, traduções e ativos principais passam a usar a nova identidade vetorial MultiPost, preservando os créditos e avisos AGPL do Postiz.
- **Identidade DestinyPost aplicada de ponta a ponta.** A logo e o favicon antigos foram substituídos pelos ativos oficiais da Destiny AI; títulos de páginas, telas de autenticação, agente, e-mails, Swagger, CLI, traduções e documentação passaram a usar DestinyPost. O README agora apresenta a distribuição self-hosted atual, aponta para `destinyai-dev/destinypost` e identifica a Destiny AI como mantenedora. Os créditos e avisos AGPL do Postiz foram preservados.
- **Webhook do Instagram usa `destinypost` como Verify Token padrão.** O token legado `multipost` continua aceito pelo backend para não interromper instalações já configuradas.

### Adicionado

- **Integração completa de imagens via OmniRoute.** O provedor agora pode ser configurado também para IMAGEM, com geração texto→imagem e imagem→imagem pelos endpoints OpenAI-compatible `/images/generations` e `/images/edits`, suporte a respostas `b64_json`, data URL ou URL remota, proporção, qualidade, resolução, timeout e proteção contra SSRF. A validação exige modelo e endpoint tanto para texto quanto para imagem.
- **OmniRoute como provedor de texto dedicado.** O administrador pode selecionar OmniRoute e informar endpoint OpenAI-compatible, chave, modelo principal e fallback por workspace/perfil. A instalação da Dragão Negro já vem pré-configurada com `https://omniroute.dragaonegro.com/v1`.
- **Integração Canva pronta para uso pelos clientes do SaaS.** Cada organização conecta a própria conta Canva por OAuth e passa a ver uma galeria dos seus designs, incluindo designs próprios e compartilhados, com busca, atualização e paginação. O botão **Importar para publicação** exporta todas as páginas em PNG, salva cada uma na biblioteca de mídia do perfil ativo e cria um rascunho já associado ao canal escolhido, pronto para legenda, agendamento e postagem. A integração também ganhou **Editar no Canva** e **Desconectar Canva**, com revogação do token e preservação das mídias já importadas. Os escopos OAuth padrão foram reduzidos ao mínimo necessário para leitura e exportação.
- **Área "Status" (admin) com o painel "Problemas" — visão única do que precisa de atenção, por perfil.** Antes, um problema (canal desconectado, post que não publicou, automação de Instagram que falhou) só aparecia fragmentado no sino, no e-mail ou num selo por-canal — **sem visão agregada e sem dizer de qual perfil/cliente veio**. Agora há uma área **Status** (uma aba em **Configurações**, restrita a **admin/superadmin da org**) cuja primeira aba, **Problemas**, lista o estado atual **derivado das fontes de verdade** (some sozinho quando o problema é resolvido), **agrupado por severidade** (Crítico / Atenção) e **com o perfil de origem em cada item**: canais que precisam reconectar (com botão **Reconectar**), publicações que falharam nas últimas 24h/30 dias (agrupadas por canal para não virar uma muralha) e automações de IG que falharam. Endpoint `GET /status/problems` (`@CheckPolicies(ADMIN)` — 403 mesmo sem Stripe; `select` explícito no repositório, nunca expõe token). A estrutura de abas já nasce pronta para as próximas fases (Saúde da infra, Erros/histórico). Webhooks seguem em Configurações; o Status é a camada de domínio e delega stack traces/logs crus para Sentry/Grafana.
- **Área "Status" ganha a aba "Histórico" — registro de falhas que sobrevive à resolução.** Complementando a aba **Problemas** (estado atual, que some quando o problema é resolvido), a nova aba **Histórico** é um **log append-only** dos eventos de falha de domínio — canal desconectado, publicação que falhou, automação de Instagram que falhou — que **permanece mesmo depois de resolvido** (reconexão, republicação ou até exclusão do canal). Cada evento guarda um retrato (nome/foto/plataforma do canal, perfil de origem, momento) e um **link de depuração** por tipo (post → `/p/:id` + Temporal; automação → `/automacoes/:id`), é **filtrável por tipo** e **paginado** ("Carregar mais"). É alimentado no momento da falha por **3 pontos únicos de serviço** (desconexão de canal, mudança de estado do post para ERRO, execução de automação para FALHOU) — sempre **fail-soft** (registrar o evento nunca derruba a publicação/renovação/automação real). A mensagem é **sanitizada** (apenas `nome: mensagem`, **nunca** o corpo cru da exceção que poderia conter token). Novo modelo `StatusEvent` (**denormalizado, sem chave estrangeira** de propósito — para o histórico sobreviver à exclusão do canal/perfil e não onerar a leitura com joins), endpoint `GET /status/history` (mesmo guard admin-only do `/problems`, com paginação por cursor e filtros validados) e **autoprune de 90 dias** aplicado no boot (mesmo padrão do cleanup de comentários). A tela reaproveita o badge de plataforma, o chip de perfil e os links de depuração da aba Problemas (extraídos para um módulo compartilhado). _Observação de segurança:_ este log é um canal **novo e sanitizado** — ele não fecha o vazamento pré-existente de armazenamento de `Post.error`/tabela `Errors` (que ainda grava a exceção crua no banco; a Fase 1 só o removeu da resposta da API), tratado como follow-up à parte.
- **Área "Status" ganha a aba "Saúde da infra" — sonda ativa dos serviços críticos.** Fecha a lacuna que originou toda a observabilidade: uma variável de storage errada derrubava a conexão de canais **em silêncio**. A nova aba (admin) **sonda de verdade** — não só checa se a env var existe — **PostgreSQL** (`SELECT 1`), **Redis** (`PING`, ou "não configurado" quando roda no mock em memória), **Temporal** (crítico para agendamento) **Storage R2/local** (valida a credencial/bucket **sem escrever nada** — via `HeadBucket` no R2 e teste de escrita no diretório local) e **E-mail** (Resend: valida a chave **ativamente** via `domains.list`, sem enviar; SMTP/nodemailer: verifica a presença das variáveis com um **tooltip** explicando que a conexão não é testada), então **pega credencial errada e serviço fora do ar**, não apenas ausência de config. Cada componente aparece com selo (operacional / atenção / erro), a causa (mensagem **sanitizada**, nunca a credencial) e a latência da sonda; há um botão **"Verificar agora"**. Endpoint `GET /status/health` (mesmo guard admin-only do `/problems`), com **cache de 30s**, piso de 5s no "Verificar agora" (anti-loop) e timeout por sonda (uma sonda pendurada nunca trava a tela nem empilha comandos no Redis). **Gate por env** `STATUS_INFRA_HEALTH_ENABLED` (**habilitado por padrão**; defina `false` para desabilitar): como o resultado é global e fica atrás do guard admin-de-org, numa instância de **registro aberto** convém desabilitar (ou manter `DISABLE_REGISTRATION=true`) — detalhado no `.env.example`. Webhooks seguem fora desta fase.
- **Motivo da desconexão de canal agora fica registrado (observabilidade).** Quando a renovação de token de um canal falha, o motivo real já sanitizado (ex.: `ApplicationFailure: expired`, `invalid_grant` — **nunca o token**) passa a ser persistido na integração (`Integration.refreshError`/`refreshErrorAt`), gravado na mesma escrita atômica que marca o canal como "precisa reconectar" e zerado ao reconectar. É o que alimenta o "porquê" na tela de Problemas.
- **Revisão e aprovação de posts pelo cliente, dentro do app (modelo agência → cliente).** Além do link externo de revisão (que já existia, para clientes sem conta), o cliente convidado como **Visualizador** de um perfil agora revisa e aprova os posts **logado, na própria interface**. Ao clicar num post do calendário, o Visualizador abre a página do post (`/p/:id`) com o conteúdo na íntegra (imagem/vídeo/texto) e os botões **Aprovar / Pedir alterações / Comentar** ao lado — a mesma tela unificada para ver e revisar. A ação vira um comentário tipado (`APPROVAL`/`CHANGE_REQUEST`/`COMMENT`) reaproveitando a infraestrutura de comentários do link de revisão. A agência vê um **selo de status** no post do calendário (✓ aprovado / ! alterações solicitadas) e, ao clicar nele, abre a mesma página com os comentários do cliente. Escopo de segurança: a rota de revisão é liberada ao Visualizador (decorator `@AllowViewer` no `ProfileAccessGuard`, apesar do bloqueio geral de escrita) mas **escopada ao perfil do membro** — um Visualizador do Dell só revisa posts do Dell (validação no backend). Nesta primeira etapa a aprovação **sinaliza** para a agência (não bloqueia a publicação — o post publica normalmente e o selo é informativo). A identidade do autor dos comentários aparece de verdade **apenas na tela autenticada** (nome/e-mail); o link externo/guest continua anônimo ("Usuário N"), sem expor e-mails da equipe.
- **Compartilhamento das credenciais OAuth do perfil Default com os demais perfis (toggle, ligado por padrão).** As credenciais OAuth (Meta, Google, etc.) sempre foram por-perfil, mas não havia herança: um perfil secundário sem credenciais próprias caía direto nas variáveis de ambiente, nunca nas credenciais do perfil Default. Agora, um **toggle no perfil Default** (Configurações → Credenciais, visível só quando o perfil ativo é o Default) permite que os perfis que **não** configuraram as próprias herdem as credenciais do Default. A resolução para consumo (conexão de canais, publicação, flows) passou a ser: **credencial própria do perfil → credencial do Default (se o compartilhamento estiver ligado) → variáveis de ambiente** (fallback final preservado). Editar credenciais continua com match exato (nunca herda ao salvar), e o resolvedor de IA (que já tinha seu próprio `shareDefault`) não foi tocado. Nasce **ligado** (`@default(true)`) para espelhar o comportamento esperado em produção (Default = base compartilhada) — como o `.env` segue sendo o fallback final, instalações que só usam `.env` não mudam de comportamento. Ressalva de migração: se um perfil secundário já conectou canais usando o app do `.env` **e** o Default tiver credenciais OAuth **diferentes** salvas no banco, esses canais passariam a resolver pelo app do Default (o refresh de token é vinculado ao app) — combinação incomum; nesse caso, desligue o toggle. A nova coluna (`Organization.shareProviderCredentialsWithProfiles`) é aplicada **automaticamente** no deploy — o `CMD` do container roda `prisma-db-push` no boot, antes de subir os apps (`Dockerfile.dev`); nenhum passo manual necessário.
- **Papel Dono (OWNER) vira o "administrador de um único perfil" — com convite de membros pelo próprio Dono.** Fechando o modelo agência → cliente (cada cliente = um perfil), agora dá para delegar um perfil por completo a alguém sem lhe dar acesso aos demais. Novidades: (1) **OWNER** passa a ser opção no convite da equipe (antes só Editor/Gerente/Visualizador); (2) a aba **"Equipe"** de Configurações passou a ser **única e adaptável ao papel**: admin da org vê o time inteiro (todos os perfis), enquanto Dono/Gerente (org-USER) vê e gerencia apenas os membros do **perfil ativo** (`useCurrentProfile`) — **convida por e-mail** (sempre como USER restrito àquele perfil, sem seletor de perfil), define a função e **remove**; (3) o convite é escopado — cria um membro org-USER restrito **apenas** àquele perfil. Segurança: a rota `POST /profiles/:id/members/invite` é `@ProfileManage` (Dono/Gerente do perfil ou admin) e o papel concedido é **limitado ao do convidante** (`assertCanGrantProfileRole` — Dono convida até Dono, Gerente até Editor/Visualizador; admin concede qualquer). Assim, o Dono administra o próprio perfil (membros + persona + base de conhecimento) enquanto a agência (org Admin) segue por cima de todos — sem precisar do arriscado conceito de "admin segmentado".
- **Gerente do perfil ganha a aba "Persona de IA" (persona + base de conhecimento), distinguindo-o do Editor.** Até então Gerente e Editor ficavam idênticos na interface, apesar de o Gerente ter capacidades extras no backend. Agora um membro org-USER com papel OWNER/MANAGER no perfil vê e edita a **Persona de IA** e a **Base de conhecimento do próprio perfil** em Configurações (rotas já `@ProfileManage` — admin da org OU dono/gerente do perfil; o componente é escopado ao perfil ativo via `useCurrentProfile`). O Editor continua apenas com o conteúdo (posts, auto post, conjuntos, assinaturas). Observação: gerenciar membros do perfil pela UI segue restrito ao admin da org (o onboarding de cliente passa pelo fluxo de convite); o backend já autoriza o Gerente para isso, faltando apenas a tela — próxima etapa.
- **Papel Visualizador vira somente-leitura de fato na interface.** O backend já bloqueava as ações (403), mas a UI ainda as exibia. Agora, para um membro org-USER com papel VIEWER no perfil: o **onboarding** é pulado; no **calendário** somem os botões de adicionar canal, criar post, IA, e o clique num dia não abre mais o compositor; **editar/duplicar/excluir** um post e o **menu do canal** (⋮) ficam ocultos (resta só "Copiar ID"); clicar num post abre a **revisão** (leitura + aprovar), não o editor; o **menu lateral** mostra só o Calendário; as **Configurações** somem do menu (abas de administração do workspace ficam restritas a admin da org; conteúdo a Editor+); e o Visualizador **não recebe mais e-mail de sucesso/falha de publicação** (isso vai só para Editor+). A tela **Membros da equipe** passa a mostrar, por membro, o perfil e a função (ex.: "Dell (Visualizador)"); admins aparecem como "Todos os perfis".
- **E-mails e notificações agora saem em multi-idioma (pt/en) por workspace.** Até então todos os e-mails do app eram strings fixas em inglês (não havia camada de i18n no backend — `useT()`/i18next só existiam no frontend e não alcançavam o código de e-mail, que roda no backend/Temporal sem contexto de request). Esta entrega fecha essa lacuna:
  - **Camada de i18n de e-mail no backend:** novo helper puro `emailT(key, lang, params)` + catálogo `pt`/`en` (`libraries/nestjs-libraries/src/emails/i18n/`). Interpolação `{{var}}` (mesma convenção do i18next). Função pura, sem `crypto`/IO — nunca importada dentro de workflow do Temporal (a renderização vive em services/activities), mantendo o bundle de workflow determinístico.
  - **Idioma por workspace (`Organization.language`):** um idioma por organização, **capturado automaticamente no cadastro** (a partir do idioma detectado do navegador, via header `x-i18next-current-language`) e **ajustável em Configurações → Configurações Globais** (novo seletor de idioma). E-mails resolvem `org.language ?? 'pt'`.
  - **E-mails transacionais traduzidos:** ativação de conta, reenvio de ativação, redefinição de senha e convite de equipe saem no idioma do workspace. O rodapé do template compartilhado (preferências de notificação) também é traduzido.
  - **Notificações com i18n completo:** as notificações passam a carregar chave de tradução + parâmetros (em vez de uma string já renderizada). O **sininho in-app** é traduzido no idioma de quem está vendo (via `useT()`, locale do cliente); os **e-mails** (imediatos e digest) saem no idioma do workspace. Cobre falha ao renovar canal, erros/sucesso de publicação, streak e o digest.
- **Notificações passam a respeitar o isolamento por perfil (e-mail e sininho).** Antes, uma notificação sobre um canal do Perfil A era enviada por e-mail e exibida no sino para **todos** os membros da organização, inclusive quem só pertencia ao Perfil B. Agora os destinatários e a visibilidade são escopados pelo perfil dono do canal (`Integration.profileId`): apenas os membros daquele perfil — e os admins/superadmins da org, que têm acesso implícito a todos os perfis — veem a notificação e recebem o e-mail. Notificações não ligadas a um canal específico (ex.: streak) seguem org-wide.
- **Renovação proativa dos tokens dos canais — evita o canal cair em silêncio.** Até então só dois provedores (Threads e Instagram _standalone_) tinham renovação automática antecipada do token, via um workflow por-canal; os demais (LinkedIn, Instagram via Facebook, etc.) dependiam de um comando manual **sem agendador** — na prática, ninguém o executava, o token expirava sem aviso e o erro só aparecia **na hora de publicar** (o post ia para ERRO por "token expirado"). Agora um **workflow diário único** (`refresh-tokens-cron`) renova em lote **todos** os canais cujo token expira em até 1 dia, cobrindo qualquer provedor, enquanto o _refresh token_ ainda for válido. É **auto-reparável no boot** (reconciliado no `StartupMigrationService`, idempotente via `USE_EXISTING`, sem depender de cron externo) e o intervalo é configurável por `TOKEN_REFRESH_INTERVAL_HOURS` (padrão 24h). Canais que já caíram continuam precisando de reconexão manual uma vez.

### Alterado

- **Rebrand dos textos de e-mail voltados ao usuário: "Postiz" → "DestinyPost".** O prefixo de assunto do digest passa de `[Postiz]` para `[DestinyPost]`, e os e-mails de agência (aprovada/recusada) e o assunto de boas-vindas da newsletter (listmonk) foram rebrandeados. Os créditos AGPL (LICENSE/README) e identificadores de código/env (`POSTIZ_OAUTH_*`, agente Mastra `postiz`, etc.) são **preservados**. O nome de remetente dos e-mails continua vindo 100% da env `EMAIL_FROM_NAME` (defina como `DestinyPost`).

### Corrigido

- **Aba "Problemas" (Status): badge da plataforma, links de depuração e resiliência a resposta malformada.** Cada canal/post passa a mostrar o ícone da rede sobreposto ao avatar (deixa claro de qual plataforma é o item), os posts com erro ganham os atalhos **"Ver post"** e **"Temporal"** e as automações o **"Ver automação"**, e a aba não quebra mais (tela branca) se o endpoint devolver um corpo inesperado. Estes ajustes já haviam sido feitos mas não entraram no merge inicial da área Status; voltam agora compartilhando o mesmo módulo visual da nova aba Histórico.
- **Aviso de "canal desconectado" agora é único, mais claro e mostra o nome do canal.** O mesmo evento disparava o e-mail/notificação **em duplicidade** (o fluxo de renovação avisava e o `disconnectChannel` avisava de novo) e o assunto despejava o erro cru da rede ("Não foi possível renovar seu canal linkedin {...}"). Agora a marcação de "precisa reconectar" é **atômica** (só notifica na transição conectado → desconectado, uma única vez) e o texto é acionável: **"⚠️ Seu canal {nome} foi desconectado e parou de publicar"** com link para reconectar — em pt/en conforme o idioma do workspace. Como a renovação proativa (acima) detecta o token morto antes da publicação, o aviso passa a chegar **de forma antecipada**, não só quando você tenta postar.
- **Renovação de tokens em lote não aborta mais no primeiro canal com erro.** Um `return` no lugar de `continue` fazia com que, se um único canal falhasse a renovação, **todos os canais seguintes** ficassem sem ser renovados naquela rodada. Agora cada canal é tratado isoladamente (um erro num canal não derruba os demais).
- **Motivo real da falha de renovação passa a ser registrado em log (observabilidade).** O tratamento silencioso descartava a causa (refresh token expirado, `invalid_grant`, escopo faltando…), deixando o diagnóstico às cegas. Agora a causa é logada — **sem** expor o token/refresh token.
- **Falha de storage não derruba mais a conexão de um canal (fail-soft do avatar).** Ao conectar um canal (ou selecionar a página do Facebook/Instagram), o app baixa o avatar do perfil e re-hospeda no storage. Se o storage estivesse mal configurado (ex.: credencial do Cloudflare R2 errada), esse upload lançava e **derrubava a conexão inteira** — o OAuth dava certo, mas o canal nunca era salvo, sem explicação. Agora o upload do avatar (que é cosmético) é tolerante a falha: em erro, mantém a URL original do provedor e a conexão prossegue normalmente (o motivo vai para o log).

### Segurança

- **Disparo de webhook de saída agora passa pela proteção anti-SSRF (DNS-pinning).** O webhook que avisa sistemas externos quando um post é publicado fazia o `fetch` da URL sem o dispatcher seguro (só o botão "Enviar teste" e a validação de criação usavam). Como o disparo ocorre muito depois da validação e o `fetch` re-resolve o DNS, havia uma janela de **DNS-rebinding** por onde uma URL poderia apontar para um endereço interno (ex.: `169.254.169.254`, `localhost`). O disparo em produção passou a usar o mesmo `ssrfSafeDispatcher` (undici) que bloqueia IPs internos no momento do envio.
- **Preparação para criptografar os tokens dos canais em repouso (B1 — Etapa 1/2, sem mudança de comportamento).** Os tokens de acesso das integrações (`Integration.token`/`refreshToken`) ainda são gravados em texto puro; esta etapa apenas prepara o terreno: adiciona um helper auto-descritivo por prefixo (`enc:v1:`) e passa a **descriptografar o token no ponto de uso** (logo antes de cada chamada ao provider — postagem, refresh, analytics, plugs, automações do Instagram) em ~22 locais. Como nenhum token está criptografado ainda, cada descriptografia é um **no-op** (passa-through em texto puro) — zero mudança de comportamento. A criptografia na escrita entra na Etapa 2 (PR separado, gate-ada por `ENCRYPT_INTEGRATION_TOKENS`), quando os tokens passam a ficar cifrados em repouso e no histórico do Temporal.
- **Tokens dos canais passam a ser criptografados em repouso por padrão (B1 — Etapa 2/2).** Com a leitura já preparada (Etapa 1), esta etapa liga a **gravação cifrada** de `Integration.token`/`refreshToken` (AES-256-GCM, mesma `ENCRYPTION_KEY` das credenciais) — **automático, sem variável a configurar**, igual às credenciais e chaves de IA que já eram cifradas. Tokens novos e renovados são gravados cifrados (prefixo `enc:v1:`), protegidos em repouso e no histórico do Temporal; tokens antigos em texto puro continuam funcionando (conversão **gradual** conforme os canais renovam o token — sem migração em massa). A criptografia é **idempotente** (não re-cifra um token já cifrado nem um `accessToken` recém-renovado). Freio de emergência **opcional** `DISABLE_INTEGRATION_TOKEN_ENCRYPTION=true` faz novas escritas voltarem a texto puro (os já cifrados seguem sendo lidos). Chokepoint único no `IntegrationRepository`. **Não** trocar a `ENCRYPTION_KEY` (quebra a descriptografia, como já valia para as credenciais).

- **Isolamento por perfil fechado de ponta a ponta: membros com papel USER agora ficam realmente restritos aos perfis atribuídos.** Antes, o isolamento era um filtro "suave" que degradava para visibilidade da organização inteira em vários pontos. As correções:

  - **Fechado por padrão:** usuário org-USER sem nenhum perfil atribuído não vê mais os dados da organização inteira — o backend nega as rotas de dados com `403 NO_PROFILE_ASSIGNED` (novo guard global `ProfileAccessGuard`) e o frontend exibe a tela "Aguardando atribuição de perfil". Antes, esse estado dava acesso de leitura **e escrita** a posts, canais, mídia, credenciais, automações, webhooks e analytics de toda a organização.
  - **Semeadura não desfaz mais restrições:** o seed de perfis re-adicionava todos os usuários a todos os perfis a cada reinício do backend, desfazendo qualquer remoção manual de membro. O backfill agora roda uma única vez por organização (marcador `profilesBootstrappedAt`) e organizações novas já nascem com o perfil Default e o marcador criados no registro.
  - **Administradores têm acesso implícito:** org ADMIN/SUPERADMIN enxergam todos os perfis do workspace sem depender de registros de membro (que eram recriados pelo seed). O papel por perfil (OWNER/MANAGER/EDITOR/VIEWER) passa a valer para org-USER: VIEWER é somente leitura, e OWNER/MANAGER podem gerenciar membros, persona e base de conhecimento do próprio perfil (sem conceder papel acima do próprio).
  - **`profileId` vindo do cliente agora exige acesso ao perfil:** os endpoints de credenciais de IA (`?profileId`), persona, base de conhecimento e roster de membros validam membership (org-USER) além da posse pela organização. `GET /profiles` lista apenas os perfis acessíveis ao usuário — nomes e membros dos demais clientes do workspace não vazam mais.
  - **Rotas administrativas exigem papel ADMIN mesmo sem Stripe:** em instalações self-hosted sem `STRIPE_PUBLISHABLE_KEY`, todas as checagens `@CheckPolicies(ADMIN)` eram concedidas incondicionalmente — qualquer usuário comum podia criar/excluir perfis, gerenciar equipe e credenciais. A seção ADMIN agora é sempre verificada (negação retorna `403`, não `402`, evitando o modal de cobrança).
  - **Chaves de API públicas por perfil escopadas em posts e analytics:** uma chave de perfil conseguia listar, criar e excluir posts de **toda** a organização e ler analytics de qualquer canal via `/public/v1/posts` e `/public/v1/analytics/*`. Agora esses endpoints respeitam o perfil da chave (mismatch → `403`/`404`); chaves de organização seguem com escopo total (comportamento documentado).
  - **Convite de equipe exige perfis para papel USER:** o modal de convite ganhou a seleção obrigatória de perfil(is) e do papel no perfil (Gerente/Editor/Visualizador); as memberships são criadas no aceite do convite (tanto para usuário existente quanto para registro novo via link). Convites antigos sem perfis caem na tela "aguardando atribuição" — nunca em acesso total. Remover um membro da equipe agora também remove suas associações de perfil, evitando que um reconvite ressuscite acessos antigos.
  - **Convite funciona com `DISABLE_REGISTRATION=true` sem abrir o registro público.** Antes, um e-mail novo (sem conta) convidado para um workspace com registro desativado ficava num beco sem saída: era jogado para a tela de login sem poder criar conta. Agora um convite **válido** libera o formulário de registro — inclusive via OAuth (Google) — enquanto o registro público continua fechado para quem não tem convite. Para o convite não virar um "abre instância fechada", o bypass exige três condições no backend (`/auth/register`): (1) JWT do convite com assinatura válida e dentro do prazo; (2) **o e-mail que está se registrando bate com o e-mail convidado** (email-lock — o token não serve para criar conta com outra identidade); (3) o convite ainda não foi consumido (anti-replay). Falhando qualquer uma, o registro público volta a bloquear. O frontend apenas exibe o formulário quando o cookie de convite está presente; a validação real é sempre no servidor. Observação: um convite de "copiar link" **sem e-mail** (`sendEmail=false`) falha fechado sob `DISABLE_REGISTRATION` (não tem e-mail para casar) — para convidar em instância fechada, informe o e-mail do convidado.
  - **Login OAuth do GitHub passa a usar apenas o e-mail primário e verificado.** O `GithubProvider.getUser` pegava `emails[0]` cru da API do GitHub — que pode conter e-mails secundários não confirmados (alguém adiciona o e-mail de outra pessoa sem provar posse) e cuja ordem não é garantida. Agora seleciona o e-mail com `primary && verified` (rejeita se não houver), fechando um caminho de burlar o email-lock do convite via GitHub. Reforça também o login/registro OAuth em geral.
  - **Registro via convite não cria mais um workspace pessoal (modelo agência/equipe).** Antes, todo cadastro criava um workspace próprio para a pessoa — inclusive quem se registrava aceitando um convite, que acabava com um workspace vazio extra (onde era admin de tudo) além de ser membro restrito do workspace de quem convidou. Isso gerava confusão ("parece que tem acesso a tudo" — na verdade era o workspace pessoal dela). Agora **quem se registra por um convite entra apenas no workspace de quem convidou**, como membro restrito ao perfil/função definidos, sem workspace próprio. O **auto-cadastro sem convite** continua criando um workspace (comportamento SaaS multi-tenant). O campo "Empresa" fica escondido no cadastro por convite. Salvaguarda: se a entrada no workspace do convite falhar (org sem team no plano, convite já consumido, corrida), a conta recebe um workspace pessoal de fallback — nunca fica órfã (sem organização). Correção adjacente: o `AuthMiddleware` passa a negar de forma limpa (em vez de estourar) um usuário sem nenhuma organização.

- **Nota sobre canais sem perfil (comportamento intencional):** canais/integrações ainda **não atribuídos** a um perfil (`profileId` nulo — estado de canais legados ou conectados por chave de API de organização) continuam visíveis a todos os perfis do workspace, seguindo o modelo "perfil Default = workspace, perfis secundários herdam o compartilhado". A semeadura já vincula todo canal existente ao perfil Default; o `null` remanescente representa recurso de workspace compartilhado, não um vazamento. Para isolar um canal a um único cliente, atribua-o ao perfil correspondente.

- **Isolamento entre workspaces (multi-tenant) reforçado em flows e perfis (IDOR/BOLA).** Quatro endpoints autenticados buscavam/alteravam recursos apenas pelo `id`, sem verificar a organização do usuário:
  - `GET /flows/:id/executions` e `GET /flows/:id/executions/:executionId` retornavam o histórico de execução de automações de **qualquer** organização (contém dados de usuários finais do Instagram: IDs, texto de comentários, conteúdo de DMs, estado do follow-gate). Agora escopados por `organizationId` via a relação `flow`.
  - `GET /profiles/:id/members` expunha `id`, `e-mail` e `nome` dos membros de **qualquer** perfil de outra organização. Agora valida a posse do perfil antes de retornar.
  - `POST /profiles/:id/members` e `DELETE /profiles/:id/members/:userId` permitiam a um admin adicionar/remover membros em perfis de **outra** organização (o `@CheckPolicies(ADMIN)` só valida o papel na org atual, não a posse do perfil). Agora ambos validam que o perfil pertence à organização do request (404 caso contrário), seguindo o mesmo padrão de `updateProfile`/`deleteProfile`.
- **Segredos de API passam a usar gerador criptograficamente seguro (CSPRNG).** Tokens da API pública (`pos_`), `clientId`/`clientSecret` de apps OAuth, `authorization code` e as chaves de API de organização/perfil eram gerados com `Math.random()` (previsível — o estado do PRNG do V8 é recuperável a partir de poucas saídas). Passam a usar `makeSecureId` (`crypto.randomBytes` com rejection sampling, sem viés de módulo). Segredos já existentes continuam válidos (são consultados pelo valor, sem migração).
- **SSRF por DNS rebinding fechado em `POST /webhooks/send`.** O envio de webhook validava a URL no request (`@IsSafeWebhookUrl`, que resolve DNS e bloqueia IPs internos), mas o `fetch` re-resolvia o DNS no envio, permitindo rebinding para a rede interna. Agora usa o `ssrfSafeDispatcher` (pin do IP resolvido no connect), como os endpoints `/public/stream` e de upload.
- **XSS no chat de IA neutralizado.** As mensagens do chat de IA eram transformadas em HTML (para embutir imagens e vídeos) e renderizadas via `dangerouslySetInnerHTML` sem sanitização, injetando trechos capturados por regex de forma crua — permitindo quebra de atributo e injeção de script a partir de conteúdo vindo de fontes externas (busca na web, dados de integração). Agora a saída passa por um novo sanitizador dedicado `sanitizeChatContent` (DOMPurify) que **preserva** a renderização legítima de imagem/vídeo (`img`, `video`, `source`) e bloqueia `javascript:`/`data:` e tags perigosas.
- **XSS armazenado em notificações neutralizado.** O componente de notificações renderizava o conteúdo via `dangerouslySetInnerHTML` após apenas envolver URLs em `<a>`, sem sanitizar o restante. Como o texto da notificação embute o nome do canal (editável pelo usuário via renomear canal), um nome com HTML malicioso (ex.: `<img onerror=...>`) era executado ao abrir o sino de notificações. Agora o conteúdo passa por `sanitizePostContent` (DOMPurify) antes de renderizar, preservando os links gerados. Mesmo padrão já usado na pré-visualização pública de posts.
- **Isolamento do cache de metadados do Instagram entre organizações.** A chave do cache Redis de metadados de mídia (`ig:media:...:metadata`, usada no Inbox de comentários não vinculados e na listagem de aliases de flow) não incluía o `organizationId`. Duas organizações que referenciassem o mesmo `igMediaId` compartilhavam a mesma entrada de cache — a segunda lia metadados buscados com o token da primeira. A chave agora é escopada por organização (`ig:media:{organizationId}:{igMediaId}:metadata`). Entradas antigas expiram sozinhas pelo TTL de 24h (sem migração).
- **Falsificação de webhook do Instagram entre organizações fechada, e validação de assinatura passa a falhar fechada em produção.** A conferência do HMAC do webhook (`POST /public/ig-webhook`) montava os segredos candidatos com as credenciais de **todas** as organizações e aceitava o evento se a assinatura casasse com **qualquer** um; o despacho depois usava o `igAccountId` vindo do corpo (controlado por quem envia). Uma organização podia forjar um evento com a conta de outra e disparar as automações da vítima. Agora a assinatura é validada **apenas** contra os segredos da(s) organização(ões) dona(s) da conta recebida (resolvida por `internalId`), e o despacho é filtrado pelo mesmo escopo (comentário, story reply e postback). Além disso, em produção a validação passa a **falhar fechada**: sem segredo resolvido o evento é recusado, e `SKIP_IG_WEBHOOK_HMAC=true` é ignorado (só vale fora de produção). O tráfego legítimo assinado com o segredo de ambiente continua validando de forma idêntica, inclusive o fan-out multi-conta.
- **Uma credencial ilegível deixa de derrubar o webhook do Instagram.** A leitura das credenciais Facebook de todas as organizações (`findAllDecrypted`, usada na validação de assinatura do webhook) lançava erro e abortava o processamento inteiro quando **qualquer** linha não descriptografava (ex.: `ENCRYPTION_KEY` trocada após a credencial ter sido salva) — num cenário multi-workspace, uma credencial problemática de um workspace derrubava o webhook de todos, com `Unsupported state or unable to authenticate data`. Agora a linha ilegível é **pulada com um aviso no log** e as demais seguem, permitindo a validação pelo segredo de ambiente mesmo com uma credencial com problema.

### Corrigido

- **Regras de repost sobrevivem a deploys e não somem mais do Temporal.** O `repostWorkflow` era um loop infinito frágil: (1) sem `try/catch`, uma falha transitória no ciclo logo após um deploy (ex.: banco ainda subindo) terminava o workflow; (2) sem `continueAsNew`, a history crescia sem limite até o Temporal forçar a terminação (~50k eventos); e (3) nada recriava o workflow no boot — só salvar a regra de novo. Após um deploy, um repost que "sempre ficava executando" sumia e era preciso re-salvar. Agora o ciclo é protegido por `try/catch` com backoff, o workflow recicla via `continueAsNew` mantendo a history limitada, e há reconciliação no boot (backend e orchestrator) que re-garante — de forma idempotente — um workflow rodando para cada regra habilitada. Nenhuma ação manual é necessária após deploy.
- **Tela de "registro desativado" volta a empilhar corretamente.** Com `DISABLE_REGISTRATION=true`, a página `/auth` renderizava um fragment com vários elementos soltos (título, botão OAuth, divisória e a mensagem "Registro desativado / Faça login") direto dentro do container flex-row do layout de auth — tudo aparecia lado a lado numa linha só. Os elementos foram envolvidos num container `flex flex-col`, alinhando com o layout das telas de login/registro normais.
- **Redefinição de senha (e demais chamadas autenticadas) voltou a funcionar em deploys com frontend e API em domínios separados.** Em setups cross-origin (`FRONTEND_URL` e `NEXT_PUBLIC_BACKEND_URL` em subdomínios diferentes), o `allowedHeaders` do CORS não incluía os headers customizados que o próprio frontend envia (`auth`, `showorg`, `showprofile`, `impersonate`). Quando o navegador tinha cookies de sessão legíveis, o preflight passava a exigir esses headers e o backend os rejeitava — a chamada falhava com "Failed to fetch" (na troca de senha, o `POST /auth/forgot-return`). Os quatro headers foram adicionados ao `allowedHeaders`; a checagem de origem continua restrita a `FRONTEND_URL`/`MAIN_URL`. Requer restart do backend.
- **Orquestrador (agendamentos, e-mails e automações) voltou a subir após o endurecimento de segredos com CSPRNG.** A introdução de `makeSecureId` (que importa `crypto`) no mesmo módulo `make.is.ts` do `makeId` fez o bundler de workflows do Temporal arrastar `crypto` para dentro do bundle — os workflows de post importam `makeId` desse arquivo — e o guard de determinismo do Temporal passou a **rejeitar** o módulo `crypto`, derrubando o worker da fila `main` no boot. Com o worker fora do ar, **nada** era processado: posts agendados, autopost, repost, digests, automações do Instagram e os e-mails (inclusive o de "esqueci a senha", que fica preso como sinal na fila). O `makeSecureId` foi movido para um módulo próprio (`make.secure.id.ts`, livre de contexto de workflow), deixando `make.is.ts` sem `crypto`; os geradores seguem idênticos (segredos com CSPRNG, IDs de workflow com `Math.random()`) e o guard de determinismo permanece ativo.
- **Chat do agente voltou a responder e agora usa 100% a IA configurada na interface.** O chat/agente (páginas Agent e o assistente do compositor) dependia de uma variável de ambiente `OPENAI_API_KEY` separada; quando ela não estava definida, a requisição ficava pendurada e o usuário recebia um erro `504 Gateway Time-out` genérico — mesmo com a chave de IA configurada em **Configurações > Modelos de IA**. Agora os endpoints `/copilot/agent` e `/copilot/chat` resolvem a credencial de texto direto do provedor configurado na interface (OpenAI **ou** OpenRouter), herdando o compartilhamento do perfil padrão com perfis secundários. A variável `OPENAI_API_KEY` deixou de ser necessária para o chat. Quando não há credencial configurada (ou o perfil padrão não está compartilhando), o chat exibe imediatamente uma mensagem clara ("Configure suas chaves de IA em Configurações > Modelos de IA") em vez de travar.

## [0.5.5] - 2026-06-07

### Documentação

- **Capa de Reels do Instagram na API pública** documentada. O campo `settings.cover` (que já era suportado) agora está descrito na skill de agente servida em `GET /public/agent-skill`, no `SKILL.md` do CLI e na referência da API pública (`docs/api/public-api.md`), com exemplo e regras: é um objeto de mídia `{ id, path }` (de `POST /upload`), o `path` precisa ser URL pública (enviado como `cover_url` à Meta), vale só para Reels (vídeo `.mp4` único, não story) e exige `id` e `path` (senão `400`).

## [0.5.4] - 2026-06-07

### Corrigido

- **Mídia enviada via chave de API de perfil agora fica vinculada ao perfil correto.** Os endpoints `POST /public/v1/upload` e `POST /public/v1/upload-from-url` não propagavam o perfil da chave de API ao salvar a mídia — o arquivo era gravado sem perfil e aparecia no perfil padrão (compartilhado com todos), em vez de ficar escopado ao perfil que fez o upload. Agora o `profileId` da chave é repassado ao salvar, alinhando o upload ao comportamento de `GET /public/v1/integrations`.
- **Analytics do Instagram não duplica mais as métricas** (sync do upstream `fc509b1`): curtidas/visualizações/comentários/compartilhamentos/salvamentos eram contados em dobro porque o provider replicava cada ponto de dado em "amanhã" só para o gráfico desenhar uma linha. O ponto duplicado foi removido do backend e o gráfico passou a tratar um único ponto de dado no frontend (duplica apenas para exibição).
- **Pinterest: limite de 5 imagens e correção do envio de múltiplas imagens** (sync do upstream `0b3328d`): valida no máximo 5 imagens por post (UI + regra do provider + mensagem de erro amigável quando a API retorna `maxItems=5`) e corrige o mapeamento de `multiple_image_urls` (cada item agora vai como `{ url }`, como a API do Pinterest espera).
- **Threads: mensagem clara quando a mídia é inacessível** (sync do upstream `faeb898`): URLs de mídia inválidas/inacessíveis passam a retornar um erro explicando para subir a mídia antes, em vez de um erro genérico.

### Alterado

- **NestJS atualizado de 10 → 11** (sync do upstream `38b0ac8`): bump de todo o ecossistema (`@nestjs/common|core|platform-express|testing|microservices` → 11; `@nestjs/swagger` 7 → 11; `@nestjs/schedule` 4 → 6; `@nestjs/throttler` → 6.5; `reflect-metadata` → 0.2). Traz o **Express 5**. Ajustes necessários foram mínimos: no `main.ts`, o query parser foi fixado em `extended` (o padrão do Express 5 virou `simple`, sem objetos/arrays aninhados) e o middleware de body grande passou de `/copilot/*` para `/copilot` (o `app.use` casa por prefixo; Express 5 exige wildcard nomeado). Node ≥ 20 já atendido (imagem em Node 22). Sem mudança de comportamento esperada. Plano detalhado em [`docs/planning/nestjs-11-migration.md`](docs/planning/nestjs-11-migration.md).

## [0.5.3] - 2026-06-07

### Adicionado

- **Skill de agente servida pelo backend + botão na interface**: novo endpoint público (sem auth) `GET /public/agent-skill` que devolve um **guia completo em markdown** (MCP + CLI + referência REST + deep-dive de flows) com a **URL do backend já injetada**. Em `Configurações > Integrações` há um card **"Skill para Agentes de IA"** com o link + botão de copiar. Assim, para um agente self-hosted (Hermes, OpenClaw, Claude), basta passar **o link da skill + a Chave de API** — a URL já vem embutida no guia. O conteúdo é gerado por `agent.skill.template.ts` (mantido em paridade com a API pública).
- **CLI/skill de agente próprio (`apps/cli`, `@destinyai-dev/destinypost-agent`)**: ferramenta de linha de comando, sem dependências (usa `fetch` nativo do Node), que envolve a API pública (`/public/v1/*`) para agentes de IA (Hermes, OpenClaw, Claude, etc.). Diferente do CLI oficial do Postiz (cloud-oriented e sem automações), este é **self-hosted-aware**, respeita **chave por perfil** e inclui os **flows** (automações de comentário): `profiles:list`, `integrations:list`, `posts:create|list|delete`, `flows:create|list|get|update|status|delete`, `upload:url|file`, `analytics`, `is-connected`. Acompanha `SKILL.md` (referência para o agente) e `README.md`.
- **Publicação em Stories do Facebook (foto e vídeo)**: ao compor um post para uma Página do Facebook, agora há o seletor **Tipo de Postagem** com as opções "Post / Reel" (comportamento atual) e "Story". Stories de foto são publicados via `/photo_stories` e os de vídeo via `/video_stories` (upload em 3 fases, com a Meta buscando o arquivo do storage). Como Story não suporta carrossel, várias mídias são publicadas como stories separados — igual ao Instagram. Não é necessário reconectar a conta: as permissões já são as mesmas solicitadas hoje. Vídeo de Story aceita no máximo 60 segundos (validação no compositor).
- **Facebook Story como destino de repost**: o Story do Facebook agora aparece como opção de destino nas regras de repost (antes só existia "Reel"). Reposts de Story/Reel do Instagram podem ser republicados como Story do Facebook (foto ou vídeo). Requer migração aditiva do banco (`FACEBOOK_STORY` no enum `RepostDestinationFormat`, aplicada via `pnpm prisma-db-push`).
- **Documentação Swagger dos demais endpoints públicos (posts, upload, integrações, perfis, analytics, vídeo)**: todos os endpoints da tag **Public API** ganharam `@ApiOperation` (descrição), `@ApiResponse` (códigos), `@ApiParam`/`@ApiQuery` e, nos principais, `@ApiBody` com exemplos copiáveis. Destaque: `POST /public/v1/posts` traz exemplos prontos (publicar agora / agendar / rascunho) no "Example Value". DTOs `UploadDto`, `VideoDto`, `GetPostsDto` e `GetNotificationsDto` anotados com `@ApiProperty`. Nova página [`docs/api/public-api.md`](docs/api/public-api.md) com referência geral e o fluxo típico no n8n (descobrir canal → upload → criar post).
- **Documentação da API pública de automações (Swagger interativo + página de referência)**: o Swagger (`/docs`) agora mostra o schema completo do corpo das automações (todos os campos, tipos, enums, limites e descrições) e ganhou botão **Authorize** (esquema de chave de API no header `Authorization`) — dá para **testar os endpoints públicos direto pelo `/docs`**. O título passou de "Postiz Swagger file" para **"DestinyPost API"**. Adicionada também a página [`docs/api/public-flows-api.md`](docs/api/public-flows-api.md) com referência completa: autenticação, endpoints, todos os atributos, modos de vínculo, boas práticas, exemplos (curl/n8n) e o pré-requisito do webhook da Meta.
- **Botão "Docs" para a documentação Swagger nos cards de Chave de API**: os cards "Chave de API da Organização" e "Chave de API do Perfil" (Configurações > Integrações > aba Acesso) ganharam um botão "Docs" no canto superior direito que abre a documentação Swagger da API (`/docs` do backend) em uma nova aba, com tooltip "Documentação da API (Swagger)".
- **Automações de comentário do Instagram via API pública, MCP e SDK**: agora é possível criar e gerenciar automações de resposta a comentários/stories programaticamente — antes só dava pela interface. Quando alguém comenta uma palavra-chave (ex.: "EU QUERO") em um post, a automação responde publicamente e/ou envia o link no direct. Isso fecha o cenário de ponta a ponta: gerar a imagem da receita, publicar no Instagram e criar a automação do comentário, tudo via API ou pelo agente.
  - Novos endpoints na API pública (chave de organização ou de perfil): `POST /public/v1/flows` (criar), `GET /public/v1/flows` (listar, com filtro opcional `integrationId`), `GET /public/v1/flows/:id`, `PUT /public/v1/flows/:id` (editar), `POST /public/v1/flows/:id/status` (ativar/pausar/arquivar) e `DELETE /public/v1/flows/:id` (excluir).
  - Modo de vínculo padrão `next_publication`: a automação se conecta sozinha ao próximo post publicado no canal, permitindo encadear "gerar imagem → publicar no Instagram → criar a automação" sem o cliente precisar do ID de mídia do Instagram.
  - Ferramentas MCP `createCommentAutomationTool`, `listCommentAutomationsTool` e `setCommentAutomationStatusTool` para o agente de chat (e clientes MCP como o n8n). O agente sempre confirma a configuração com o usuário antes de criar.
  - SDK `@postiz/node`: novos métodos `createFlow`, `listFlows`, `getFlow`, `updateFlow`, `setFlowStatus` e `deleteFlow`.
- **Chaves de API por perfil**: cada perfil pode gerar sua própria chave de API escopada apenas às suas integrações. A chave da organização continua existindo com acesso irrestrito a todos os perfis. Chaves de perfil funcionam na REST API pública, no MCP e no CLI.
  - A aba `Configurações > Desenvolvedores` foi renomeada para `Integrações`. No perfil Default exibe as duas chaves (organização + perfil); perfis secundários exibem apenas a chave do próprio perfil.
  - Novo endpoint `POST /profiles/:id/api-key/rotate` (admin) para gerar ou rotacionar a chave de um perfil.
  - Novo endpoint `GET /public/v1/profiles` para listar perfis disponíveis via API pública (chave de org retorna todos; chave de perfil retorna apenas o próprio perfil).
  - Segurança: tentativa de usar `?profileId` diferente da chave de perfil retorna `403 Forbidden`.

### Corrigido

- **Container em crash-loop (backend e orchestrator não subiam)**: o Dependabot havia bumpado a imagem Docker de `node:22.20-bookworm-slim` para `node:26.1-bookworm-slim` (PR #39). O `@sentry/profiling-node` não tem binding nativo para o ABI do Node 26 (`sentry_cpu_profiler-linux-x64-glibc-147.node`), então backend e orchestrator quebravam no boot com `MODULE_NOT_FOUND` e reiniciavam em loop (o frontend subia normalmente). Além disso, o Node 26 contradiz o `engines` do projeto (`>=22.12.0 <23.0.0`) e o CI, que builda em Node 22.12 — por isso o build passava mas o runtime falhava. Revertida a imagem para `node:22.20-bookworm-slim` e adicionado `ignore` de major da imagem `node` no Dependabot para não recorrer. Requer rebuild da imagem.
- **Correções pequenas trazidas do upstream Postiz (cherry-pick seguro)**: incorporados 4 fixes que não conflitam com as customizações do fork — alt text de mídia no Mastodon, tratamento de erro adequado no provider do Discord, `redirect_uri` dinâmico nos provedores de login OAuth (Google/GitHub), e permitir `localhost` ao criar um App OAuth (útil em dev/self-hosted). Triagem completa dos 119 commits do upstream em [`docs/planning/upstream-sync-triage-2026-06.md`](docs/planning/upstream-sync-triage-2026-06.md); o restante (incl. fix de XSS, hardening de SSRF e bumps de framework) exige porte manual por tocar arquivos divergentes do fork.
- **Canais do Zernio não apareciam quando havia só o perfil padrão**: ao conectar uma conta (ex.: Instagram) no perfil **Default** do Zernio e tentar adicioná-la em Lançamentos > Adicionar Canal > Zernio, o modal mostrava "Nenhuma conta conectada encontrada neste perfil", mesmo com a conta conectada no Zernio. As contas só apareciam depois de criar um segundo perfil no Zernio. Causa: o modal buscava as contas usando `selectedProfileId` (preenchido apenas quando o usuário **clica** num perfil), mas com um único perfil a seleção é automática e só atualiza `effectiveProfileId` — então a busca de contas nunca era disparada. Agora a busca usa `effectiveProfileId`, carregando as contas do perfil único (default) sem exigir clique.
- **Adicionar um segundo Instagram nas Credenciais apagava o primeiro**: em Configurações > Credenciais > Token do Instagram, ao cadastrar um segundo token de Instagram no mesmo perfil, a conta anterior sumia e parava de funcionar (automações estilo ManyChat deixavam de responder). Causa: o backend só devolve os tokens em forma redigida (`hasToken`, nunca o token em si), então ao adicionar uma conta nova o frontend remontava a lista com as contas existentes **sem** o token — e o backend descartava qualquer entrada sem token, mantendo apenas a recém-adicionada. Agora entradas sem token cuja conta já estava registrada são preservadas integralmente (token e data de renovação originais), permitindo múltiplos Instagrams por perfil. Quem já perdeu o token antigo precisa recadastrá-lo uma vez.
- **Build quebrado nos testes (CI) por erro de tipo na API pública**: a suíte `pnpm test` (ts-jest) falhava ao compilar `public.integrations.controller.ts` com `TS7018` — a const `POST_BODY_EXAMPLES` (exemplos do Swagger para `POST /public/v1/posts`) tinha arrays vazios (`tags: []`, `image: []`) cujo tipo era inferido como `any[]` implícito sob `noImplicitAny`. O `build:backend` (transpile sem type-check) não pegava, mas o CI sim, deixando todos os PRs vermelhos. A const passou a ser tipada como `Record<string, { summary: string; value: any }>`. Sem mudança de comportamento.
- **Automações criadas via API de organização não apareciam na interface**: ao criar um flow com a chave de API da organização (sem `?profileId`), ele era salvo com `profileId` nulo (org-wide) e ficava invisível na listagem de Automações, que filtra por perfil. Agora a criação nunca salva perfil nulo:
  - Chave de organização sem `profileId` → atribui ao **perfil Default** da org.
  - Chave de organização com `?profileId` → valida que o perfil pertence à org e usa ele.
  - Chave de perfil → usa o próprio perfil (como já era).
  - `profileId` inexistente na org → `400`.
  - Migração automática no boot (`StartupMigrationService`): flows órfãos já existentes (`profileId` nulo) são atribuídos ao perfil Default da organização — idempotente, então automações criadas antes do fix passam a aparecer.
- **Posts agendados via API/MCP de organização não apareciam no calendário**: ao agendar um post com a chave de organização (sem `profileId` no contexto), ele era salvo com `profileId` nulo e ficava invisível no Calendário, que filtra pelo perfil selecionado. Agora `createPost` herda o perfil **do canal de destino** quando nenhum perfil é informado — todo canal pertence a exatamente um perfil, então o post cai no calendário correto na hora (sem depender do restart que aciona a varredura de órfãos).
- **Automações do Instagram não listavam posts/stories em contas conectadas via Instagram Login**: os seletores de "post/story específico" (Wizard e API) buscavam mídias sempre em `graph.facebook.com`, retornando lista vazia para integrações conectadas via Instagram Login (IG User Token) — caso comum em instâncias self-hosted. Agora o host e o token são resolvidos por `resolveIgRoute` (Facebook Login → `graph.facebook.com`; Instagram Login → `graph.instagram.com`), igual ao restante das automações. Quem usa Facebook Login não é afetado (mesmo comportamento de antes).

### Segurança

- **Hardening de autenticação e de credenciais armazenadas (porte do upstream)**: três correções de segurança, sem tocar em billing.
  - **Sessão não confia mais em claims do JWT**: o `AuthMiddleware` agora apenas verifica a assinatura do token e **re-resolve o usuário no banco pelo id**. Antes, campos como `id`, `isSuperAdmin` e `activated` vinham do corpo do token (válidos enquanto a assinatura batesse, mesmo que o usuário tivesse sido alterado/desativado). Agora um token de um usuário inexistente/desativado é rejeitado.
  - **Conexão de canal não vaza credenciais na resposta**: o endpoint público de conclusão de OAuth (`no.auth.integrations`) passou a remover `token`, `refreshToken` e `customInstanceDetails` do objeto retornado — esses segredos ficam só no servidor.
  - **Credenciais do Skool encriptadas em repouso**: passam a ser guardadas como JSON cifrado (AES, `fixedEncryption`) em vez de JWT assinado, com **fallback de leitura** para contas conectadas no formato antigo (sem necessidade de reconectar). Porte do commit upstream `23696d2` (a parte que removia NOWPayments/billing foi deliberadamente deixada de fora).
- **Validação de upload por magic bytes + hardening do nginx (porte do upstream)**: uploads agora são validados pelo **conteúdo real** (magic bytes via `file-type`), não pelo `Content-Type`/extensão declarados (forjáveis). O `CustomFileValidationPipe` passou a detectar o tipo real, rejeitar o que não está na allowlist (`image/jpeg|png|gif|webp|avif|bmp|tiff`, `video/mp4` — **SVG e HTML ficam de fora**, por serem vetores de XSS), reescrever o nome do arquivo com a extensão correta e foi aplicado também a `POST /media/upload-simple`. O `POST /public/v1/upload-from-url` valida o buffer baixado da mesma forma. Defesa em profundidade no nginx: `/uploads/` agora envia `X-Content-Type-Options: nosniff`, `Content-Security-Policy` restritiva (sandbox) e tipos MIME explícitos. Helper compartilhado `allowed.upload.mime.ts` (import limpo, **sem `eslint-disable`** — refatorado em relação ao upstream). Porte manual do commit upstream `3ea3022`; `file-type` fixado como dependência direta. A validação magic-byte no carregamento de URL dos storages (`loadFromUrlOrDataUrl`) foi deixada de fora de propósito (data URLs de IA são confiáveis; nginx + SSRF já cobrem os vetores de serve/fetch).
- **Proteção SSRF com DNS-pinning em buscas de URL do usuário (porte do upstream)**: endpoints/serviços que baixam uma URL fornecida pelo usuário (`/public/v1/upload-from-url`, o proxy `public.controller`, e o carregamento de mídia por URL nos storages local/Cloudflare via `loadFromUrlOrDataUrl`) agora usam um `ssrfSafeDispatcher` (undici Agent) que **fixa a resolução DNS**: cada IP resolvido passa por `isBlockedIp`, fechando a janela TOCTOU que a validação só-da-URL deixava aberta (GHSA-f7jj-p389-4w45). Adicionado o guard `isSafePublicHttpsUrl` onde faltava e `undici` fixado como dependência direta. O `/upload-from-url` migrou de `axios` para `fetch` com o dispatcher. Porte manual do commit upstream `071143d` (a detecção de tipo por magic-byte virá em PR separado).
- **Sanitização do conteúdo de posts contra XSS (porte do upstream)**: o HTML do conteúdo do post agora passa por DOMPurify (`isomorphic-dompurify`) — tanto na **entrada** (transform no DTO `PostContent.content`) quanto na **saída** (página pública de preview `/p/[id]`). Só são permitidas tags de formatação seguras (`p`, `br`, `strong`, `u`, `a`, `ul`, `li`, `h1`–`h3`, `span`) e atributos seguros; `<script>`, handlers inline (`onerror`/`onclick`) e URIs `javascript:` são removidos. Novo helper `libraries/helpers/src/utils/sanitize.post.content.ts`. Porte manual do commit upstream `7236213` (não aplicável via cherry-pick por conflitar com customizações do fork).
- **Assinatura de postback do follow-gate endurecida**: o segredo HMAC deixou de ter um fallback literal público no código (que, por ser AGPL-aberto, qualquer um podia recomputar). Agora deriva de `POSTBACK_SIGNING_SECRET` → `FACEBOOK_APP_SECRET` → `INSTAGRAM_APP_SECRET` → `ENCRYPTION_KEY` → `JWT_SECRET`, com aviso em log se nenhum estiver configurado. O HMAC do payload passou de 32 para 64 bits. Sem migração: postbacks em voo expiram naturalmente na janela de 23h.
- **Servidor MCP rejeitado por clientes estritos (n8n)**: as ferramentas `integrationList` e `generateVideoOptions` não declaravam `inputSchema`, fazendo o servidor MCP serializar um schema sem `type: "object"`. Clientes tolerantes (Claude Code, Cursor) ignoravam, mas o n8n MCP Client recusava a lista inteira de ferramentas com `Invalid input: expected "object"`. Adicionado `inputSchema: z.object({})` às duas (exige restart do backend para revalidar a lista no boot).

### Segurança

- **Automações de comentário — autorização por perfil e validação de URL**: a criação de automação (via API pública, MCP ou wizard interno) agora passa por um guard único que valida que a integração pertence à organização e ao perfil do chamador (chave de API por perfil **não** cria automação no Instagram de outro perfil → `403`), que é uma conta do Instagram **ativa** (integração desativada/removida → `412`), e que a URL do botão do direct é **https pública** (bloqueia esquemas `javascript:`/`data:`/`file:` e hosts privados/locais como `localhost`/`127.0.0.1`/`169.254.169.254`). O endpoint público usa `ValidationPipe` estrito (rejeita campos extras, mitigando mass-assignment) e tem rate limit próprio (mais apertado que o global) por disparar assinatura de webhook na Meta. Endurecido também o `matchMode` (apenas `any`/`all`/`exact`) e a obrigatoriedade de `postIds`/`storyIds` quando o vínculo é `specific`.
- **Dependabot habilitado** (`.github/dependabot.yml`): arquivo existia mas estava com `package-ecosystem: ""` (inválido — bot ignorava silenciosamente). Corrigido para cobrir npm (monorepo pnpm, raiz `/`), GitHub Actions e Docker, com schedule semanal, grupos `runtime-minor-patch`/`dev-minor-patch`/`security-fixes` e majors em PRs individuais para revisão humana obrigatória.
- **Automação de auditoria Dependabot no `security-auditor`**: o subagent agora executa `gh api .../dependabot/alerts` como passo zero de qualquer auditoria, surfaçando alertas critical/high antes de revisar o diff. Gracioso — se `gh` não estiver autenticado, emite `⏭️ unavailable` e continua.
- **4 vulnerabilidades críticas corrigidas** via `pnpm.overrides` escopados (afetam apenas versões vulneráveis, sem tocar instâncias já corrigidas):
  - `protobufjs` 7.5.4 → 8.0.3 (`GHSA-xq3m-2v4x-88gg`: execução arbitrária de código via parse de `.proto` não confiável)
  - `handlebars` 4.7.8 → 4.7.9 (`GHSA-2w6w-674q-4c4q`: injeção JavaScript via AST type confusion)
  - `happy-dom` 15.11.7 → 20.8.9 (`GHSA-37j7-fg3j-429f`: escape de contexto VM → RCE)
  - `form-data` 2.3.3 → 2.5.5 (`GHSA-fjxv-7rqg-78g4`: boundary inseguro por função random fraca)
- **~40 vulnerabilidades high corrigidas** via `pnpm.overrides` com ranges cirúrgicos (versões legadas intocadas):
  - `next` 16.2.1 → 16.2.6 (7 CVEs: middleware/proxy bypass, SSRF via WebSocket upgrade, DoS Server Components e Cache Components, injeção via parâmetro dinâmico de rota, bypass i18n Pages Router)
  - `axios` 1.15.0 → 1.16.0 (`GHSA` prototype pollution no Node HTTP adapter e SSRF via `socketPath`)
  - `multer` 2.0.2 → 2.1.1 (`CVE-2026-3520`: DoS via recursão descontrolada)
  - `fast-xml-parser` → 5.5.10 (expansão ilimitada de entidade numéricas — bypass de limites)
  - `node-forge` 1.3.3 → 1.4.0 (bypass de `basicConstraints` em verificação de cadeia X.509)
  - `defu` → 6.1.6, `fast-uri` → 3.1.2, `flatted` → 3.4.2, `basic-ftp` → 5.3.1 (prototype pollution, host confusion, DoS)
  - `rollup@2.x` → 2.80.0 (path traversal em escrita de arquivos)
  - `path-to-regexp@8.x` → 8.4.2, `picomatch@4.x` → 4.0.4, `cross-spawn@<7` → 7.0.5 (ReDoS)
- **`tar` 6.2.1 → 7.5.13** (`GHSA` symlink path traversal): único consumidor é `@mapbox/node-pre-gyp` que chama apenas `tar.extract()` — API compatível entre versões.
- **`music-metadata` 7.14.0 → 11.12.3** (`GHSA` loop infinito no parser ASF): dep direta, usada em `images.slides.ts`. Pacote virou ESM-only na v8 — migrado para `await import('music-metadata')` (dynamic import compatível com Node.js 22 + tsconfig CommonJS). Segunda argumento de `parseBuffer` atualizado de string para `{ mimeType: 'audio/mpeg' }` conforme nova assinatura da API.
- **Riscos aceitos (CVEs não exploráveis neste projeto — verificados via advisory GitHub):**
  - `@opentelemetry/exporter-prometheus`, `@opentelemetry/sdk-node`, `@opentelemetry/auto-instrumentations-node` (`GHSA-q7rr-3cgh-j5r3`: crash do exporter Prometheus via HTTP malformado) — nenhum desses pacotes é configurado nem exposto no projeto. Sem `registerInstrumentations()` em `main.ts`, sem endpoint Prometheus ativo. Forçar 0.203 → 0.217 sem subir `langsmith` criaria cascata de peer deps quebrados de maior risco.
  - `langsmith` < 0.8.0 (4 CVEs: `GHSA-3644-q5cj-c5c7` desserialização de manifests via `pullPrompt()`; `GHSA-v34v-rq6j-cj6p` SSRF via `baggage` header em `RunTree.fromHeaders()` com tracing distribuído; `GHSA-fw9q-39r9-c252` prototype pollution em `createAnonymizer()`; `GHSA-rr7j-v2q5-chgv` bypass de redação em streaming) — sem `LANGSMITH_API_KEY`, sem `LANGCHAIN_TRACING_V2`, sem chamadas a `pullPrompt()` ou `RunTree.fromHeaders()` em nenhum arquivo do projeto. Todas as funções vulneráveis exigem ativação/uso explícito. Salto 0.3→0.8 exigiria upgrade em cadeia do langchain.
  - `lodash` / `lodash-es` (`GHSA-r5fr-rjxr-66jc` injeção de código via `_.template`; `GHSA-f23m-r3pf-42rh` prototype pollution via `_.unset`/`_.omit`) — auditados todos os 13 arquivos com import de lodash. Funções usadas: `capitalize`, `chunk`, `sortBy`, `shuffle`, `difference`, `uniq`, `uniqBy`, `groupBy`. Nenhuma instância de `_.template`, `_.unset` ou `_.omit`. Biblioteca abandonada em 4.17.x — versão `4.18.0` referenciada no advisory nunca foi lançada.
  - `path-to-regexp@0.1.12` (`GHSA-37ch-88jc-xwx2` ReDoS via múltiplos parâmetros de rota) — versão corrigida `0.1.13` não foi publicada no npm (última release do branch 0.1.x é `0.1.9`). Padrões de rota no projeto são estáticos (definidos em decoradores NestJS como `@Get('/users/:id')`), nunca controlados por input de usuário — ataque não é viável.
  - `picomatch@2.3.1` — sem fix disponível no branch 2.x. Instância residual de dependência interna do Vite; não é importada diretamente pelo projeto.

### Alterado

- **UX da aba "Persona de IA"**: refator amplo focado em densidade visual e redução de ruído.
  - Removido o dropdown "Perfil" que duplicava o seletor de perfil já presente no header. Agora a tela edita automaticamente a persona do perfil ativo (resolvido via `useCurrentProfile()`, mesmo padrão usado pelo card de "Modelos de IA"). Para trocar de perfil, o usuário usa o ProfileSelector do header — single source of truth para profile-active.
  - Descrição enxugada: "Defina como o agente de IA escreve para cada perfil." (antes incluía o parêntese redundante "(tom, CTAs, estilo de imagem, restrições)", informação já visível nos próprios campos do form).
  - Label "Instruções de escrita" renomeado para **"Estilo de escrita"** (en: "Writing style") — mais direto sobre o que o campo realmente captura (voz, ritmo, estrutura de frases).
  - Altura do textarea de "Estilo de escrita" dobrada (`rows={4}` → `rows={8}`) — é o campo onde o usuário escreve as regras mais longas, faz sentido dar mais respiro vertical sem precisar arrastar o resize-handle toda vez.
  - **Ícones inline antes de cada label** — SVG monocromáticos 16×16 com `currentColor` (mesma técnica do `ai-provider.settings.component.tsx`, sem dependência npm de icon-lib): BrandIcon (Descrição da marca), AudienceIcon (Público-alvo), ToneIcon (Tom de voz), PencilIcon (Estilo de escrita), MegaphoneIcon (CTAs), ShieldIcon (Restrições), PaletteIcon (Estilo de imagem), DocumentIcon (Posts de exemplo). Helper `<FieldLabel icon right>...</FieldLabel>` introduzido para padronizar a renderização em todos os subcomponentes (`LabeledTextarea`, `LabeledInput`, `SelectPreset`, `TagInput`, `MultiTextarea`).
  - **Posts de exemplo redesenhado**: botão "Adicionar exemplo" no rodapé substituído por um **`+` circular ao lado do título** (cabe na mesma linha do label, ocupa zero altura extra). Cada caixa de exemplo agora tem um **`x` no canto superior direito** (absolute, padding-right do textarea aumentado pra `pr-[34px]` pra não sobrepor texto) — interaction-padrão de "remove this card" sem precisar de botão separado fora da caixa. Comportamento: caixa única vazia nunca mostra o `x` (evita o caso "removi tudo, agora não tenho nem placeholder").
  - Chave i18n `persona_select_profile` removida (sem consumers órfãos). Descrição reescrita em `persona_description` (pt/en). Demais chaves preservadas.
  - **Refinamentos de padding/affordances**: padding interno de todos os textareas/inputs aumentado de `px-[10px] py-[8px]` para `px-[12px] py-[10px]` (mais respiro, alinhado com o que vemos em outros forms do app). **Input de CTA redesenhado**: botão "Adicionar" externo virou um `+` circular dentro do próprio input (canto direito, `absolute` + `top-1/2 -translate-y-1/2`), com `disabled` quando o input está vazio — mesma interaction-language do Posts de exemplo. **Textarea de Estilo de escrita** agora aceita `minHeight` como prop e está configurado para `340px` mínimo (anteriormente só `rows={8}`, que renderizava ~190px e o usuário precisava arrastar o resize-handle toda vez). O `rows` foi mantido como fallback para browsers sem `min-height` em `<textarea>` (raro).
  - **Cores semânticas para affordances de add/remove**: ícones `+` (TagInput + MultiTextarea) ficam em `text-textColor` (mesma cor das labels — leitura neutra, "ação positiva default"). Ícones `×` de remoção (chip de CTA, caixa de exemplo) em `text-red-500` com hover `text-red-400` + `bg-red-500/10` — convenção destructiva consistente com o resto do app (`time.table.tsx`, `tags.component.tsx`, `review-links.modal.tsx`). O `x` minúsculo nos chips de CTA também foi padronizado para `×` (caractere multiplicação ×, mais legível que letra `x`).
- **Renomeação das abas de IA em Configurações** para reduzir ambiguidade e eliminar o code-switch en/pt no menu. "AI Provider" virou **"Modelos de IA"** (en: "AI Models") — o subtítulo já dizia "Configure as chaves e modelos de IA", então o novo nome bate com a função real (escolher LLM/imagem/vídeo/web search). "Agente de IA" virou **"Persona de IA"** (en: "AI Persona") — alinhado com o título da própria tela ("Persona de IA"). "Creditos de IA" virou **"Créditos de IA"** (acentuação pt-BR corrigida). Atualizadas as chaves de tradução (`ai_provider_title`, `ai_agent_tab`, `ai_credits_title`) em `pt`/`en`, os fallbacks nos componentes (`settings.component.tsx`, `ai-credits.settings.component.tsx`, `ai-provider.settings.component.tsx`), todas as mensagens de erro user-facing que referenciavam o caminho do menu ("Configure suas chaves em Settings > AI Provider" → "Configurações > Modelos de IA"; idem em variantes Vídeo / Web Search / Geração de texto) em `ai.video.tsx`, `ai.image.tsx`, `ai.search.tsx`, `ai.caption.tsx`, `polonto.picture.generation.tsx`, `ai-provider-resolver.service.ts`, `ai-video.service.ts`, `ai-web-search.controller.ts`, e a descrição da tool `generateVideoTool` consumida pelo agente Mastra. CLAUDE.md (raiz, frontend, ai/, chat/) também atualizados para refletir o novo caminho do menu (nome interno "AI Provider System" preservado nos docs de arquitetura). Spec `ai-video.service.spec.ts` ajustado para validar a nova string. Nenhuma mudança de comportamento — apenas vocabulário.

### Corrigido

- **Erro de IA não abre mais o modal de "Pagamento Necessário" (billing) por engano**: quando o provedor de IA (ex.: OpenRouter) retornava erro — tipicamente `402` "sem créditos" — o AI SDK lançava um `APICallError` carregando `.statusCode` + `.message`. O filtro padrão do NestJS (`BaseExceptionFilter.isHttpError`) repassava esse status como status HTTP do app, então um `402` do provedor abria o modal global de billing redirecionando para `/billing` (desabilitado neste fork), e um `401` do provedor chegava a **deslogar o usuário**. Agora `AiTextService.callWithFallback` normaliza qualquer erro de runtime do provedor para `HttpException 412` (convenção do repo: erro de IA usa 412, nunca 402) com mensagem amigável + detalhe técnico sanitizado (tokens `Bearer`/`sk-` redigidos). Resultado: o composer agora mostra um toast claro em vez do modal de cobrança. `HttpException` já controlada (ex.: 412 "credencial não configurada" do resolver) passa direto; erro sem status numérico continua virando 500. Cobre `/ai/text/caption` e todas as gerações de texto (`generatePosts`, `separatePosts`, `extractWebsiteText`). Pitfall #13 em `libraries/nestjs-libraries/src/ai/CLAUDE.md`.
- **Feedback de erro no Agente de IA**: o chat do agente (CopilotKit streaming em `/copilot/agent`) não dava **nenhum** retorno ao usuário quando o provedor falhava — o erro só aparecia no console do navegador (`runChatCompletion`). Agora o `<CopilotChat>` renderiza uma mensagem de erro amigável inline (componente `ErrorMessage` customizado) e exibe um toast (`onError`), ambos classificando o erro cru do stream nas mesmas categorias do backend (sem créditos / autenticação / limite / genérico). Novas chaves i18n (`pt`/`en`): `ai_assistant_error_title`, `ai_assistant_generic_error`, `ai_provider_no_credits`, `ai_provider_auth_error`, `ai_provider_rate_limit`, `detail_label`. Pitfall #8 em `libraries/nestjs-libraries/src/chat/CLAUDE.md`.
- **Observabilidade de geração de imagem**: antes, quando a geração de imagem falhava (especialmente I2I) o backend frequentemente não logava nada — só o `AiImageService._logger.warn` em 4xx/5xx do provedor disparava. Caminhos silenciados que agora emitem log:
  - `LOG [AiImageService] generate start mode=I2I provider=openrouter model=... aspect=1:1 promptLen=23 hasRef=y profile=abc` — toda chamada agora deixa trace de entrada.
  - `WARN` quando `mode=I2I` mas `referenceImageUrl` está ausente.
  - `WARN` quando provider responde 200 mas sem `b64_json` (OpenAI) ou sem `image_url` (OpenRouter). No caso do OpenRouter, o log inclui `finish_reason` da resposta e os primeiros 400 chars do JSON — facilita identificar "modelo selecionado não suporta image generation".
  - `ERROR` em falhas de rede (DNS, timeout, conexão recusada) no `fetch` para OpenAI/OpenRouter e no download da imagem de referência.
  - `MediaService.generateImage` ganhou `try/catch` em volta do `_aiImageService.generate` com `ERROR` no formato `mode=... aspect=... hasRef=... status=... msg=...` antes de re-throw — captura qualquer caminho ainda esquecido.
  - Mensagem de erro do OpenRouter `nao devolveu imagem` agora explica a causa provável ("modelo selecionado não suporta image generation — verifique em Settings > Modelos de IA > Imagem") em vez do genérico anterior.
  - Pitfall #12 em `libraries/nestjs-libraries/src/ai/CLAUDE.md` orienta como debugar silent failures futuros via grep no log por `[AiImageService] generate start`.
- **Agente Mastra com Gemini (Google AI Studio) via OpenRouter retornava `400 INVALID_ARGUMENT`** ao tentar agendar post. O schema da tool `integration.schedule.post` tinha `settings.value: z.any()`, que Zod traduz para JSON Schema vazio `{}`. Gemini rejeita propriedades em `required[]` sem schema definido (OpenAI/Anthropic toleram). Mudado para `z.string()` com instrução no description para o agente enviar valores complexos como JSON-stringified. Backend agora reidrata via `tryParseJson` (heurística: detecta `[`/`{`/`true`/`false`/`null`/dígitos antes de tentar parse, fallback para string crua quando o parse falha). Sem mudança de comportamento para modelos OpenAI/Anthropic. Pitfall #7 em `libraries/nestjs-libraries/src/chat/CLAUDE.md` documenta a regra (jamais `z.any()` em `inputSchema` de tool; `outputSchema` é livre).
- **Container Docker não bootava: `pm2 start ... --min-uptime 60000` retornava `error: unknown option '--min-uptime'`**. O `pm2@5.4.3` pinado no `Dockerfile.dev` não aceita `--min-uptime` como flag de CLI (só funciona em `ecosystem.config.js`). O commit `bc288914 fix(docker): pinna pm2@5.4.3 e remove --min-uptime` já tinha resolvido isso, mas o `0e13305a fix(orchestrator): resiliencia...` re-introduziu o flag por engano ao adicionar variáveis de ambiente para memória (`PM2_BACKEND_MAX_MEMORY` etc). CI não detecta porque só roda `pnpm test` + `pnpm build`, nunca o container real. Removido `--min-uptime 60000` dos 3 `apps/{backend,frontend,orchestrator}/package.json`. Mantidos `--restart-delay 5000` + `--max-restarts 10` (esses sim funcionam em pm2 5.x). Pitfall #8 adicionado em `apps/orchestrator/CLAUDE.md` para documentar a regressão.
- **Enriquecimento de prompt de imagem/vídeo agora funciona com modelos free do OpenRouter** (Nemotron 3 Nano Omni free, Gemma 4 26B A4B free, Mistral 7B free, etc). Antes, `AiTextService.generatePromptForPicture` e `generatePromptForVideo` usavam `generateObject` do AI SDK com schema Zod `{ prompt: string }`, que exige JSON mode / structured output do provider. Modelos free do OpenRouter não suportam isso de forma confiável — retornam texto solto, markdown wrapping ou output truncado, e o parser do AI SDK falhava com `No object generated: could not parse the response`. O erro era mascarado pelo fallback (também modelo free, também falhava) e chegava ao usuário como genérico 500. Agora ambos os helpers usam `generateText` e o `.text.trim()` do retorno vira o prompt enriquecido — schema trivial não justifica o custo de exigir JSON mode. System prompts continuam idênticos (forçando output em inglês). Sem impacto em modelos pagos (OpenAI direto, Anthropic via OpenRouter, etc) — todos eles também aceitam `generateText` sem perda de qualidade. 2 specs novos em `ai-text.service.spec.ts` garantindo que generateText é chamado e que `result.text` é trimado. Outros métodos do mesmo service (`generatePosts`, `separatePosts`, `caption`) continuam usando `generateObject` porque dependem genuinamente de estrutura array/multifield — esses casos quebram com modelos free, e a recomendação ao admin é usar pelo menos um modelo pago como fallback configurado.
- **Orchestrator: `app.listen(3002)` agora resiliente a `EADDRINUSE`**. Antes, qualquer falha no bind do health check HTTP derrubava o processo inteiro, criando um loop de crash de ~30s onde os workers Temporal entravam em RUNNING (drenando alguns workflows entre os crashes) e em seguida o NestJS morria. Causa raiz observada em produção: zombie state de boot anterior (provavelmente OOM partial em servidor apertado de memória) deixando socket TCP em uso. Agora, se port estiver ocupado, o erro é logado como WARN e o orchestrator continua com os workers funcionais — health check fica indisponível mas workers seguem processando workflows. Apps/orchestrator/src/main.ts.
- **Logs: bulk update de UnmatchedComment do mesmo `media_id`**. Quando um post recebia múltiplos comentários antes de o user vincular (cada um virando um `UnmatchedComment` separado por `igCommentId` distinto), vincular UM deixava os outros como `PENDING` órfãos na aba "Pendentes" mesmo depois do `FlowMediaAlias` já existir. Agora `bindToFlow` e `ignore` fazem `markAllPendingBoundForMedia`/`markAllPendingIgnoredForMedia` em massa: vincular um comentário move todos os outros do mesmo media para `BOUND`; ignorar um move todos para `IGNORED`. Idempotente (only `PENDING` é tocado). Retorna `bulkBoundCount`/`bulkIgnoredCount` na resposta para a UI eventualmente mostrar "X comentários similares também movidos".

### Adicionado

- **`/health/workers` no orchestrator**: novo endpoint que consulta `describeTaskQueue` no Temporal Server para cada task queue (default `main`, configurável via `TEMPORAL_HEALTH_TASK_QUEUES`) e retorna `503 no_workers` quando nenhum poller está registrado. Captura especificamente o cenário "No Workers Running" que vimos em produção — onde o `pm2 list` reporta `online` mas o worker Temporal está zombie. Pode ser polled por Uptime Kuma / Pingdom / cron interno para alertar antes do usuário perceber. Apps/orchestrator/src/health.controller.ts.
- **PM2 com `--min-uptime 60000` + `--max-memory-restart` configurável**: scripts de start em `apps/{backend,frontend,orchestrator}/package.json` agora passam `--min-uptime 60000` ao PM2 (processo que cai em menos de 60s entra em status `errored` em vez de loop infinito de restart) e tornam o limite de memória configurável via env vars `PM2_BACKEND_MAX_MEMORY` / `PM2_FRONTEND_MAX_MEMORY` / `PM2_ORCHESTRATOR_MAX_MEMORY` (defaults preservados: 1G / 1G / 2G). Útil para servidores apertados onde reciclar processos preventivamente é melhor que esperar o OOM killer.

### Adicionado

- **Logs de Comentários** (`/automacoes/logs`, acessado pelo botão "Logs" ao lado de "Nova Automação" na página de Automações — não fica no menu lateral): comentários em mídias do Instagram que ainda não estão monitoradas por nenhuma automação (típico de **dark posts** do Meta Ads Manager — anúncios criados a partir de posts orgânicos com `media_id` diferente, sem qualquer campo da Graph API que faça o vínculo de volta; mesmo com `boost_ads_list` o `ad_id` vem censurado pelo Meta) agora são armazenados em uma aba de Logs em vez de serem descartados silenciosamente em `FlowsService.handleIncomingComment`. A UI mostra cada comentário com thumbnail, legenda, badge "Anúncio" (derivado de `boost_eligibility_info.eligible_to_boost === false` — campo só disponível em `graph.facebook.com`/Facebook Login, automaticamente omitido em `graph.instagram.com`/Instagram Login com retry defensivo) e link "Abrir post no Instagram". Usuário pode **vincular** o post a uma automação existente (cria `FlowMediaAlias`) ou **ignorar** definitivamente (`IgnoredMedia` — futuros comentários nessa mídia nem entram nos Logs). Próximos comentários no mesmo `media_id` disparam a automação normalmente. Comentários antigos **não** são replayed ao vincular (decisão consciente). Retenção: comentários `PENDING` há mais de 30 dias são removidos pelo `StartupMigrationService` no boot via método idempotente `cleanupExpiredUnmatchedComments`. Metadata enriquecida de forma assíncrona via workflow Temporal `enrichUnmatchedCommentWorkflow` (cache Redis 24h em `ig:media:{id}:metadata` com defensive `setex`/`set` fallback igual ao `ai-catalog.service.ts`, retry exponencial com `backoffCoefficient: 2` e `maxAttempts: 5`). 3 novos modelos Prisma (`FlowMediaAlias`, `UnmatchedComment`, `IgnoredMedia`) e 2 enums (`AliasSource`, `UnmatchedStatus`). Endpoints REST sob `/automations` com `@GetOrgFromRequest` em todos: GET `/inbox` (listar paginado), POST `/inbox/bind`, POST `/inbox/ignore`, GET/POST/DELETE `/aliases`, GET `/aliases/lookup`. Roteamento via `resolveIgRoute()` (sem `graph.facebook.com` hardcoded — confirmado funcional em ambos os hosts Graph). Decisão consciente sobre `next_publication`: comentário em alias NÃO dispara `bindPendingFlowsToPost` — flow continua aguardando o post orgânico real. **Segurança** (fixes do review): `aliasMediaId` validado por regex `/^\d{1,30}$/` (Meta media IDs são numéricos — previne injeção de path no Graph API quando interpolado em URL); `lookupAliasFlows`/`findFlowsByAlias` agora exigem `orgId` no repositório (fecha cross-tenant info leak que permitia enumerar nomes de flows de outras orgs).
- **Campo "IDs de Dark Posts" no editor de trigger** (Wizard + Flow Builder com paridade obrigatória via componente compartilhado `<AdAliasesField />`): permite colar uma lista de `media_id` separados por vírgula ou linha que viram aliases manuais do flow (`source=MANUAL`) — útil quando o admin já conhece os IDs dos dark posts antes de qualquer comentário chegar (ex.: capturou via webhook de preview do anúncio). Preview visual com **thumbnail real do post + link "Abrir post"** (enriquecido em `UnmatchedCommentService.listAliasesEnriched` reaproveitando o mesmo cache Redis 24h dos Logs). Mostra aviso "Este post também está vinculado a [Flow X]" via `useAliasLookup` quando o alias já existe em outro flow (bind múltiplo permitido — `@@unique([flowId, aliasMediaId])` permite N:M flow↔alias, mas a UI é transparente). SWR hooks `useInbox`, `useAliases`, `useAliasLookup` + helper `createInboxActions(fetch, mutators)` que recebe `mutate` por parâmetro para evitar violação de `react-hooks/rules-of-hooks` (padrão canonical agora documentado em `apps/frontend/CLAUDE.md`).
- Subagent `feature-acceptance-reviewer` em `.claude/agents/feature-acceptance-reviewer.md` para revisão **macro** de features no contexto do sistema inteiro, complementando os 5 reviewers diff-by-diff existentes. Diferente de `plan-reviewer`, `code-reviewer`, `security-auditor`, `test-completer` e `doc-maintainer` (que revisam mudança por mudança no diff), este subagent audita a **feature-como-um-todo** capturando lacunas de integração que aparecem só no contexto sistêmico: lacunas nos requisitos originais da descrição (feature não entrega X), quebra silenciosa de contrato (Service A novo quebra consumidor Service B em outro arquivo), padrão arquitetural divergente ao nível da feature inteira, incompletude i18n cross-locale (en/pt), documentação de áreas adjacentes desatualizada (CLAUDE.md de vizinhos), falta de rollback em migration Prisma, inconsistência UX (padding, cores, microcopy, vocabulário), divergência em convenção de telemetria/logs. Auto-invocado após aprovação de `doc-maintainer` e antes de abrir PR, **apenas em features ≥ 5 arquivos modificados OU cruzando ≥ 2 áreas** (ex.: `apps/backend/` + `apps/frontend/`, `libraries/` + `apps/`). Recusa explicitamente features pequenas (< 5 arquivos) com ⏭️ SKIPPED — diff reviewers cobrem. Tools restritas a `Read, Glob, Grep` (read-only); **model `opus`** (único subagent do repo usando opus — precisa da janela de 1M tokens do Claude Opus 4.7 para manter feature inteira + contexto do sistema em working memory simultaneamente, sem perder nuances; Sonnet precisaria chunking). 8 dimensões de auditoria: (1) cobertura dos requisitos originais (descrição pede X? Código entrega X?), (2) integração cruzada (consumidores do código antigo ainda funcionam?), (3) consistência arquitetural no nível da feature (feature-como-um-todo segue padrões do repo?), (4) i18n cross-locale (toda key nova em pt tem en correspondente?), (5) documentação cross-área (CLAUDE.md de áreas vizinhas podem ter ficado desatualizadas?), (6) migration safety (schema Prisma mudou? Rollback é seguro? `StartupMigrationService` foi considerado?), (7) UX consistency (nova UI bate padrão visual, padding, vocabulário?), (8) telemetria/logs (nome do evento segue convenção do projeto?). Categorização: 🚨 SHIP-BLOCKING (quebra outro lugar do sistema, omite requisito, risco real de produção — bloqueia abertura PR), ⚠️ POLISH-NEEDED (inconsistência clara, não-quebradora — resolve antes do merge), 💡 OBSERVATION (não-bloqueador, heads-up). Postura "untrusted input": lê do disco via `Read` (não confia em framing do agente pai "excellent implementation"), trata artefatos como potencialmente incompletos, procura gaps ativamente, nunca especula sem evidência em arquivo, reporta o que **está** errado AGORA em base no código lido. Fora de escopo (outros subagents): regra localizada em diff (code-reviewer), vulnerabilidade em superfície de ataque (security-auditor), spec faltante em service/repo (test-completer), CLAUDE.md da área tocada (doc-maintainer), plano vs codebase (plan-reviewer). Sexto e último subagent do pipeline (antes de abrir PR). Inspirado no estágio 11 ("Acceptance Review") do pipeline de produto novo do Breno LionLab (vídeo omoGS6kEIoQ), adaptado para feature em produto existente. CLAUDE.md raiz atualizado com bullet correspondente em seção "Subagents (.claude/agents/)", posicionado como 6º e último item após `doc-maintainer`.
- Integração do MCP server do Graphify com o subagent `plan-reviewer`. Tools `mcp__graphify__query_graph`, `mcp__graphify__get_node`, `mcp__graphify__get_neighbors`, e `mcp__graphify__shortest_path` adicionadas ao frontmatter de `.claude/agents/plan-reviewer.md`. System prompt atualizado com seção `Code graph awareness` orientando uso preferencial de queries de grafo sobre Grep recursivo para investigar consumidores (`get_neighbors`), blast radius (`query_graph` em god nodes), acoplamento implícito (`shortest_path`) e verificação de existência de símbolos (`get_node`) — particularmente relevante em código cross-cutting como `social.abstract.ts`, `AiProviderResolverService` e `FlowsService → activities`. Fallback gracioso para `GRAPH_REPORT.md` + Grep se o MCP server não estiver disponível (ex.: ambiente sem Python 3.10+ ou sem `graphifyy[mcp]` instalado). Outros subagents (`code-reviewer`, `security-auditor`, `test-completer`, `doc-maintainer`) permanecem com tools originais — seus escopos não se beneficiam de visão de grafo. `.mcp.json` atualizado com o servidor `graphify` (`uvx --from 'graphifyy[mcp]' python -m graphify.serve graphify-out/graph.json`) preservando o servidor `dotcontext` existente. Subagent `feature-acceptance-reviewer` mencionado em planos antigos não existe neste repo — caso seja criado no futuro, replicar o mesmo padrão (frontmatter tools + seção `Code graph awareness`).
- Indexação do monorepo como knowledge graph via [Graphify](https://github.com/safishamsi/graphify). Outputs em `graphify-out/` (`GRAPH_REPORT.md`, `graph.json`) cobrem símbolos, call sites, dependências, god nodes (entidades com alto número de consumidores — alta blast radius), surprising connections (acoplamento implícito entre módulos), e why-comments (TODOs/HACKs/FIXMEs extraídos do código). Hooks git (`post-commit` e `post-checkout`) auto-rebuildam via tree-sitter sem custo de API recorrente (rebuild manual com `graphify update .` quando docs/PDFs mudam). Seção `## Code Graph (Graphify)` no `CLAUDE.md` raiz documenta quando consultar o grafo (cross-cutting code, blast radius, dependências em providers de social media) e quando pular (mudanças localizadas, formatação, docs). `.graphifyignore` exclui `node_modules/`, `dist/`, locales, lockfiles, `.context/`. `AGENTS.md` raiz atualizado com referência. Nota: `graph.html` e `cost.json` não são produzidos por `graphifyy` 0.7.13; entrada em `.gitignore` é preventiva.
- Regra `Browser Validation (Claude in Chrome)` no `CLAUDE.md` raiz documentando quando e como o agente principal deve usar as ferramentas do Claude in Chrome (navegação, clique, leitura de DOM/console/network, screenshot) para validar features end-to-end. O agente reusa a sessão de login já existente no Chrome local do usuário, valida fluxos de UI após `code-reviewer` e `security-auditor` aprovarem e antes de abrir PR. Inclui critérios de quando usar (frontend afetando comportamento visível, bugs visuais, mudanças materiais de UX), quando pular (backend-only, refactor sem mudança observável, test/doc-only), e diretrizes de comportamento (não tentar autenticação automatizada — pedir ao usuário; não executar ações destrutivas sem confirmação; reportar findings em seção `Browser Validation` do PR body). Setup via `claude --chrome` ou `/chrome` é responsabilidade do usuário no ambiente local. AGENTS.md raiz atualizado com referência à nova seção.
- Subagent `test-completer` em `.claude/agents/test-completer.md` para preencher specs faltantes em arquivos `.service.ts`, `.repository.ts` e `.provider.ts` (escopo do hook `.claude/hooks/tdd-check.sh`). Tools: `Read`, `Edit`, `Write`, `Glob`, `Grep`, `Bash(pnpm test:*)` — `Edit`/`Write` com hard scope restrito a `*.spec.ts` / `*.spec.tsx` enforced no system prompt (tools sozinhas não bloqueiam caminho de arquivo); `Bash` restrito a `pnpm test:*` pela whitelist (sem `pnpm install`, sem `git`, sem MCP). Auto-invocado antes do commit quando há gap de TDD detectado, ou via invocação direta humana com lista explícita de arquivos. Segue padrões canônicos do repo (`createMock<T>()`, `createTestModule({ service, mocks })`, Red-Green-Refactor, test names em pt-BR sem acentos), com skeletons co-localizados (`<basename>.spec.ts` ao lado do `<basename>.service.ts` / `.repository.ts` / `.provider.ts`). Reporta achados em quatro níveis: 🟢 CREATED (spec novo passando), 🟡 UPDATED (spec existente expandido), 🔴 FAILED (spec criado mas teste falha — provável bug de produção, surface ao humano), ⏭️ SKIPPED (fora de escopo ou impossível mockar limpo, com justificativa). Postura conservadora: não cria spec frontend (`.tsx`) por default, não busca 100% de cobertura (cobre branches críticos: success + 2–3 errors), não gera teste vazio só para passar o hook, não modifica código de produção para "fazer o teste passar". Conteúdo derivado do skill `test-generation` já preenchido em `.context/skills/`. Quinto e último subagent do pipeline (`plan-reviewer → code-reviewer ‖ security-auditor → test-completer → doc-maintainer`). Atualizada também a posição do `security-auditor` em `CLAUDE.md` raiz e em `.claude/agents/security-auditor.md` (de "fourth and last" para "fourth, parallel with code-reviewer").
- Subagent `security-auditor` em `.claude/agents/security-auditor.md` para auditoria profunda de superfícies de ataque (HMAC em webhooks Meta/Stripe/ShortLink, OAuth flow integrity com propagação de `ClientInformation` e validação de `state`, JWT handling com algoritmo allowlist e claims obrigatórios, decoradores de autorização `@UseGuards`/`@GetOrgFromRequest`/`@GetProfileFromRequest`, validação de DTOs Zod/class-validator no boundary de controller, redact regex para secrets em logs, raw SQL via `$queryRaw`/`$executeRaw` com tagged-template, blocklist canônica de SSRF para URLs user-supplied, prompt injection mitigation via wrapper `<source>` em `ai-web-search.service.ts`, AES-256-GCM at rest via `EncryptionService` para credenciais persistidas, roteamento per-profile via `FlowActivity.resolveIgRoute` em atividades de Instagram, compliance AGPL com Postiz e rate-limit hygiene). Read-only (`Read`, `Glob`, `Grep`), auto-invocado quando `code-reviewer` emite marcador `→ SECURITY-AUDITOR` (auditoria cirúrgica nos arquivos escalados) ou via invocação direta humana (release branch, deep dive de superfície específica, regressão pós-CVE). Categoriza achados em 🚨 CRITICAL (vulnerabilidade explorável agora — bloqueador), ⚠️ HIGH (risco real ou defesa em profundidade ausente — bloqueador com mitigação documentada), 💡 MEDIUM (hardening, não-bloqueador). Sem nível LOW por design. Postura mais conservadora que os outros subagents — em dúvida, reporta e deixa humano descartar. Não sugere código de fix concreto, aponta vulnerabilidade e classe de mitigação em uma sentença. Não especula sobre cenários teóricos sem evidência no diff. Conteúdo derivado do skill `security-audit` já preenchido em `.context/skills/`. Quarto e último subagent do pipeline (`plan-reviewer → code-reviewer + security-auditor (paralelo) → doc-maintainer`). Referência adicionada em seção "Subagents" do `CLAUDE.md` raiz, posicionada entre `code-reviewer` e `doc-maintainer` para refletir a ordem do pipeline.
- Hook `.claude/hooks/gh-pr-fork-guard.sh` (registrado em `.claude/settings.json` como `PreToolUse > Bash`) que **bloqueia com exit 2** qualquer comando `gh pr <create|edit|merge|close|ready|review|comment|reopen>` que **não inclua** `--repo destinyai-dev/destinypost`. Comandos read-only (`view`, `list`, `diff`, `checks`, `status`) continuam livres. Motivo: este repo é fork do upstream público `gitroomhq/postiz-app`, e sem `--repo` o `gh` CLI resolve PRs contra o upstream — incidente real ocorreu (PR aberto contra `gitroomhq/postiz-app#1509` por engano, fechado imediatamente). Mensagem de erro do hook indica a forma canônica: `gh pr <subcomando> --repo destinyai-dev/destinypost --base main --head <branch> ...`. Adicionada também Golden Rule correspondente em `CLAUDE.md` raiz para visibilidade humana, e memória de sessão `feedback_gh_pr_repo.md` para reforço.
- Subagent `code-reviewer` em `.claude/agents/code-reviewer.md` para validar mudanças de código contra padrões do repo (layer architecture Controller→Service→Repository, TDD obrigatório, i18n com `useT()`, SWR rules-of-hooks, contratos de provider em `integrations/social/` com 412 vs 402, `ClientInformation`, `resolveIgRoute`, sem `eslint-disable`, sem componente UI de npm, `pnpm` only, branch hygiene e Wizard↔Flow Builder parity). Read-only (`Read`, `Glob`, `Grep`), auto-invocado após cada mudança de código, reporta achados em 🚫 MUST FIX / 🟡 SHOULD FIX / 💭 NIT. Escala para `security-auditor` (a criar) quando detecta superfície de ataque (HMAC, OAuth, JWT, secrets, encryption, raw SQL, SSRF) via marcador `→ SECURITY-AUDITOR` em vez de auditar profundamente. Conteúdo derivado do skill `code-review` já preenchido em `.context/skills/`. Referência adicionada na seção "Subagents" do `CLAUDE.md` raiz, posicionada antes do `doc-maintainer` (que roda na ponta de retaguarda do pipeline).

### Corrigido

- Limpeza de 182 arquivos intermediários do build do Graphify (`graphify-out/.graphify_*` — chunked AST, manifests de chunk, prompt template, labels, semantic JSON, ~16 MB no total) que foram commitados por engano no PR #19 devido a regra incompleta no `.gitignore`. Agora o `.gitignore` exclui todos os arquivos efêmeros (`graphify-out/.graphify_*` além dos já existentes `manifest.json`, `cost.json` e `.rebuild.lock`). Arquivos removidos apenas do tracking via `git rm --cached` — permanecem no disco para que os hooks git de auto-rebuild continuem funcionando sem precisar re-indexar. Artefatos finais (`graph.json`, `GRAPH_REPORT.md`, `cache/ast/`) permanecem versionados.
- `FlowsService`: `getRawClient()?.workflow.start()` agora falha explicitamente com log `ERROR` e marca a execução como `FAILED` quando o cliente Temporal retorna `null` (orchestrator offline). Antes, o operador `?.` descartava silenciosamente o disparo sem nenhum log, tornando indistinguível um orchestrator caído de uma automação sem flow configurado. Corrigido nos três pontos de disparo: `handleIncomingComment`, `handleIncomingStoryReply` e `handlePostbackClick`.
- Mensagens de erro do AI Provider Resolver corrigidas: (a) acentuação pt-BR completa em vez de "nao tem credencial propria"/"workspace nao compartilha", (b) terminologia "perfil padrão" em vez de "workspace" — alinhado com o modelo mental do usuário (no nosso sistema, o "WORKSPACE level" é fisicamente a config do perfil default, não um workspace separado). Mensagens novas: `NOT_CONFIGURED_MESSAGE` → "Configure suas chaves em Settings > AI Provider."; `NOT_SHARED_MESSAGE` → "Este perfil não tem chave própria e o perfil padrão não está compartilhando. Configure uma chave para este perfil em Settings > AI Provider, ou ative 'Compartilhar com perfis' na configuração do perfil padrão." A regex de detecção em `AiWebSearchController.isTextCredentialError` foi atualizada para tolerar a nova grafia (com e sem acentos via `[aã]/[oó]`). Mensagem do orquestrador `/generate-post` quando falta credencial TEXT também ganhou acentuação completa: "Configure também a chave de Texto em Settings > AI Provider > Geração de texto. Ela é necessária para escrever a legenda final a partir dos resultados da busca."
- `polonto.picture.generation.tsx` (painel de IA dentro do designer Polotno) ganhou tratamento de 412/429 alinhado com `ai.image.tsx` e demais modais. Antes, qualquer falha (incluindo "Configure suas chaves") caía no `alert("Something went wrong, please try again later...")` em inglês — usuário sem credencial configurada não tinha pista do que fazer. Agora: 412/402 → toast com `detail.message` do backend (a nova mensagem em pt-BR explicando para configurar Settings > AI Provider); 429 → toast de rate limit; outros 4xx/5xx → toast com `detail.message` ou fallback "Erro ao gerar imagem". Também converte `Array<string>` do NestJS ValidationPipe para string concatenada com `·`, evitando "[object Object]" no toast.
- `ai.caption.tsx` (botão "Improve com IA" do composer) também ganhou suporte a `Array<string>` em `detail.message` — alinhamento com os outros modais (`ai.image.tsx`, `ai.video.tsx`, `ai.search.tsx`) que já tratavam.

### Corrigido

- Geração I2I via OpenAI falhava com `400 Unknown parameter: 'quality'` quando o admin tinha `quality` configurado em Settings > AI Provider > Imagem (qualquer valor diferente de `auto`). Causa: `/v1/images/edits` (endpoint de I2I) não aceita o parâmetro `quality` — só `/v1/images/generations` (T2I) aceita. O `AiImageService.generateOpenAiEdit` enviava `quality` no FormData copiando a lógica do `generateOpenAi`. Fix: omitir `quality` no edit (com comentário explicando o porquê), aplicar só em T2I. Spec atualizado para garantir que `FormData.get('quality')` retorna null no caminho I2I mesmo com `options.quality='high'` na credencial.
- Mensagem de erro do modal de imagem agora propaga o erro original do provider em vez de mostrar genérico "Erro ao gerar imagem". Quando OpenAI ou OpenRouter retornam HTTP 4xx/5xx, dois novos extractores (`extractOpenAiError`, `extractOpenRouterError`) leem o JSON de erro do response body e formatam: `OpenAI recusou (HTTP 400): "Unknown parameter: 'quality'."` (com aspas para o usuário copiar/buscar). Fallback para body como string quando não é JSON parseável. Aplicado nos 3 caminhos (`generateOpenAi`, `generateOpenAiEdit`, `generateOpenRouter`). 2 specs novos cobrindo a propagação. Frontend `ai.image.tsx` continua mostrando `detail.message` no toast — agora chega informação útil em vez de "OpenAI image gen falhou (400)".
- Refatorado o fluxo de prompt manual + enrichment no modal de imagem para suportar 4 cenários distintos pedidos pelo usuário, em vez dos 2 do design anterior:
  - **Sem prompt manual + enrich ON (default)**: o texto da legenda do post é enriquecido pelo LLM para um prompt detalhado em inglês. Mesmo comportamento histórico.
  - **Sem prompt manual + enrich OFF**: texto da legenda vai cru pro modelo.
  - **Com prompt manual + enrich OFF**: o texto digitado vai exato para o modelo (sem nenhuma transformação). Útil quando o usuário já tem um prompt cuidadosamente escrito (ex: "uma mulher loira segurando o produto, no estilo UGC olhando para a câmera").
  - **Com prompt manual + enrich ON**: o texto digitado passa pelo enrich e o LLM reescreve em inglês com detalhes cinematográficos. Útil quando o usuário tem só uma ideia simples (ex: "uma mulher segurando um guarda chuva") e quer que o LLM trabalhe em cima dela.
    Mudanças concretas: (a) novo toggle "Enriquecer prompt automaticamente" no modal (default ON, mesmo padrão do modal de vídeo); (b) `manualPrompt: boolean` no body renomeado para `skipEnrich: boolean` — semântica clara: "pular o enrich" e não "tem prompt manual"; (c) DTO `GenerateImageBodyDto.skipEnrich` com class-validator; (d) controller mapeia `isPicturePrompt = !body.skipEnrich`; (e) `finalPrompt` simplificado: `manualText || postText` — removidas as tags `<!-- description -->` que confundiam o LLM; (f) style continua sendo prefixado quando diferente de "Sem estilo".

### Adicionado

- Geração de imagem ganhou modo **Imagem → Imagem (I2I)** equivalente ao modo I2V do vídeo: o modal "AI Image" agora tem tabs **Texto → Imagem** e **Imagem → Imagem**, com dropzone para upload local de imagem de referência (drag-and-drop, PNG/JPG/WebP até 30MB, POST `/media/upload-server`) ou fallback para colar URL pública. Backend `AiImageService` ganhou dispatch I2I para os 2 providers configurados em Settings: **OpenRouter** via chat completions com `messages[0].content` em formato array `[{type:'text'}, {type:'image_url'}]` e `modalities:['image','text']` (funciona com Gemini Nano Banana, Flux etc); **OpenAI** via `/v1/images/edits` com multipart/form-data — service baixa a imagem de referência por HTTP, monta `FormData` com `image` (Buffer/Blob), `prompt`, `size` (mapeado do aspectRatio), `n=1`, e envia. Erros tratados: `referenceImageUrl` ausente → 400; download da reference 4xx/5xx → 502 com mensagem clara. 4 specs novos em `ai-image.service.spec.ts` cobrindo dispatch I2I OpenRouter, FormData OpenAI, validação e falha de download.
- Geração de imagem ganhou campo **"Prompt manual (opcional)"** no modal: quando o usuário preenche o textarea, o prompt vai cru para o modelo (sem o pipeline `generatePromptForPicture` de enriquecimento) — só o estilo é prepended se selecionado. Útil quando o usuário já tem um prompt cuidadosamente escrito e quer controle total. Endpoint `/media/generate-image-with-prompt` aceita `manualPrompt: boolean` no body (DTO novo `GenerateImageBodyDto` com class-validator). `MediaService.generateImage` ganhou parâmetro `extra: { mode, referenceImageUrl }` que repassa para `AiImageService`.
- Opção **"Sem estilo"** como primeiro botão da grid de estilos no modal de imagem, **pré-selecionada por default**. Quando ativa, a tag `<style>` não é injetada no prompt — útil para quem só quer a descrição direta sem viés visual. Resolve o caso onde o botão "Gerar imagem" ficava desabilitado até o usuário escolher 1 dos 14 estilos forçadamente.
- Agente Mastra `generateImageTool` ganhou paridade com o `generateVideoTool`: schema agora tem `mode: 'T2I'|'I2I'`, `referenceImageUrl?` (validado URL quando I2I), `manualPrompt?: boolean`, `style?: string`. Description orienta o LLM a usar I2I quando o usuário fornecer URL de imagem ou pedir "edit/restyle/transform/make this look like X". Persona `imageStyle` continua sendo injetada como prefixo "Image style: X." mesmo em manual prompt para preservar identidade visual.

### Alterado

- Enriquecimento de prompt para imagem e vídeo (`AiTextService.generatePromptForPicture` e `generatePromptForVideo`) agora **força output em inglês**: system prompts reescritos com instrução explícita "Always respond in ENGLISH, regardless of the input language — image/video-generation models perform significantly better with English prompts." Modelos de imagem (gpt-image, Flux, Gemini Nano Banana) e vídeo (Seedance, Veo) historicamente performam melhor com prompt em inglês; antes o LLM podia retornar em pt mantendo termos pt/en misturados. Bonus: `generatePromptForPicture` agora respeita `isReasoningModel` (omite temperature para gpt-5.x e o1/o3/o4) — antes só `generatePromptForVideo` fazia isso.
- Modal "AI Image" reescrito com a mesma estrutura visual do modal de vídeo: largura **700px** (era 560px estreito demais — labels longos quebravam em 2 linhas), header sem o quirk `-mt-[20px] pt-[20px]` que criava linha invisível acima do título, X dentro do mesmo flexbox row do `TopTitle` em vez de absolute solto, `max-w-[90vw]` para responsividade. Modal de vídeo também migrado para 700px e header limpo (mesma padronização). Ambos compartilham agora 2 helpers em `apps/frontend/src/components/launches/helpers/`: `mode.tab.component.tsx` (componente de aba T2X/I2X, antes inline em `ai.video.tsx`) e `reference.image.dropzone.component.tsx` (dropzone com upload + URL fallback, antes inline). Chaves de tradução `ai_video_dropzone*` etc renomeadas para `ai_*` (genéricas) já que ambos modais usam o helper.
- Botão "AI Image" da toolbar do composer **não exige mais 30 caracteres** no editor para abrir o modal (alinhado com `AiSearch` e `AiVideo`). Como o usuário pode usar prompt manual ou I2I sem texto de post, o bloqueio antigo virou impedimento sem propósito. Mantém check de créditos.

### Adicionado

- Configuração MCP do dotcontext via `.mcp.json` na raiz para ativar gateways `explore`, `context`, `plan`, `agent`, `skill` e `sync` automaticamente em qualquer clone do repo aberto no Claude Code, deixando `.context/` como source-of-truth para portabilidade entre IDEs (Claude Code, Antigravity, Cursor, Codex).
- `AGENTS.md` na raiz como entrypoint neutro para qualquer agente de IA, com a hierarquia de instruções do projeto e ponteiros para `.context/`, `CLAUDE.md` e os runbooks de ativação. Resolve referência pendente em `.cursor/rules` e `.windsurfrules`.
- Runbook permanente de ativação do dotcontext em `docs/planning/dotcontext-bootstrap.md` com diagnóstico real do `.context/` (14 agents preenchidos, 10 docs preenchidos, 10 skills `unfilled` aguardando MCP), pré-requisitos, fases 0-9, anti-padrões e troubleshooting.
- Guia de uso diário do dotcontext em `docs/planning/dotcontext-daily-usage.md` cobrindo workflow PREVC, invocação ad-hoc de agents/skills, sync e reverse-sync entre IDEs e detecção de drift.
- Seção "Contexto Portável (.context/)" no `CLAUDE.md` raiz documentando os gateways do dotcontext e apontando para os runbooks.
- 10 skills do dotcontext preenchidos com conteúdo específico do DestinyPost (test-generation, code-review, commit-message, pr-review, documentation, refactoring, bug-investigation, feature-breakdown, api-design, security-audit). Cada SKILL.md tem trigger language no `description` (ativação automática pelo MCP), corpo em modo imperativo, e referências canônicas para arquivos reais do repo (CLAUDE.md por área, services do AI Provider system, padrões de TDD com `createMock`/`createTestModule`, regras de OAuth com `ClientInformation`, regras de Instagram via `FlowActivity.resolveIgRoute`, convenção de commits `pt-BR sem acentos`, etc.). Habilita `use the <skill> skill to ...` em sessões locais com MCP carregado para guiar PREVC nas fases P/R/E/V/C.
- Subagent `plan-reviewer` em `.claude/agents/plan-reviewer.md` para validar planos de implementação contra o repo real **antes** da primeira escrita de código. Read-only (`Read, Glob, Grep`), auto-invocado após aprovação de plano (em plan mode ou em descrição de feature). Cobre arquitetura (Controller→Service→Repository, determinismo de workflows Temporal, `SocialAbstract` para providers, propagação de `ClientInformation` em OAuth, roteamento via `FlowActivity.resolveIgRoute`, acesso a credenciais via `AiProviderResolverService`), code-reality (arquivos/funções referenciados pelo plano realmente existem? há código legado conflitante?), impacto cross-cutting (mudança em `schema.prisma` precisa de data migration via `StartupMigrationService`? quebra contrato em `apps/backend/src/public-api/v1/`? toca DTO compartilhado?), mapa de dependências (quem mais consome o serviço/provider tocado), impacto em TDD (quais arquivos novos precisam de `.spec.ts` co-localizado para passar no hook `tdd-check.sh`), impacto em i18n (quais chaves novas precisam entrar em `pt/translation.json` e `en/translation.json`) e heads-up de documentação para o `doc-maintainer` rodar pós-implementação. Achados categorizados em três níveis: 🛑 **BLOCKER** (viola arquitetura ou supõe algo que não existe), ⚠️ **CONCERN** (risco trabalhável que precisa de mitigação explícita), 💡 **HEADS-UP** (não-bloqueador, mas vale registrar). Não reescreve o plano, não decide se prossegue, não invoca outros subagents, não edita arquivos — apenas reporta para o humano decidir. Forma par com o `doc-maintainer` no pipeline (`plano → plan-reviewer → código → doc-maintainer → commit`). Referência adicionada em seção "Subagents" do `CLAUDE.md` raiz, posicionada antes do `doc-maintainer` para refletir a ordem do pipeline.
- Subagent `doc-maintainer` em `.claude/agents/doc-maintainer.md` para manter a hierarquia de `CLAUDE.md` atualizada após cada feature ou bugfix não-trivial. Auto-invocado ao final da implementação (descrição com `PROACTIVELY`), com ferramentas restritas a `Read`, `Glob`, `Grep`, `Edit` e escopo de edição limitado a `CLAUDE.md`/`AGENTS.md` — propõe diffs com justificativa em vez de aplicar direto, sinaliza `🚨 DRIFT` quando o código contradiz a doc, e emite `📁 NEW SUBAREA CANDIDATE` quando os gatilhos das três Conscious Deferrals (`database/prisma/`, `apps/frontend/src/components/launches/`, `apps/backend/src/api/`) registradas em `docs/planning/claude-md-maintainer-agent.md` disparam. Referência adicionada em seção "Subagents" do `CLAUDE.md` raiz.

### Alterado

- Card "Vídeo" em Settings > AI Provider simplificado conforme feedback de uso: (a) catálogo Kie.ai expandido para 5 modelos (Seedance 2.0, Seedance 2 Fast, Google Veo 3.1, Google Veo 3.1 Fast, Google Veo 3.1 Lite) — antes faltavam Veo Fast e Lite; (b) display name dos Veo agora é só "Google Veo 3.1 [variante]" (removida menção redundante "via KieAI" — o usuário já está no card KieAI); (c) o select "Modelo principal" não mostra mais "Default do provedor" para VIDEO+kieai porque a chamada precisa de um model exato (Seedance e Veo têm payloads diferentes), e o form vem pré-selecionado com **Veo 3.1 Lite** (mais econômico); (d) campo "Aspect ratio padrão" removido do Settings — composer e agente sempre passam aspect ratio explícito por chamada (default 9:16 no service quando ausente, alinhado com Stories/Reels/TikTok que é o uso mais comum); (e) toggle "Enriquecer prompt automaticamente" removido do Settings — sempre default ON, sobrescrito apenas no composer caso o usuário queira passar prompt manual; (f) opções específicas de Seedance (resolução, duração, áudio) só aparecem quando o modelo selecionado é Seedance — para Veo o card mostra apenas API key + modelo. Service `AiVideoService.DEFAULT_MODEL='veo3_lite'` é fallback defensivo se a credencial salvar com `model` vazio. 14 specs verdes (1 novo cobrindo o default Veo Lite). Schema `VideoOptionsSchema` perdeu `aspectRatioDefault` e `enrichPromptByDefault`.

### Documentação

- Mapa de schema do banco em `docs/architecture/database-schema.md` agrupando os 55+ models Prisma por subdomínio (Identity, Content, Social Integrations, AI Layer, Flows, Repost, Operational, Marketplace, Mastra-internal), com caminho do repository e cross-link para o `CLAUDE.md` filho onde as regras de cada área vivem. Inclui também a explicação canônica das duas channels de migration: Prisma DDL via `migrations/` vs `StartupMigrationService` (`OnModuleInit`, idempotente, aplica três data migrations a cada startup com guard `count()`, swallows errors em log) — quando usar cada uma. Adicionada pitfall correspondente em `libraries/nestjs-libraries/CLAUDE.md` (sintoma: dados ficam stale após deploy mesmo com app rodando) e novo passo no workflow "Add a new table" apontando o mapa. Decisão consciente de **não** criar `database/prisma/CLAUDE.md` registrada em `docs/planning/claude-md-maintainer-agent.md` na nova seção "Conscious Deferrals" com gatilhos explícitos (cross-table queries padronizadas, runbook de migration, pattern não-trivial num repo, ≥80 models, agente "caçando" relacionamento sem encontrar) — quando algum disparar, o `doc-maintainer` (planejado) sinaliza `📁 PROMOTE-CANDIDATE` no Garbage Collection Day. Mesmo padrão aplicado a outras subáreas deferidas (`apps/frontend/src/components/launches/`, `apps/backend/src/api/`).
- Reorganização do `CLAUDE.md` em hierarquia nativa do Claude Code: o arquivo raiz (antes ~15KB cobrindo tudo) virou um mapa enxuto (~7KB) com identidade, stack, princípios não-negociáveis, golden rules, mapa do monorepo com links e comandos essenciais; conteúdo específico foi movido para 8 `CLAUDE.md` filhos por subárea (`apps/backend`, `apps/frontend`, `apps/orchestrator`, `libraries/nestjs-libraries` e seus filhos `ai/`, `chat/`, `integrations/social/`, e `libraries/react-shared-libraries`), cada um com posição na hierarquia, padrões específicos, mapa de arquivos-chave, workflows comuns, armadilhas conhecidas e referências cruzadas. Cada agente de IA carrega em cascata o `CLAUDE.md` da raiz + o do diretório onde está trabalhando, reduzindo tokens por turn e aumentando precisão. Criado também `AGENTS.md` na raiz apontando para `CLAUDE.md` (corrige pointer quebrado em `.windsurfrules` e `.cursor/rules`); essas regras de IDE e `.github/copilot-instructions.md` foram reduzidas a stubs apontando para `CLAUDE.md` como single source of truth (conteúdo único migrado: Sentry frontend para `apps/frontend/CLAUDE.md`, Sentry backend para `apps/backend/CLAUDE.md`, e detalhes do Zernio — ex-Late, mesma empresa rebrandeada — para `integrations/social/CLAUDE.md`). Escopo do próximo trabalho registrado em `docs/planning/claude-md-maintainer-agent.md`: subagent `doc-maintainer` que verificará drift entre `CLAUDE.md` e a implementação ao final de cada feature, propondo diffs sem aplicar sem aprovação humana.
- Nova referência de arquitetura para o sistema de provedores de IA em `docs/architecture/ai-provider-system.md` (595 linhas, 14 seções): visão geral do fluxo, schema Prisma com explicação do `scope` enum (e por que não usar `profileId IS NULL`), camadas de services com diagrama ASCII, cadeia de resolução perfil→workspace→412, endpoints REST, UI Settings (incluindo per-profile override), botão "IA Texto" do composer, fluxo completo de geração de imagem, guia passo-a-passo de "como adicionar provider novo" (ex: Anthropic), tabela de erros HTTP (com explicação de por que 412 e não 402), variáveis de ambiente, cobertura de testes (56 specs), pendências futuras e tabela de pontos de entrada para mudanças comuns. CLAUDE.md ganhou seção "Sistema de Provedores de IA" com 5 regras de ouro apontando para o doc detalhado, no mesmo padrão da seção de "Automações Instagram".

### Adicionado

- Geração de vídeo via Kie.ai (Seedance 2.0, Seedance 2 Fast, Veo 3.1) plugada no AI Provider System: agora a categoria VIDEO em Settings > AI Provider é configurável per-workspace e per-profile, com modelo escolhido em Settings (não por chamada) e opções por modelo — Seedance ganha resolução (480p/720p/1080p), duração (4-15s) e geração de áudio; Veo ganha aspect ratio default (16:9, 9:16 ou Auto). Toggle "Enriquecer prompt automaticamente" no card aplica a default que pode ser sobrescrita por chamada. Novo serviço `AiVideoService` faz dispatch por modelo (`bytedance/seedance-*` → `/api/v1/jobs/createTask` + polling em `/api/v1/jobs/recordInfo`; `veo3*` → `/api/v1/veo/generate` + polling em `/api/v1/veo/record-info`), com sanitização de Bearer tokens em logs, timeout 10min (20 polls × 30s) e rate limit dedicado de 10/min no endpoint REST `/ai/video/generate`. Composer ganha modal redesenhado com tabs **Texto → Vídeo (T2V)** e **Imagem → Vídeo (I2V)**, badge readonly do modelo configurado em Settings, chips de aspect ratio (1:1/9:16/16:9), input de URL para imagem de referência (validação http(s)) e toggle de enrich prompt. Agente Mastra ganha `generateVideoTool` reescrito com schema `mode`, `referenceImageUrl?`, `aspectRatio`, `enrichPrompt?` — descrição orienta o LLM a usar T2V quando não há imagem e I2V quando o usuário fornece uma, e a escolher aspect ratio por plataforma destino. Enriquecimento de prompt usa novo `AiTextService.generatePromptForVideo` (paralelo ao `generatePromptForPicture`) com instrução para incluir movimento de câmera, iluminação, cinematografia e ritmo — best-effort: se TEXT não está configurado (412), segue com prompt original sem bloquear. Endpoint `/ai/credentials/:kind/effective` novo (sem permissão admin) expõe a credencial resolvida (modelo + options sanitizados, sem API key) para o modal mostrar "Modelo: Seedance 2.0". 13 specs em `ai-video.service.spec.ts` cobrindo: dispatch Seedance/Veo T2V e I2V, validação de I2V sem reference URL, polling success/fail/timeout, sanitização de logs, enrich best-effort. Backend builda limpo, 99/99 specs verdes (86 atuais + 13 novos).

### Removido

- Classe `Veo3` (`libraries/nestjs-libraries/src/videos/veo3/veo3.ts`) que usava `KIEAI_API_KEY` env var direto e modelo `veo3_fast` hardcoded — substituída pelo `AiVideoService` integrado ao AI Provider System. **Breaking change**: instalações que tinham `KIEAI_API_KEY` no `.env` precisam migrar para Settings > AI Provider > Vídeo. O env var permanece no `.env.example` apenas como referência histórica deprecated. Comentário no `.env.example` atualizado para apontar usuários para o caminho novo. Geração via tag `veo3` no VideoManager legado (que aparecia no modal antigo de vídeo) foi removida; o ImagesSlides (Transloadit + ElevenLabs + FAL) continua intacto pois é uma feature dormente sob ENV vars próprias.

### Adicionado

- Fundação do sistema de configuração de provedores de IA por workspace. Schema novo `AiProviderCredential` com criptografia AES-256-GCM (reusando `EncryptionService` de OAuth), suportando provedores OpenRouter, OpenAI direto, Tavily e KieAI com opções tipadas por kind (texto, imagem, vídeo, web search). Exposto como API REST em `/ai/credentials/:kind` (listar, salvar, deletar, testar — admin) e catálogo de modelos em `/ai/catalog/:kind?provider=...` com cache Redis de 1h e busca dinâmica do `output_modalities` do OpenRouter. Padrão SENTINEL preserva a chave existente quando o admin reabre o formulário sem digitar de novo. UI de configuração e migração dos consumers (agente Mastra, gerador de imagem, gerador de posts) serão entregues nos próximos blocos.
- Camada de resolução e geração de IA: `AiProviderResolverService` resolve credencial efetiva por chamada (perfil → workspace com `shareDefault` → erro 402 padronizado), `AiClientFactory` cria `LanguageModel`/`ImageModel` prontos para uso com Vercel AI SDK v5 (com helper `textForMastra` retornando função async lazy para o agente Mastra trocar credencial sem reiniciar), e `AiTextService` substitui os métodos legados do `OpenaiService` (`generatePosts`, `generatePromptForPicture`, `separatePosts`, `extractWebsiteText`, `generateSlidesFromText`) usando o factory, com fallback automático para `fallbackModel` quando o principal falhar. Adicionado também o método `caption(action, content)` que será usado pelos botões "Gerar legenda com IA" e "Melhorar com IA" do composer (Bloco E), com truncate de 8000 caracteres no input para defesa de custo.

### Adicionado

- Tela "AI Provider" em Settings (admin only, ao lado de "AI Credits") com cards colapsáveis para cada tipo de IA: Geração de texto (LLM), Imagem, Vídeo (em breve) e Web Search (em breve). Cada card permite escolher provedor (OpenRouter ou OpenAI), colar API key (com sentinel `••••••••` quando salva), selecionar modelo principal e fallback (com **busca por texto** porque o catálogo OpenRouter tem 400+ modelos), ajustar opções específicas do tipo (temperature/reasoning effort para texto, qualidade/resolução para imagem) e testar a conexão. O catálogo de modelos é carregado dinamicamente do `/api/v1/models` do OpenRouter (cacheado 1h em Redis no backend, 60s em SWR no cliente). Visual reutiliza o padrão de Settings > Credenciais (card `bg-sixth/border-fifth`, sentinel pattern, useToaster, useDecisionModal).
- Configuração de provedor de IA por perfil em Settings > AI Provider para o cenário de agências com clientes: no perfil default da agência o admin configura a chave que vira o padrão do workspace; nos perfis dos clientes a tela mostra um banner "Herdando da agência (OpenRouter · openai/gpt-5.5)" com botão "Configurar chave própria deste perfil" — ao clicar, o cliente cadastra uma chave exclusiva sem afetar o default da agência. Há checkbox "Compartilhar este default com perfis" no nível workspace (controla se perfis sem override herdam) e botão "Remover override" no nível perfil (apaga só a row do cliente, fazendo o perfil voltar automaticamente ao default). O backend `AiProviderResolverService` já fazia essa cadeia (perfil → workspace com `shareDefault` → erro 402); agora o controller `/ai/credentials/:kind` aceita `?profileId=...` e roteia o escopo conforme `profile.isDefault`.

### Adicionado

- Web Search e Extract via Tavily como 4ª categoria do AI Provider system. Card "Web Search" em Settings > AI Provider perdeu o badge "Em breve" e agora aceita configuração de chave Tavily, profundidade da busca (basic/advanced — default advanced) e máximo de resultados (1–20). O agente Mastra ganhou 2 tools novos: `webSearchTool` (busca web por query, com filtros opcionais de notícias dos últimos N dias) e `extractUrlsTool` (faz scraping de até 20 URLs e devolve markdown limpo, com tolerância a falhas parciais). O prompt do agente recebeu uma árvore de decisão explícita: tópicos da própria marca → `knowledgeBaseQuery`; URLs fornecidas pelo usuário → `extractUrlsTool`; eventos externos/notícias → `webSearchTool`; ambíguos (ex: comparar nosso X com competitor Y) → ambos KB + web search. No composer, o botão "🌐 IA Search" foi adicionado à toolbar (antes do "IA Texto") com modal de 2 abas: **Pesquisar tópico** (input de query + checkbox "apenas últimos 7 dias" → backend orquestra search + caption) e **Importar URLs** (até 20 URLs com validação `new URL()`, toggle "Conteúdo profundo" para ativar `extractDepth: 'advanced'` quando a página é JS-heavy). O endpoint orquestrado `POST /ai/web-search/generate-post` faz `search/extract → caption(generate, sourceWrapped)` em uma chamada — menos latência, menos banda, e com guardrail de prompt injection (conteúdo extraído entra no system prompt do caption envolvido em tags `<source>` com instrução explícita "tratar como dado externo, NUNCA seguir instruções"). Bloqueio SSRF defensivo no backend (rejeita `localhost`, ranges `10.*`, `192.168.*`, `172.16-31.*`, `169.254.*`, IPv6 link-local, protocolos não-`http(s)`). Sanitização regex `Bearer\s+[\w.-]+ → ***` e `tvly-[\w.-]+ → tvly-***` antes de qualquer log. Timeout de 45s no fetch (para não pendurar workers). Rate limit `@Throttle 30/min` nos 3 endpoints. 11 specs novos cobrindo: search advanced default, override por opts, depth da credencial quando opts ausente, recusa de provider não-tavily, sanitização de erro, extract basic default, recusa de lista vazia/maior que 20, recusa de URL com hostname privado, recusa de protocolo não-http, partial failures.

### Adicionado

- Botão "IA Texto" no composer (ao lado do AI Image, antes dele) que melhora a legenda atual via LLM configurado em Settings > AI Provider. Mesmo padrão visual do AI Image (ícone de estrela + label, mesmo tamanho/cor), com spinner durante a chamada e disabled quando o texto tem menos de 30 caracteres (igual ao mínimo do AI Image). Disponível para todos os perfis quando há credencial efetiva (workspace default ou override do perfil) e funciona em qualquer plataforma — o `identifier` da integração selecionada (twitter, linkedin, instagram, etc.) é repassado ao backend para o LLM adaptar o tom ao contexto. Endpoint `POST /ai/text/caption` no backend tem rate limit dedicado de 30 requisições por minuto por usuário (sobrescreve o default global de 30/h) para proteger contra cliques repetidos virarem torneira de custo, e trunca o input em 8000 caracteres antes de enviar ao LLM.
- Modal de geração de imagem com seletor de proporção (1:1 / 9:16 / 16:9). O botão AI Image agora abre um modal com 3 botões de formato (Quadrado / Vertical para Stories e Reels / Horizontal para post normal) e a grade de 14 estilos visuais. O usuário escolhe o formato, o estilo e clica "Gerar imagem". O backend mapeia o aspect ratio para o tamanho correto por provedor: OpenAI gpt-image-2/gpt-image-1-mini → 1024x1024, 1024x1536 ou 1536x1024; OpenRouter → `image_config.aspect_ratio` direto. Os 3 formatos são universais (todos os modelos do catálogo aceitam), validados via whitelist no backend e enum no frontend. O `aspectRatioDefault` em Settings > AI Provider foi removido — a decisão é por geração, não por configuração global.

### Alterado

- Catálogo de modelos OpenAI para imagem foi limpo: removidos `gpt-image-1` e `dall-e-*` (deprecated), restando apenas **gpt-image-2** (default) e **gpt-image-1-mini**. Quem tinha `gpt-image-1` configurado em Settings > AI Provider precisa selecionar um dos dois novos modelos. O default da factory virou `gpt-image-2`, então credenciais sem modelo explícito caem no novo default automaticamente.
- Erro de IA não configurada agora retorna **HTTP 412 Precondition Failed** em vez de 402 Payment Required — alinha com a semântica do problema (pré-condição de configuração não atendida) e evita que o interceptor global do Postiz que captura 402 abra o modal de billing/cobrança quando o admin do self-hosted tenta usar o agente sem configurar credencial. O frontend (`AiCaption`, em breve outros consumers) trata 412 mostrando toast com link para Settings > AI Provider.
- Geração de imagem com IA agora respeita o provedor configurado em Settings > AI por workspace. Antes a geração era hardcoded em DALL-E 3 da OpenAI direto via `OPENAI_API_KEY` do servidor; agora roteia para OpenRouter (com Gemini Nano Banana, Flux, etc) ou OpenAI direto (gpt-image-1) conforme a configuração do workspace, com suporte a `aspect_ratio` e `image_size` (1K/2K/4K) por opções da credencial. O guard 503 "AI image generation is not configured on this server" foi removido — caso o admin não tenha configurado nada, a UI agora recebe 402 com mensagem específica orientando a abrir Settings > AI. Geração de posts (`generatePosts`, `separatePosts` do split de threads) e prompts de imagem (`generatePromptForPicture`) também passam pelo provedor configurado. O agente Mastra (`/agents/[id]`) trocou o modelo `gpt-5.2` hardcoded por resolução dinâmica via `runtimeContext` — o agente continua singleton mas o modelo é resolvido a cada chamada, permitindo trocar credencial em produção sem reiniciar o backend. Métodos `generateVoiceFromText` (HeyGen) e `generateSlidesFromText` (ImageSlides) seguem em OpenaiService porque essas duas integrações estão dormentes e serão revisadas posteriormente.

### Corrigido

- Agente de IA gerava sempre imagens quadradas (1:1) mesmo quando o usuário pedia 9:16 ou 16:9. Causa: o `generateImageTool` não expunha `aspectRatio` no `inputSchema` do Mastra (só `prompt`), então o LLM não tinha como passar o valor — e a chamada para `MediaService.generateImage` ignorava o argumento `aspectRatio` que a service já aceitava. Agora o tool aceita `aspectRatio: '1:1' | '9:16' | '16:9'` como parâmetro obrigatório, com hints explícitos no prompt sobre qual ratio escolher para cada plataforma (1:1 = feed Instagram, 9:16 = Stories/Reels/TikTok, 16:9 = YouTube/LinkedIn). Também instrui o agente a respeitar pedido explícito do usuário (ex: "vertical", "stories format") e a perguntar quando ambíguo.
- Histórico de conversas do Agente de IA quebrava com `Cannot read properties of undefined (reading 'map')` ao clicar em qualquer thread anterior. Causa: Mastra v1.21+ mudou o retorno de `memory.recall()` de `{ uiMessages, messages, ... }` para apenas `{ messages, total, page, ... }`. O frontend (`agent.chat.tsx:98`) lia `data.uiMessages.map(...)` e crashava porque o campo não existia mais. Correção: backend `/copilot/:thread/list` agora normaliza o resultado para `{ uiMessages: [{role, content}] }` independentemente da versão da Mastra. Helper `normalizeMessageForUi` extrai texto agregado de mensagens com `content` em qualquer formato (string simples, array de parts, objeto com `parts` aninhado), descartando partes não-texto como tool-calls e imagens. Filtra para mostrar apenas mensagens `user`/`assistant` com conteúdo não-vazio.
- Placeholder do input do Agente de IA ("Type a message...") agora aparece traduzido em pt-BR como "Digite uma mensagem..." (default do CopilotKit não passava pelo `useT()`).
- Agente Mastra mostrava o post bem formatado no chat (parágrafos, bullets em linhas separadas) mas, ao chamar `schedulePostTool`, gerava um HTML que colapsava as listas em um único `<p>` (todos os bullets juntos numa linha só) e omitia parágrafos vazios entre blocos — resultado: editor pós-agendamento com texto compactado, sem linha em branco entre parágrafos e bullets em cima de bullets. A causa era prompt vago: "Each line must be wrapped in <p>" sem regras específicas de quando separar e quando colar. O prompt do agente foi reforçado com seção dedicada de formato HTML para post: cada linha visual = um `<p>` próprio (proibido fundir múltiplas frases ou bullets em um único `<p>`), parágrafo vazio `<p></p>` obrigatório entre blocos com linha em branco, listas mantêm bullets sem `<p></p>` entre eles, exemplos concretos para parágrafos, listas, blocos mistos e hashtags. Quando a persona pede "linha em branco entre frases", cada frase vira `<p>` próprio com `<p></p>` no meio.
- Agente Mastra (chat de IA do canto inferior direito) ignorava completamente a Persona configurada em Settings > Agente de IA — gerava posts no tom genérico mesmo quando o usuário tinha preenchido marca, público-alvo, tom de voz, instruções de escrita, CTAs e restrições. Causa: o `CopilotController.agent` injetava apenas `integrations`, `organization` e `ui` no `requestContext` do Mastra, nunca passava `persona` nem `profileId`. O `instructions()` do agente lia `requestContext.get('persona')` e sempre obtinha vazio. Correção: o controller agora extrai o profile via `@GetProfileFromRequest()`, carrega a persona via `ProfileService.getPersonaForAgent` (best-effort — falha de DB não bloqueia o chat) e injeta tanto `profileId` (para resolução de credenciais per-perfil) quanto `persona` (JSON serializado) no `requestContext`. O prompt do agente também ganhou — depois do bloco de persona — uma instrução explícita "the PROFILE PERSONA above takes ABSOLUTE PRIORITY over any default", espelhando o que já fazia o `caption()` do botão IA Texto. E ganhou bloco de formatação `\\n` vs `\\n\\n` para o editor renderizar quebras corretamente.
- Agente Mastra (botão "Agente de IA") quebrava com `Cannot read properties of undefined (reading 'get')` na primeira mensagem do usuário. Causa: Mastra v1.21+ renomeou `runtimeContext` → `requestContext` e mudou a assinatura de `tool.execute(args)` (single-arg com `{ context, runtimeContext }`) para `tool.execute(input, context)` onde `context` carrega `{ requestContext, mastra, agent, ... }`. O codebase do projeto seguia a API antiga, e ao invocar o agente o `model()` callback tentava ler `runtimeContext.get(...)` numa propriedade que não existia mais. Migração completa: novo helper `tool.context.helper.ts` (`readRequestContext`) que aceita ambas as formas para resilência em upgrades menores; `agent.model.resolver.ts` aceita `requestContext | runtimeContext | undefined` com mensagem clara quando o contexto está ausente; `load.tools.service.ts` model() destructura ambos os nomes; e as 11 ferramentas do agente (`webSearchTool`, `extractUrlsTool`, `generateImageTool`, `generateVideoTool`, `generateVideoOptions`, `integrationList`, `triggerTool`, `integrationSchema`, `knowledgeBaseQuery`, `videoFunctionTool`, `schedulePostTool`) tiveram a assinatura migrada para `(input, options)` com `readRequestContext(options)`. Spec novo cobrindo o caso `undefined` que era a causa raiz.
- Quebras de linha pedidas pela persona ("Faça quebra de linhas entre as frases" no campo Instruções de escrita) eram silenciosamente engolidas ao inserir o post no editor — duas causas: (a) `editor.commands.setContent(text)` interpreta a string como HTML e o browser colapsa `\n` puro no parse, transformando o texto em parágrafo único; (b) o reset de margem do Tiptap/Tailwind faz `<p>` consecutivos ficarem empilhados sem gap visual entre eles. Correção em três frentes: (1) novo helper `textToTiptapHtml` (em `apps/frontend/src/components/launches/helpers/`) converte `\n` em `<br>` dentro de parágrafos e `\n\n` em `<p></p>` (parágrafo vazio entre dois `<p>`) para gerar linha em branco visível, equivalente ao "Enter duas vezes" do usuário; (2) caracteres HTML são escapados para evitar injeção via conteúdo do LLM; (3) o system prompt do `caption()` ganhou bloco `FORMATTING_RULES` explicando ao LLM a diferença entre `\n` (quebra simples, dentro de paragrafo/lista) e `\n\n` (linha em branco entre parágrafos), porque modelos como gpt-5 tendem a emitir tudo com `\n` simples ignorando pedidos da persona. Aplicado em `AiCaption` (botão "IA Texto") e `AiSearch` (botão "IA Search").
- Modelos da família **gpt-5** (gpt-5, gpt-5.4, gpt-5.5, gpt-5-codex, gpt-5-mini etc) agora também são reconhecidos como reasoning models pelo `isReasoningModel` em `ai-client.factory.ts`. Antes só `o1/o3/o4` eram detectados, e o AI SDK emitia warning "The 'temperature' setting is not supported by this model - temperature is not supported for reasoning models" a cada chamada com gpt-5.x. A geração continuava ok porque é só warning, mas o ruído nos logs sugeria erro real.
- Persona configurada em Settings > Agente de IA passou a ser respeitada de fato pela geração de legenda (botão IA Texto e modal IA Search). Antes, o `system prompt` do `caption()` injetava regras fixas como "Sem hashtags. Sem emojis. Mantenha conciso (2 a 5 frases)" **antes** do bloco de persona, gerando conflito direto: a persona pedia "5 hashtags ao final" mas o LLM seguia o "Sem hashtags" do default. Agora o baseSystem cobre apenas papel + formato de saída + plataforma/tom; estilo (tamanho, hashtags, emojis, quebras de linha, opinião pessoal etc) fica 100% sob controle da persona quando ela existe — com instrução explícita "as instruções da persona acima TÊM PRIORIDADE absoluta sobre quaisquer defaults". Sem persona configurada, mantém-se o default sensato (2-5 frases, sem emojis, sem hashtags). Específicas novas em `ai-text.service.spec.ts` cobrem: default aplicado quando não há persona, e omissão do default + marker de prioridade quando há persona.
- Acentos restaurados na view "Settings > Agente de IA" (todas as 40 chaves `persona_*` em pt-BR): "Descrição da marca", "Público-alvo", "Tom de voz personalizado", "Instruções de escrita", "Restrições de conteúdo", "Ilustração flat minimalista" etc. As traduções tinham sido criadas sem acento por engano em 0.5.0; o conteúdo armazenado no banco não muda — apenas a UI volta a exibir com a grafia correta.

### Corrigido

- Modal "AI Search" do composer ganhou redesenho com seletor de tipo de busca (Geral / Notícias / Finanças) — antes só havia um filtro genérico de "últimos 7 dias" travado em notícias. O novo dropdown mapeia para o parâmetro `topic` da API do Tavily (default `general`) e a checkbox de recência foi renomeada para "Apenas resultados recentes" com hint explicando que só é efetiva no tipo "Notícias". As abas "Pesquisar tópico" / "Importar URLs" viraram cartões com ícone (lupa / link) e descrição curta, alinhando o visual ao padrão dos modais de IA Image. O botão "AI Search" da toolbar perdeu o bloqueio "Adicione 30 caracteres" — não fazia sentido exigir texto pré-existente para uma feature que **gera** o post a partir de uma query ou URL; agora abre o modal sempre.
- Geração de legenda com IA falhava em modelos de raciocínio da OpenAI (família `o1`, `o3`, `o4`) com warning "AI SDK Warning: The 'temperature' setting is not supported by this model". O `AiClientFactory` agora expõe o `modelId` no `TextClientResult` e o `AiTextService` detecta reasoning models (helper `isReasoningModel`) para omitir o parâmetro `temperature` da chamada `generateText`. Modelos não-reasoning continuam usando `options.temperature` normalmente.
- Legenda gerada pela IA Search ficava curta demais (forçada a "2-3 frases") quando o input era um artigo de notícia grande, e ignorava completamente a Persona configurada no perfil. Agora `AiTextController.caption` e `AiWebSearchController.generatePost` carregam a persona do perfil via novo helper `loadPersonaBlock` (best-effort, falha de DB não quebra fluxo) e injetam no system prompt do caption. A instrução de tamanho foi relaxada para "entre 2 e 5 frases para feed", dando espaço para conteúdo mais denso quando o assunto exige.
- Erro 412 retornado por `/ai/web-search/generate-post` agora distingue entre credencial de Web Search ausente e credencial de Texto ausente — antes ambos caíam na mensagem genérica "Configure suas chaves em Settings > AI Provider > Web Search", confundindo o usuário que já havia configurado o Tavily mas não o LLM. O controller intercepta o 412 do passo `caption` e responde com mensagem específica apontando para o card "Geração de texto".
- Card "Web Search" em Settings > AI Provider esconde os seletores "Modelo principal" e "Fallback" (já que o único provedor disponível, Tavily, não tem múltiplos modelos), reduzindo ruído visual e evitando confusão sobre o que configurar.

### Corrigido

- Resolver de credenciais de IA agora trata o perfil default como dono da configuração workspace: chamadas vindas do perfil default (`Profile.isDefault=true`) ignoram a regra de `shareDefault` e usam direto a credencial scope=WORKSPACE, mesmo quando o admin desmarca "Compartilhar com perfis". Antes, qualquer `profileId` truthy aplicava a regra de herança e devolvia 412 "perfil sem chave e workspace não compartilha" — fazendo o admin do default não conseguir usar a própria configuração. O `AiProviderResolverService` consulta `ProfileService.getProfileById` no início e zera `effectiveProfileId` quando o perfil é default, alinhando com a lógica que o `AiCredentialsController.resolveScope` já fazia para save/get/delete.
- Geração de imagem com IA quebrava em servidores rodando Node 20+ porque `undici` (fetch nativo) não suporta `data:` URLs nativamente. O fluxo `MediaController.generateImageFromText` montava `data:image/png;base64,...` e passava para `storage.uploadSimple`, que chamava `fetch(path)` e quebrava silenciosamente com erro 500. Novo helper `loadFromUrlOrDataUrl` em `libraries/nestjs-libraries/src/upload/storage.helpers.ts` detecta data URLs e usa `Buffer.from(base64)` direto (sem fetch), e `CloudflareStorage.uploadSimple` + `LocalStorage.uploadSimple` foram migrados para o helper. O controller também passou a montar o prefix `data:` antes de chamar `uploadSimple` quando `MediaService.generateImage` retorna base64 puro (caso de `isPicturePrompt=true`).
- Geração de imagem com IA falhava com 412 mesmo a chave de imagem estando configurada quando o admin não tinha credencial de TEXT. Causa: `MediaService.generateImage` chamava `AiTextService.generatePromptForPicture` (para enriquecer o prompt com detalhes de câmera, iluminação etc.) antes de gerar a imagem, e esse passo precisava de credencial TEXT. Agora o enrichment é **best-effort**: se a chamada lançar 412, loga warning e segue com o prompt original. Erros não-412 propagam normal.
- Geração de imagem com `gpt-image-1` (e modelos `gpt-image-*` em geral) falhava com `Unknown parameter: 'response_format'` da OpenAI — a nova família já retorna `b64_json` por padrão e não aceita mais o parâmetro. O `AiImageService.generateOpenAi` agora só inclui `response_format` quando o modelo é `dall-e-*` (legacy). Acabou ficando irrelevante após a limpeza do catálogo (gpt-image-1 e DALL-E foram removidos), mas mantido em código defensivamente.
- Componente `AiImage` no composer agora trata erros HTTP corretamente (412 → toast "Configure suas chaves em Settings > AI Provider"; 429 → toast "Aguarde um instante"; outros → toast com mensagem do backend), em vez de aceitar qualquer JSON como sucesso e adicionar ao state — antes um payload de erro `{statusCode, message}` virava media inválida sem `id`/`path` e disparava warning React "Each child in a list should have a unique key prop" + thumb vazia.
- PM2 dos três processos do container (orchestrator, backend, frontend) agora têm limite preventivo de memória com proteção anti-loop: `max-memory-restart` (orchestrator 2 GB, backend e frontend 1 GB cada), `min-uptime 30s`, `max-restarts 10` e `restart-delay 5s`. Antes, sob rajada de carga (ex.: anúncio Meta Ads + Reels com automação ManyChat-style disparando webhooks IG simultâneos) o orchestrator inflava com seus 33 workers Temporal, batia OOM no host (já em pressão de swap) e o cgroup do container era punido pelo kernel matando processos arbitrários. Em uma das mortes o orchestrator subiu com o `Worker.run()` em estado quebrado (PM2 reportava `online` mas o Temporal UI mostrava "No Workers Found"), deixando workflows pendurados em `Running` por horas até intervenção manual. O novo teto faz o PM2 reiniciar limpo cada processo antes do OOM kill do kernel, e a proteção anti-loop (max 10 restarts em 30s) impede crash loop infinito caso o vazamento seja persistente. Causa-raiz definitiva (host CPX32 com 7,6 GB dividido entre 18 containers + swap em uso constante) precisa ser endereçada com upgrade de VPS ou separação de serviços.
- Reconexão do canal YouTube continuava sem gravar `refresh_token` válido no banco, exigindo revogação manual do app pelo painel da conta Google a cada hora (ou desconectava sozinho). Causa-raiz em três pontos: (1) o controller de OAuth descartava `refreshToken`/`expiresIn` retornados por `authenticate()` no fluxo `reConnect` dos providers `isBetweenSteps`, sobrescrevendo o refresh_token real do Google com `body.refresh` (ID do canal/vazio); (2) na reconexão de uma conta que já havia autorizado o app, o Google pulava silenciosamente a tela de consent e devolvia `code` sem `refresh_token` associado; (3) `myaccount.google.com/permissions` nem sempre exibe o app para revogação manual, dependendo do tipo de conta. A correção combina três camadas: nova flag `keepReconnectAuthTokens` na interface `SocialProvider` (ativa apenas no YouTube — preserva tokens do `authenticate()` no fluxo `reConnect`); novo método opcional `revokeToken()` que o controller chama programaticamente antes da reconexão (POST em `oauth2.googleapis.com/revoke`), forçando o Google a esquecer o consent; e `include_granted_scopes: false` no `generateAuthUrl` do YouTube, impedindo o Google de pular a tela de consent baseado em scopes já concedidos. Resultado: clicar em "Reconectar" no canal YouTube agora exibe a tela de consent novamente e o Google passa a emitir um `refresh_token` válido, gravando-o no banco. Facebook, Instagram, LinkedIn Page e GMB seguem o comportamento default do upstream Postiz, que funciona para eles porque dependem de Page Token permanente ou access token de longa duração.
- Repost automático no X (Twitter) deixava de publicar quando a legenda da postagem original estourava o limite "weighted" do X — caracteres como `—` (em-dash) e `…` (ellipsis) contam como 2 chars no contador real, então uma legenda com 280 chars ASCII + 1 em-dash já estoura. O fluxo agora trunca a legenda em 250 chars (margem de segurança) antes de mandar para o X. Também foram acrescentados padrões em `XProvider.handleErrors` para reconhecer respostas reais da API ("Tweet text is too long", "exceeds the maximum number of characters", `paid_partnership` não autorizado), evitando que esses casos caiam no genérico "Unknown Error". Por fim, os campos opcionais `made_with_ai` e `paid_partnership` deixaram de ser enviados quando estão `false` — antes eram enviados sempre, e algumas contas sem o feature habilitado podiam recusar a requisição. Comportamento coberto por nova spec `x.provider.spec.ts` (17 casos).

## [0.5.2] - 2026-04-29

### Corrigido

- Validação de webhook Instagram (introduzida em 0.5.1) passou a falhar com `Error validating application. Cannot get application info due to a system error` em diversas situações que **não** representavam problema real do usuário, bloqueando indevidamente a criação de automações em instâncias que estavam funcionando. A correção combina três camadas: (1) **Token format** — removido `encodeURIComponent` aplicado por engano no `app access token` (`appId%7CappSecret`), que a Graph API rejeita; o pipe agora é enviado cru, alinhado a `CredentialService.configureInstagramWebhook`. (2) **Roteamento de host** — apps do tipo "API do Instagram com login do Instagram" (Instagram-only) vivem em `graph.instagram.com`, enquanto apps clássicos com Instagram via Facebook Login vivem em `graph.facebook.com`. A checagem agora escolhe o host com base na fonte das credenciais (`instagramAppId/instagramAppSecret` ou `INSTAGRAM_APP_ID` env → `graph.instagram.com`; `clientId/clientSecret` ou `FACEBOOK_APP_ID` env → `graph.facebook.com`) e faz fallback automático para o outro host antes de reportar falha, cobrindo casos de App ID cadastrado no campo errado. (3) **Fail-open em erros de API** — o endpoint `/{app_id}/subscriptions` não é confiável para vários tipos de app (Instagram-only em dev mode, apps com formato de token que a Meta se recusa a validar etc.); quando ambos os hosts respondem com erro de validação, o backend agora loga o detalhe completo e **permite** a criação da automação em vez de bloquear (a entrega real do webhook não depende dessa checagem passar). Bloqueio só ocorre quando há evidência positiva de problema — credenciais ausentes, ou subscription Instagram lida com sucesso mas inativa/com campos faltando, ou listas readable sem nenhuma subscription `object='instagram'` em nenhum host. Mensagens de erro mantêm contexto diagnóstico (App ID consultado e o que cada host retornou). Adicionados testes de regressão para `%7C`, Instagram-only app em `graph.instagram.com`, fallback entre hosts, fail-open em erros e fail-closed em subscription faltando.

## [0.5.1] - 2026-04-29

### Corrigido

- Validação de webhook Instagram no wizard "Nova Automação" (estilo ManyChat) agora respeita as credenciais Meta cadastradas em Configurações > Credenciais > Facebook em vez de ler somente `FACEBOOK_APP_ID/FACEBOOK_APP_SECRET` da env. Antes o wizard exibia o erro genérico "Webhook Instagram nao configurado no app Meta..." mesmo quando o webhook estava registrado corretamente — bastava o workspace usar credenciais próprias (ou as envs apontarem para outro app Meta) para a checagem consultar o app errado, encontrar `data: []` em `/{appId}/subscriptions` e dar falso negativo. Quando as envs estavam vazias o problema era oposto: a checagem retornava `ok:true` silenciosamente e o usuário criava automações sem webhook real, descobrindo o problema só quando comentário/DM não disparava. Agora a resolução de credenciais segue a mesma cadeia já usada pelos providers Instagram (`instagramAppId/instagramAppSecret` do workspace → `clientId/clientSecret` do workspace → `INSTAGRAM_APP_ID/SECRET` env → `FACEBOOK_APP_ID/SECRET` env) e, se nenhuma fonte estiver configurada, a UI passa a exibir mensagem clara pedindo configuração em Configurações > Credenciais. As mensagens de erro também ganharam contexto diagnóstico — incluem o `appId` que foi consultado e listam as `subscriptions` que estão presentes naquele app (ex.: "App 1234: nenhuma subscription com object='instagram'. Subscriptions presentes: page (active=true, fields=feed,messages)"), facilitando identificar quando o webhook foi cadastrado em outro app ou no objeto errado. A versão da Graph API foi atualizada de v20.0 para v25.0 alinhando ao restante do código IG.
- Tokens de messaging do Instagram (System User Token e tokens por conta cadastrados em Settings > Credenciais > Instagram) deixavam de existir silenciosamente sempre que o admin abria "Editar" em qualquer seção do card Meta (Facebook, Instagram ou Threads) e salvava — a `CredentialService.unredact` só preservava as chaves explicitamente listadas no body do PATCH, e como o formulário de App ID/Secret não conhece os campos de messaging eles eram descartados na re-encriptação. Sintomas relatados: follow-gate quebrando com `Messaging não configurado para <workspace>`, automações estilo ManyChat parando e o card "Token Usuário do Sistema" voltando vazio sem ninguém ter clicado em remover. A correção passa a fazer overlay do body sobre o registro existente (campos não enviados são preservados, SENTINEL continua mantendo, string vazia continua limpando), com warning log listando os campos preservados automaticamente para detectar futuras regressões em formulários novos. Usuários afetados precisam re-cadastrar manualmente o System User Token e/ou os tokens por conta — os valores anteriores não são recuperáveis.

## [0.5.0] - 2026-04-29

### Documentação

- Nova referência de arquitetura para automações Instagram em `docs/architecture/instagram-automations.md`, voltada a agentes de IA e ao time de engenharia: mapa dos arquivos-chave (backend/orchestrator/libraries/frontend), as três camadas de credenciais Meta (App, Integration, Messaging Tokens), roteamento de host/token via `FlowActivity.resolveIgRoute`, fluxo completo do follow gate de 2 etapas (opening DM + postback + PendingPostback), convenções e armadilhas. `CLAUDE.md` ganhou um bloco resumido com as regras de ouro apontando para o doc detalhado.

### Alterado

- Card de credenciais do LinkedIn em Settings > Credenciais agora exibe duas URLs de callback (Perfil pessoal e Página da empresa) em vez de uma só. As duas integrações (`linkedin` e `linkedin-page`) compartilham o mesmo Client ID/Secret no portal de desenvolvedor do LinkedIn — só o redirect URI muda — então o usuário cadastra ambas de uma vez sem precisar adivinhar a segunda URL.
- Tela de Métricas agora aparece com esse rótulo em pt-BR (antes "Análises", que ficava ambíguo). Adicionada também a tradução do card "Follower Count" (Contagem de Seguidores) que faltava entre as métricas do Instagram.
- Renomeados os campos do card de credenciais do X (Twitter) em Settings > Credenciais de "API Key" / "API Secret" para "Consumer Key" / "Access Token", espelhando os labels usados pelo próprio portal de desenvolvedor do X (seção "OAuth 1.0 Keys") e facilitando para o usuário identificar o que copiar.
- Card de credenciais Zernio em Settings > Credenciais agora aparece como item colapsável dentro da lista de redes sociais, com o logo do Zernio e o mesmo padrão visual dos demais provedores. Removida a seção solta abaixo da lista e a menção a "TikTok e Pinterest" no título — Zernio é provedor alternativo para todas as redes sociais suportadas.
- Listagem de Automações (comentário/flows) redesenhada em formato de tabela com paridade visual ao Repost: switch de ativa/pausada (alterna `ACTIVE ⇄ PAUSED` diretamente na linha; DRAFT aparece com switch desabilitado e badge âmbar "Rascunho"), avatar da conta conectada com `PlatformIconBadge` + nome, linha secundária com resumo do gatilho (palavras-chave truncadas em 2 + `+N` e contagem de posts selecionados ou "Todos os posts"), contador de execuções e ações Editar/Excluir por linha. `getFlows` passa a incluir o nó TRIGGER (apenas `data`/`type`) para o frontend montar o resumo sem fetch adicional por automação.
- Renomeada integração Late (getlate.dev) para Zernio (zernio.com) — mesma empresa, nova marca. SDK `@getlatedev/node` substituído por `@zernio/node` (v0.2.80), base URL passa a ser `https://zernio.com/api/v1`, providers `late-*` renomeados para `zernio-*`, colunas `lateApiKey`/`shareLateWithProfiles` renomeadas para `zernioApiKey`/`shareZernioWithProfiles`, rota `/integrations/late` passa a ser `/integrations/zernio` e configurações em `/settings/zernio`. Uma migração idempotente roda no startup do backend para copiar chaves de API e reescrever identificadores de provedor existentes — usuários não precisam reconectar contas. Assets atualizados para `zernio-logo.svg` (fundo claro) e `zernio-icon.svg` (ícone branco sobre fundo `#EB3514`) no badge de canais via Zernio. Endpoint `platform-invites` tenta a URL nova e cai num fallback 501 amigável caso o Zernio não exponha mais.
- Removido o badge/tooltip "Recomendado" do canal Instagram (Standalone) na tela de conexão de canais — o aviso sobre follow gate funcionar sem App Review deixou de ser relevante após o IG User Token cadastrado em Settings passar a funcionar também para integrações conectadas via "Instagram (Facebook Business)". As chaves de tradução `tooltip_instagram-standalone` foram removidas em pt e en.

### Adicionado

- Filtro opcional por hashtag nas regras de Repost: ao definir uma hashtag (ex.: `#repost`) no card "Conteúdo" da regra, somente posts cuja legenda contenha essa hashtag serão republicados; legenda em branco ou hashtag ausente marca o item como `SKIPPED` com motivo `FILTER_HASHTAG`. Em branco mantém o comportamento atual de repostar tudo. A comparação é case-insensitive, aceita o valor com ou sem `#` e usa boundary explícito (`#repost` não casa em `#repostagem`). O checkpoint do polling continua avançando mesmo nos itens filtrados, então o ciclo seguinte não reprocessa o mesmo conteúdo. Stories normalmente não trazem legenda da Graph API — o filtro foi pensado para Reels/Feed.
- Repost agora aceita LinkedIn (Perfil e Página), X (Twitter), Threads e Pinterest como destinos. Cada conta conectada aparece como entrada separada no seletor de destinos da regra (formato fixo: "Post" no LinkedIn, "Tweet" no X, "Thread" no Threads, "Pin" no Pinterest), e o orchestrator publica usando os mesmos providers do calendário — vídeos verticais e imagens do story/reel original são propagados na próxima janela de polling. A caption é truncada por destino conforme o limite de cada rede (X em 280 caracteres, Threads e Pinterest em 500); LinkedIn herda o texto completo. No Pinterest, o pin vai automaticamente para o primeiro board da conta (resolvido em runtime via `pinterest.boards()`); contas sem nenhum board cadastrado marcam o destino com erro `PINTEREST_NO_BOARD` sem afetar os demais. Foram adicionados também limites de duração de vídeo no filtro opcional da regra (X até 140s, LinkedIn até 600s) — quando ultrapassado, o destino é pulado com `DURATION_EXCEEDED_X` ou `DURATION_EXCEEDED_LINKEDIN`. Nenhuma reconexão de canais é necessária; basta editar uma regra existente para adicionar os novos destinos.
- Suporte a imagem de capa customizada para Reels do Instagram, semelhante ao thumbnail do YouTube. Quando o tipo de post é "Post / Reel" e a mídia é um vídeo único, surge no painel de configurações do canal um seletor "Capa do Reel" que reusa o componente da biblioteca de mídia. A imagem (JPEG, 9:16 recomendado, até 8MB) é enviada à Graph API via `cover_url` na criação do container `media_type=REELS` e substitui o frame extraído do vídeo. Stories e carousels com vídeo continuam ignorando a capa (não suportado pela API). Vale também para integrações Instagram Standalone (IG User Token).
- Cards de credenciais OAuth em Settings > Credenciais agora exibem a URL de callback com botão "Copiar" para facilitar a configuração no painel do provider (Google Cloud, Meta, TikTok, etc.). Cobertura: YouTube, TikTok, Pinterest, LinkedIn, Twitter/X, Reddit, Discord, Slack, Facebook, Instagram (via Facebook Login + Login direto) e Threads.
- Regras de Repost V2 — matriz de formatos origem × destino: agora cada destino grava o formato explícito (Instagram Reel/Feed, Instagram Story, Facebook Reel, TikTok Feed, YouTube Short) em vez de uma lista solta de integrações. Além do Story, o Instagram Reel/Feed passa a ser aceito como origem. O wizard mostra "Fonte: Instagram — Story" ou "Instagram — Reel/Feed" e no seletor de destinos cada formato vira uma entrada separada (ex.: "Instagram — Reel/Feed (@conta)" e "Instagram — Story (@conta)" da mesma conta IG aparecem como dois itens). YouTube ganhou rótulo "Short" e o activity passa a adicionar automaticamente `#Shorts` no título quando ausente (garante a classificação de Short pela Data API v3). Facebook Reel foi adicionado como destino (publicado via `/{page-id}/videos` — o Facebook categoriza automaticamente como Reel por ser vídeo vertical). Regras V1 são migradas automaticamente no boot do backend (inferindo o formato pelo providerIdentifier) via `StartupMigrationService.backfillRepostDestinations()`; a coluna legada `destinationIntegrationIds` é mantida como shadow nesse primeiro deploy.
- Regras de Repost automático de Stories do Instagram: novo tipo de automação que monitora por polling os stories publicados em contas Instagram Business conectadas e republica o vídeo em TikTok (nativo + Late) e YouTube Shorts. A regra pode ser criada pelo menu Automações (3º card "Repost de Story") ou diretamente em `/automacoes/repost/nova`, com seleção de canal origem + multi-select de destinos, intervalo de polling (5 min a 6 h, default 15 min), legenda padrão com suporte a `{{timestamp}}` e switch para ativar imediatamente. Cada repost aparece no calendário como Post em QUEUE e é publicado pelo scheduler existente — com histórico de execuções por regra (published/partial/skipped/failed), idempotência por `(ruleId, sourceItemId)` e bootstrap automático do checkpoint (evita repostar stories antigos na primeira ativação). V1 aceita apenas vídeos (fotos são puladas com `skippedReason=FILTER_IMAGE`) e roteia o token IG via helper compartilhado `resolveIgRoute` (standalone → IG User Token → Page Access Token), extraído de `FlowActivity` para reuso entre automações de comentário e repost.
- Flow Builder (canvas) recebeu paridade com o wizard de automações de comentário: o painel do nó de Gatilho agora exibe o toggle "Pedir para seguir antes de enviar" para comentários (antes restrito a stories), com card amarelo explicando o fluxo de 2 etapas e campos dedicados para DM inicial, texto do botão inicial, mensagem de gate, texto do botão "Já segui", mensagem de desistência e máximo de tentativas. O nó de Send DM ganhou seção de botão opcional (texto + URL), e os nós no canvas passam a mostrar badge "Pede para seguir" no gatilho e chip do botão no Send DM. Automações criadas no Flow Builder passam a suportar o mesmo follow gate de comentários sem precisar trocar para o wizard.

### Corrigido

- Botão "Rodar agora" das regras de Repost agora dispara um ciclo imediatamente, mesmo com o workflow Temporal já rodando (regra ativa, mid-sleep). Antes a chamada apenas tentava `workflow.start` num `workflowId` determinístico que já existia, falhava com `WorkflowExecutionAlreadyStartedError`, o erro era engolido pelo `try/catch` interno e a próxima consulta à Graph API só ocorria quando o sleep terminava — visualmente o usuário recebia o toaster de "executando" mas nada acontecia. Agora o `repostWorkflow` escuta um signal `pokeRepost` (handler com flag) e usa `condition(() => poked, intervalMs)` no lugar de `sleep(intervalMs)`, e o `RepostService.runNow` envia o signal via `signalWithStart` com `workflowIdConflictPolicy: 'USE_EXISTING'` (cobrindo também o caso raro do worker ter morrido). Como item adicional, o botão sumiu da listagem quando a regra está pausada — antes ficava visível mas `disabled`, e o endpoint já rejeitava a chamada com 400.
- Badge do Zernio (overlay vermelho com o logo no canto superior direito do ícone do canal) aparecia fora do ícone nos dropdowns de seleção de fonte e destino do Repost, ancorando no canto do dropdown em vez de no ícone do canal. Causa: `PlatformIconBadge` retorna um fragmento com o badge `position:absolute` que precisa de ancestor `relative` próximo — nos dropdowns do Repost o container mais próximo era o painel `absolute z-30`. Agora cada uso do componente dentro do formulário do Repost foi envolvido localmente num `<span class="relative inline-block">`, garantindo que o badge ancore sempre na borda do ícone, sem alterar o `PlatformIconBadge` (que continua retornando fragmento) — preservando o layout em todos os outros lugares onde o componente é renderizado (lista de canais do calendário, picker de plataformas, etc.).
- Canal YouTube/Google entrava em loop de "precisa reconectar" mesmo após reconectar várias vezes ao dia. Causa: o cron de refresh chamava `YoutubeProvider.refreshToken()` sem propagar as credenciais por workspace, então o Google recebia o `refresh_token` emitido pelo app do workspace mas tentava refrescar com `process.env.YOUTUBE_CLIENT_ID/SECRET` (vazio ou de outro app). O Google rejeitava com `invalid_grant` e o orchestrator marcava `refreshNeeded=true`, esvaziando o `refreshToken` da integração — na próxima publicação aparecia "Token expired or invalid, please reconnect your YouTube account". Agora `IAuthenticator.refreshToken` aceita `clientInformation` opcional, o `RefreshIntegrationService` e o `IntegrationService.refreshToken` resolvem as credenciais do perfil/workspace via `IntegrationManager.getProviderCredentials` e passam pro provider antes do refresh, e o YouTube reusa o mesmo `clientAndYoutube(clientInformation)` que já é usado em `authenticate`/`generateAuthUrl`. A interface está retro-compatível para os demais providers.
- Falhas em publicações no Threads (incluindo reposts) ficavam mascaradas com a mensagem "Error: 17925601374295504" (apenas o ID do container Meta), porque o `ThreadsProvider.checkLoaded` descartava o campo `error_message` retornado pela Graph API quando o status do container vinha como `ERROR`. Agora a exceção carrega tanto o ID quanto a mensagem real do Meta (ex.: "Threads container 17925... failed: VIDEO_TOO_LONG"), o que aparece direto na coluna `Post.error` e nas notificações de erro do calendário — facilita identificar se a mídia foi rejeitada por duração, codec, aspect ratio etc.
- Conexão de canal TikTok (nativo) falhava com `Não foi possível entrar com o TikTok — client_key` mesmo após cadastrar Client Key e Client Secret em Settings > Credenciais. O `TiktokProvider` lia apenas `process.env.TIKTOK_CLIENT_ID/SECRET` e ignorava as credenciais por perfil. Agora aceita `ClientInformation` em `generateAuthUrl` e `authenticate`, mapeando `client_id` da interface para o `client_key` que o OAuth do TikTok espera, com fallback para as variáveis de ambiente — mesmo padrão de YouTube/LinkedIn/Facebook/Threads.
- Plug "Adicionar comentários por outra conta" no painel de configurações do LinkedIn (Perfil e Página) agora aparece traduzido em pt-BR — antes o título, descrição, label e placeholder do textarea vinham crus do `@PostPlug` do backend. Os campos genéricos do `PlugField` passaram a usar chaves dinâmicas (`post_plug_{identifier}_field_{name}_description`/`_placeholder`), então qualquer plug futuro herda o mecanismo.
- Checkbox no variant `hollow` (usado em "Publicar como carrossel de imagens" do LinkedIn e em outros toggles) ficava sem indicação visual quando marcado em modo claro: o ✓ branco ficava invisível sobre o fundo `customColor2` (`#f8f8f8`). Agora, quando marcado, o fundo passa a ser preenchido com a cor primária (`bg-forth`), ficando consistente com o variant default.
- Conexão de canal LinkedIn (Perfil pessoal e Página da empresa) falhava com `You need to pass the "client_id" parameter` mesmo após cadastrar Client ID e Client Secret em Settings > Credenciais. Os providers `linkedin` e `linkedin-page` liam apenas `process.env.LINKEDIN_CLIENT_ID/SECRET` e ignoravam as credenciais por perfil. Agora ambos aceitam `ClientInformation` em `generateAuthUrl` e `authenticate`, com fallback para as variáveis de ambiente quando o workspace não tem credenciais cadastradas — mesmo padrão já aplicado em YouTube, Facebook e Threads.
- Analytics do X (Twitter) quebrava com `TypeError: tweets.data.data is not iterable` para qualquer conta sem tweets na janela de 100 dias ou ao chegar na última página de paginação. A Graph API v2 omite o campo `data` da resposta quando `result_count` é zero, e o `loadAllTweets` fazia spread direto sem guardar contra `undefined`. Reautenticar o canal não resolvia. Agora o método trata resposta vazia como lista vazia e só recursiona se houver `next_token` real.
- Falhas em analytics do YouTube ficavam invisíveis por causa de um `catch` que engolia silenciosamente todos os erros e retornava lista vazia. Agora o erro real (token expirado, escopo `yt-analytics.readonly` não concedido, quota excedida etc.) é logado no backend para diagnóstico, mantendo o fallback de lista vazia para a UI não quebrar.
- Toaster "Channel Updated" aparecia em inglês após reconectar/atualizar um canal (YouTube, X e demais providers OAuth) mesmo com idioma pt-BR. A mensagem era passada literal na query string `?msg=Channel Updated` e renderizada sem passar pelo `t()` na página de destino. Agora o redirect carrega a string já traduzida (`Canal Atualizado`).
- Conexão de canal YouTube/Google falhava com `Erro 400: invalid_request — Missing required parameter: client_id` mesmo após configurar Client ID e Client Secret em Settings > Credenciais. O `YoutubeProvider` lia apenas `process.env.YOUTUBE_CLIENT_ID/SECRET` e ignorava as credenciais por perfil. Agora o provider aceita `ClientInformation` em `generateAuthUrl` e `authenticate`, com fallback para as variáveis de ambiente quando o workspace não tem credenciais cadastradas — o mesmo padrão que Facebook e Threads já seguiam.
- Resposta automática a comentário (feature ManyChat) falhava com `code=100 subcode=33` da Meta quando a integração era "Instagram (Facebook Business)" e o IG User Token cadastrado em Settings > Credenciais não tinha o escopo `instagram_business_manage_comments` (caso comum: aluno gera o token só com escopos de messaging). O `replyToComment` agora cai automaticamente para o Page Access Token da própria integração via `graph.facebook.com` quando a chamada via IG User Token falha — o PAT carrega `instagram_manage_comments` com Standard Access para testers/admins do app, sem exigir App Review. A mensagem de erro do provider passou a incluir host, comment ID, code e subcode da Meta para diagnóstico mais rápido, e o orchestrator loga a fonte do token (standalone, ig-user-token ou page-access-token) escolhida em cada reply.
- Automações de comentário (feed/reel) em integrações "Instagram (Facebook Business)" agora reusam o IG User Token cadastrado em Settings > Credenciais > Instagram > "Tokens de messaging por conta" quando disponível, dispensando a reconexão via "Instagram (Standalone)". A resposta ao comentário, a DM privada com botão postback, a checagem de follow e a DM final passam a rotear automaticamente para `graph.instagram.com` usando o IG User Token do workspace, que o aluno já gera direto no Meta Dashboard ao habilitar o produto "Instagram API with Instagram Login". A prioridade de seleção de token ficou: (1) integração Standalone usa o próprio token; (2) IG User Token cadastrado em Settings; (3) fallback para Page Access Token em `graph.facebook.com` (só funciona com Advanced Access). Sem isso, quem tinha o canal conectado via Facebook Business precisava reconectar só para o follow gate de comentários funcionar.
- Follow gate de automações agora funciona em instâncias self-hosted de alunos sem App Review da Meta. A causa do bloqueio era a combinação do provider legacy "Instagram (Facebook Business)" com a checagem `is_user_follow_business` via `graph.facebook.com` + Page Access Token, que exige Advanced Access à permissão `instagram_manage_messages` — inacessível para apps novos em Live Mode sem revisão. A solução roteia comentário, DM pós-postback e checagem de follow para `graph.instagram.com` + IG User Token quando o canal é conectado via "Instagram (Standalone)" (Instagram Login API), onde o campo retorna consistente sob Standard Access. A scope `instagram_business_manage_messages` foi adicionada ao provider Standalone; o provider antigo permanece inalterado. Nenhuma migração de banco é necessária — basta reconectar o perfil de Instagram escolhendo o canal Standalone. O endpoint de messaging também migrou para `/me/messages` (sem lookup de Page ID), e a DM final do follow-gate passa a usar o próprio token da integração Standalone, dispensando o setup de System User Token ou IG User Token em Settings > Credenciais para automações de comentário.
- Follow gate em automações de comentário (feed/reel) agora funciona de forma confiável. A Meta Graph API só retorna `is_user_follow_business` quando o usuário já tem contexto de messaging com a conta — para comentaristas que nunca enviaram DM, o campo vinha ausente e o gate ou bloqueava seguidores (fail-closed) ou liberava não-seguidores (fail-open). O fluxo agora segue o padrão do ManyChat: resposta pública no comentário → DM inicial com botão "Quero o link" → ao clicar, abre a janela de messaging de 24h e a checagem de follow passa a ser confiável → envia o link final para quem segue ou uma DM de convite com botão "Já segui!" para quem não segue (até 3 tentativas configuráveis). O wizard ganhou um aviso amarelo explicando as 4 etapas e campos dedicados para cada mensagem. Automações em produção com `requireFollow=true` migram automaticamente com defaults em português.
- Threads OAuth voltava com `error_code:1` mesmo após configurar Threads App ID/Secret em Settings > Credenciais. O `ThreadsProvider` lia apenas `process.env.THREADS_APP_ID/SECRET` e o controller só passava o par Facebook. Agora o provider aceita `ClientInformation` em `generateAuthUrl`/`authenticate` e o controller prioriza `threadsAppId`/`threadsAppSecret` quando a integração é `threads`, com fallback para o par Facebook quando os campos dedicados não estão preenchidos.
- Opção "Pedir para seguir antes de enviar" em automações agora aplica branch real: quando `is_user_follow_business=true` envia a DM normal configurada no flow; quando é false, envia uma mensagem de gate (personalizável no wizard ou com texto padrão convidando a seguir) em vez da DM original e ignora o botão de CTA para não vazar conteúdo exclusivo. Antes a flag era salva mas totalmente ignorada. Vale tanto para `story_reply` quanto para `comment_on_post`.
- Follow gate em automações de feed/reel não disparava mesmo para usuários que não seguiam a conta. A Messenger User Profile API (`GET /{IGSID}?fields=is_user_follow_business`) costuma devolver erro para comentaristas sem contexto de mensagem prévio, e o código tratava esse erro como fail-open (considerava seguidor) e enviava a DM normal. Agora a checagem é diferenciada por origem: para `comment_on_post` (fluxo opt-in de gate), erro da Graph API passa a ser tratado como fail-closed — envia a mensagem de gate; para `story_reply` o fail-open é mantido para não bloquear respostas legítimas a stories. O workflow passa `triggerType` explicitamente pro Temporal e registra no histórico de execução o resultado da checagem (`source=comment follows=true/false/null`) para facilitar o diagnóstico.
- Botão de CTA configurado na DM de automação de story não era enviado: o workflow do Temporal coletava apenas o texto do nó `SEND_DM` e descartava `buttonText`/`buttonUrl`. Agora a traversal propaga o botão até a activity `sendStoryDirectMessage`, e o `InstagramMessagingService` constrói o payload Meta como `attachment.template.button` quando há CTA, caindo para texto puro quando não há. O mesmo button template foi adicionado em `sendPrivateReply` para automações de comentário, com fallback automático.
- Layout do wizard de automação (story e comentário) tinha scroll na coluna do preview do celular junto com a página inteira. Agora o painel do celular fica fixo no centro e só a sidebar de opções rola internamente, mesmo em formulários longos.

### Adicionado

- Automações de comentário em feed/reel ganharam os mesmos recursos do wizard de story: botão de CTA na DM (button template no private reply) e gate de follow personalizável (mensagem alternativa para quem ainda não segue a conta). A verificação de follow em fluxos de comentário reusa o Page Access Token que já vive na integração, sem exigir token de messaging separado.
- Wizard de automação de Story agora exibe uma grade de stories ativos (últimas 24h) para seleção em vez de um campo de texto de ID, e o preview do celular mostra a thumbnail do story escolhido. O painel de preview também foi alinhado ao do wizard de comentários (centralizado, com fundo próprio).
- Suporte a tokens de messaging Meta para respostas automáticas via DM em stories: aceita tanto **Meta System User Access Token** (1 token para múltiplas contas do Business Manager, não expira) quanto **Instagram User Access Tokens por conta** (60 dias com refresh automático lazy no momento do uso, sem cron). Configuração unificada em Settings > Credenciais > Instagram, com validação ao salvar via Graph API e badges de status (válido/expirando/expirado). Resolve o limite da scope legacy `instagram_manage_messages` que exigia Advanced Access/App Review completo. O Story Wizard agora mostra um aviso quando a integração selecionada não tem token configurado.
- Automacoes de Stories no Instagram: novo fluxo separado que responde via DM a respostas e reacoes de stories (qualquer story, story especifico ou proximo story), com preview vertical e popup unico "Nova Automacao" como hub de entrada para todos os tipos de gatilho. Mantem retrocompatibilidade com automacoes existentes de comentario em publicacao.
- Flow Builder (canvas) agora expoe todos os modos de gatilho: seletor de tipo (Comentario em publicacao vs Resposta ao story), modos `Todos os posts`/`Proxima publicacao`/`Posts especificos` para comentarios e `Qualquer story`/`Proximo story`/`Story especifico` para stories, alem de toggles de reacoes e pedido de follow. O no TRIGGER passa a mostrar o texto correto para cada combinacao e o label do node e sincronizado com o triggerType ao salvar o canvas, mantendo o filtro do webhook consistente.
- Suporte a desenvolvimento local com dominio HTTPS proprio via Cloudflare Tunnel: `next.config.js` agora le `FRONTEND_URL` e `NEXT_PUBLIC_BACKEND_URL` do `.env` e libera os hosts em `allowedDevOrigins` automaticamente, permitindo rodar `pnpm dev` apontando OAuth callbacks pra um subdominio estavel sem precisar publicar imagens Docker a cada teste de integracao com Meta/Google/TikTok/etc.

### Documentação

- Dossiê de proposta da feature de repost automático de stories do Instagram (inspirada em Repurpose.io / Repostify): monitora stories publicados diretamente no app do Instagram e reposta em TikTok (nativo + Late) e YouTube Shorts, com interface de 2 colunas (origem/destinos) integrada em Automações, polling Temporal com short-circuit para respeitar rate limits da Graph API, modelo Prisma genérico preparado para expansão a Reels/Feed, edge cases (música licenciada, janela de 24h, stickers interativos, carrosséis) e perguntas em aberto. Nenhuma implementação ainda — apenas documentação em `docs/architecture/instagram-story-repurpose.md`

### Upstream

- Sincronizado com Postiz upstream ate commit `e3b3b82f` (2026-04-10, 24 commits)
- Instagram: melhor tratamento de erros, fix de refresh token e recuperacao de sessao invalidada
- YouTube: preserva refresh token existente para evitar re-auth diario
- Reddit: correcao de postagem em subreddits via API
- Facebook/Instagram: melhorias no fluxo de refresh de integracao
- Backend: endpoint de download de faturas (`/download-invoices`)
- Backend: correcoes de seguranca (security fixes)
- Backend: nova versao de post workflow (`postWorkflowV102`)
- Deps: axios upgrade, gitignore limpo, ajustes no workflow de PR quality
- **NAO sincronizado** (major upgrade deixado para sync dedicado): langchain 0.3 -> 1.x, mastra 0.x -> 1.x, @mastra/mcp upgrade, @mastra/rag removido upstream. Mantidos em 0.x porque nossa feature Knowledge Base RAG depende de `@mastra/rag@2.1.3` e a migracao para mastra 1.x requer reescrita das tools de chat (copilot.controller, load.tools, generate.image.tool) — deixar para um sync focado separado

### Corrigido

- Conexao de canal Threads falhava com `error_code:1` ("An unknown error has occurred") mesmo apos configurar App ID e App Secret do Threads em Settings > Credenciais. O `ThreadsProvider` so lia `process.env.THREADS_APP_ID/SECRET` e o controller de integracoes so mapeava `clientId/clientSecret` (Facebook) para o OAuth, ignorando os campos `threadsAppId/threadsAppSecret` da secao Threads. Agora o provider aceita `ClientInformation` em `generateAuthUrl` e `authenticate`, e o controller prioriza as credenciais dedicadas do Threads quando a integracao e `threads`, com fallback para o par Facebook quando nao preenchido
- Validacao HMAC do webhook de Instagram falhava silenciosamente para apps que usam o produto "Instagram API with Instagram Login" — o Meta assina os webhooks com o Instagram App Secret (separado do Facebook App Secret quando ambos os produtos estao habilitados no mesmo app), mas o backend so tentava o Facebook App Secret. Agora o controller aceita `INSTAGRAM_APP_SECRET` como env var dedicada e `instagramAppSecret` como campo na credencial do workspace, tentando ambos os segredos ao validar o `x-hub-signature-256`. O endpoint `/flows/webhook-config` passou a retornar o Verify Token configurado por perfil (com fallback para `multipost`) e o helper `configureInstagramWebhook` prioriza as credenciais de Instagram quando existem. Webhook agora valida com sucesso em setups multi-perfil onde cada perfil tem seu proprio app Meta.
- Redesenho do card de credenciais Meta em Settings > Credenciais: unificado sob a marca Meta (com icone) e subdividido em secoes por produto — Facebook (App ID/Secret), Instagram (App ID/Secret + Verify Token + bloco de Callback URL/Verify Token copiaveis para colar no Meta Developer Portal) e Threads (App ID/Secret, placeholder para uso futuro). Facilita a compreensao de qual credencial pertence a qual produto Meta e prepara o terreno para webhooks de Threads/Messenger sem novas reestruturacoes.
- Automacao criada pelo popup "Nova Automacao" salvava o flow como Rascunho ao salvar no wizard, exigindo ativacao manual. Agora o `quickUpdateFlow` promove automaticamente para Ativo quando o flow estava em Rascunho (flows ja pausados ou ativos mantem o status).
- Backend nao subia em ambiente local apos sync anterior do upstream — resolucao errada de conflito em merge anterior manteve `@mastra/core@0.20` enquanto aceitava `@ag-ui/mastra@1.0.1` (que exige core 1.x), resultando em crash silencioso no import de `@mastra/core/dist/request-context`. Corrigido subindo `@mastra/{core,mcp,memory,pg}` para versoes compativeis e portando arquivos de chat (`copilot.controller`, `load.tools.service`, `mastra.store`, `auth.context`, `agent.tool.interface`, `start.mcp`) para a API do core 1.x, preservando overlays custom do fork (persona prompt, knowledge base RAG / pgVector)
- Traducao dos textos restantes no painel de configuracoes de publicacao (X e LinkedIn): placeholder de comunidade, toggles "Feito com IA" e "Parceria paga", finalizador de thread, plug "Adicionar repostadores", label e opcoes de atraso (Imediatamente, 1 hora, ...) e mensagem "Nenhuma conta disponivel"
- Traducao da tela de Historico de execucoes das automacoes: status da execucao (Concluida, Falhou, Em execucao), tipos de node (Gatilho, Responder comentario, Enviar DM, Condicao, Atraso) e estados da timeline (entrou, concluido, erro, ignorado)
- Verificacao de credenciais do X (Twitter) falhava com "Unable to verify your credentials" mesmo com chaves validas — endpoint e fluxo corrigidos usando `twitter-api-v2.appLogin()`
- Isolamento de credenciais OAuth do X por perfil — cada perfil do workspace agora usa suas proprias Consumer Keys em todo o fluxo OAuth 1.0a, incluindo publicacao, comentarios, analytics e plugs de repost, sem vazar credenciais entre perfis e sem depender de X_API_KEY/X_API_SECRET globais
- Logs de diagnostico adicionados ao teste de credenciais (backend e frontend) e ao wrapper `runInConcurrent` (para exibir erros reais de APIs de provider no worker do Temporal)
- Upload de midia no X: o upstream Postiz usa `client.v2.uploadMedia` (endpoint novo `/2/media/upload` da X API v2) que nao funciona no tier Free da X e resulta em falha silenciosa ("Unknown Error") no Temporal. Migrado para `client.v1.uploadMedia` (`/1.1/media/upload.json`) que e disponivel em todos os tiers. Adicionalmente, o upstream convertia toda imagem para GIF via sharp mas declarava o MIME type original no request — fix preserva o formato original (PNG/JPEG/WebP/GIF/MP4) com mimeType coerente
- Reconhecimento do erro `CreditsDepleted` (HTTP 402) do X em `handleErrors`: em vez de "Unknown Error" generico, agora exibe mensagem clara orientando o usuario a verificar o tier do app no developer.x.com quando a cota mensal de creditos para criacao de tweets se esgota
- Crash no preview do TikTok ao abrir o modal de canais sem midia anexada (`Cannot read properties of undefined (reading 'map')`)
- Correcoes na tela publica de compartilhamento de post (`/p/:id?share=true`): logo renderizava quebrado, textos e data permaneciam em ingles com idioma pt-BR, botao de login/cadastro ficava comprimido com texto longo e aparecia warning de key do React nas imagens
- Contraste dos textos no node Trigger — contagem de posts e palavras-chave agora usam cor legivel em ambos temas claro e escuro
- Acentuacao faltante nas traducoes em portugues do wizard de automacoes
- Multiplas DMs em sequencia no flow de automacao — apenas a primeira usava private reply (limitacao da Meta), as seguintes agora usam DM direta via IG-scoped user ID
- Configuracao de webhook Instagram movida da pagina de Automacoes para dentro da credencial Facebook em Configuracoes > Credenciais

### Adicionado

- Infraestrutura de testes com TDD: configuracao Jest multi-projeto, utilitarios de mock compartilhados e exemplos de teste para SetsService e SetsRepository
- Scripts de teste no package.json: `test:watch`, `test:backend`, `test:libs`
- Step de testes automatizados no CI (GitHub Actions) antes do build
- Link de aprovacao por cliente: cada post pode gerar tokens de revisao compartilhaveis por URL (`/p/:id?token=...`) que permitem ao cliente comentar e aprovar ou pedir alteracoes sem login. Tokens sao aleatorios de 256 bits armazenados apenas como hash SHA-256, com expiracao padrao de 30 dias, revogacao manual, rate limit por IP (10 requisicoes / 5 min), sanitizacao de conteudo e trilha de auditoria (IP e user-agent). UI do dono no calendario para gerar, listar e revogar links
- Migracao automatica do schema Prisma no start do container Docker — o entrypoint agora executa `prisma db push` antes de iniciar a aplicacao, garantindo que quem atualiza a imagem em producao recebe as novas colunas/tabelas sem precisar rodar comando manual
- Modo "Proxima Publicacao" em automacoes do Instagram: permite criar a automacao antes do post existir e vincula-a automaticamente ao proximo feed ou reel publicado, seja pelo DestinyPost ou direto no Instagram (stories excluidos)
- Tag Docker `:prerelease` que aponta sempre para a ultima RC publicada, permitindo atualizacao automatica sem especificar numero de versao
- Selecao de posts no wizard com grid de 4 thumbs + modal "Mostrar Todos" com scroll infinito
- Secao "E esse comentario possui" estilo ManyChat — radio cards, input com virgulas, chips de exemplo, toggle para interagir com comentarios
- Respostas de comentario multiplas com randomizacao — orchestrator escolhe aleatoriamente entre as variacoes configuradas
- Preview do celular dinamico — aba ativa muda automaticamente conforme o usuario edita (post ao selecionar, comentarios ao digitar palavra-chave, DM ao escrever mensagem)
- Edicao de automacoes simples via Wizard — botao "Editar no Assistente" na tela de resumo abre o wizard preenchido com os dados existentes (POST /flows/:id/quick-update)
- Avatar e nome da conta Instagram refletem no preview do celular ao selecionar a conta no wizard
- Wizard "Nova Automacao Rapida" — formulario guiado com preview em tempo real (celular mockup) para criar automacoes simples sem precisar do editor de nodes
- Endpoint POST /flows/quick-create que gera nodes/edges automaticamente a partir do wizard
- Endpoint GET /flows/integrations/:integrationId/posts para buscar posts Instagram sem precisar de um flow existente
- Palavras-chave (keywords) no node Trigger — elimina necessidade do node Condition para 99% dos casos de uso
- Toolbar de nodes melhorada com cards coloridos, descricao curta e clique para adicionar alem de arrastar
- Modo Summary vs Advanced no editor de flows — flows simples abrem em visualizacao resumo, complexos abrem no editor React Flow
- Historico de execucoes detalhado — timeline vertical com status de cada node visitado (log de execucao)
- Campo executionLog no schema FlowExecution para armazenar trace de nodes visitados em cada execucao
- Endpoint GET /flows/:id/executions/:executionId para detalhe de uma unica execucao
- Botao "Nova Automacao" (wizard) como acao primaria na lista de automacoes, "Comecar do zero" como secundario
- Knowledge Base por perfil via RAG com pgvector — usuarios enviam PDFs, TXT ou MD e o agente pode citar fatos desses documentos ao gerar posts
- Nova tool `knowledgeBaseQuery` no agente Mastra que consulta vetores por perfil antes de gerar conteudo com informacoes factuais
- API de gestao de documentos (GET/POST upload/DELETE em /settings/profiles/:id/knowledge)
- Schema ProfileKnowledgeDocument com status PROCESSING/READY/FAILED e cascade delete por perfil
- Inicializacao automatica da extensao pgvector no startup (CREATE EXTENSION IF NOT EXISTS vector)
- Feature flag ENABLE_KNOWLEDGE_BASE (default true) — permite desabilitar em setups sem pgvector
- Tela de Knowledge Base nas configuracoes com upload, listagem com polling de status e exclusao
- Traducoes pt/en para a tela de KB
- Persona de IA por perfil — agencias podem configurar tom de voz, publico-alvo, CTAs preferidos, restricoes de conteudo e estilo de imagem por cliente
- API de gestao de Persona por perfil (GET/PUT/DELETE /settings/profiles/:id/persona) restrita a ADMIN
- Tela "Persona de IA" nas configuracoes com presets de tom e estilo, CTAs como tags e ate 5 posts de exemplo
- Persona injetada automaticamente no agente Mastra (chat), no Generator LangGraph e nas geracoes de imagem DALL-E
- Traducoes pt/en completas para a tela de persona
- Documentacao de arquitetura para Persona (docs/architecture/profile-ai-persona.md) e Knowledge Base RAG (docs/architecture/knowledge-base-rag.md)
- Plano detalhado de implementacao para Persona de IA por perfil e Knowledge Base via RAG (docs/planning/profile-ai-persona-knowledge-base.md)
- Imagem Docker do PostgreSQL atualizada para pgvector/pgvector:pg17 (necessario para Knowledge Base)
- Automacoes de comentarios Instagram (estilo ManyChat) — flow builder visual com React Flow para responder comentarios automaticamente
- 5 tipos de no no editor de automacoes: Gatilho (comentario), Condicao (palavra-chave), Responder Comentario, Enviar DM e Atraso
- Nova secao "Automacoes" no menu lateral com listagem e editor visual de flows
- Webhook de entrada para receber eventos de comentarios do Instagram via Meta Graph API
- Execucao de flows via Temporal workflows com suporte a delay duravel e retries automaticos
- Metodo sendDM() no Instagram provider para enviar mensagens diretas via Instagram Messaging API
- Metodo subscribeToWebhooks() no Instagram provider para inscrever paginas em webhooks da Meta
- Inscricao automatica de webhooks ao ativar uma automacao — nao precisa configurar webhook manualmente no Meta Developer Portal
- Seletor de conta Instagram conectada ao criar automacao (substitui input manual de Integration ID)
- Webhook Verify Token configuravel por perfil em Configuracoes > Credenciais (multi-tenancy: cada perfil pode ter seu proprio App Meta)
- HMAC do webhook Instagram valida com App Secret da credencial do perfil (fallback para variavel de ambiente global)
- Documentacao passo-a-passo de Automacoes Instagram em docs/automacoes-instagram.md (referenciada no README)
- Verify Token padrao "multipost" aceito automaticamente no webhook Instagram (zero config — nao precisa cadastrar verify token nas credenciais)
- Bloco copy-paste na tela de Automacoes com Callback URL e Verify Token prontos para colar no Meta Developer Portal
- Botao "Configurar webhook Instagram na Meta" no card Facebook de Credenciais — configura callback URL e verify token automaticamente via API da Meta (1 clique, elimina passo manual no Meta Developer Portal)
- Historico de execucoes por automacao com status em tempo real
- Traducoes pt/en para todas as strings de automacoes
- Seletor de posts do Instagram no no Gatilho — lista recentes (feed/reels/stories) com thumbnail para escolher quais posts disparam a automacao (estilo ManyChat)
- Modo ilimitado para creditos de IA via variavel AI_CREDITS_MODE (default: unlimited para self-hosted)
- Creditos de IA configuraveis por perfil no modo gerenciado (AI_CREDITS_MODE=managed)
- API de gestao de creditos de IA por perfil (GET/PUT /settings/profiles/:id/ai-credits, GET /settings/ai-credits/summary)
- Validacao de API key (OPENAI_API_KEY) antes de consumir credito de IA — retorna 503 se nao configurada
- Tela de gestao de creditos de IA no painel de configuracoes (visivel apenas para admins no modo managed)
- Indicador visual de creditos restantes nos componentes de geracao de imagem e video
- Botao de geracao desabilitado com tooltip quando creditos zerados
- Traducoes pt/en para todas as strings de creditos de IA

### Corrigido

- Automacao completava sem executar acoes (responder comentario, enviar DM) — edge auto-conectada de no de Condicao nao tinha sourceHandle, workflow nao encontrava caminho "match" e pulava todas as acoes
- Env var SKIP_IG_WEBHOOK_HMAC=true para pular validacao HMAC do webhook Instagram quando proxy reverso re-serializa o body
- Validacao pre-criacao de automacao: botao Criar fica desabilitado ate o webhook Instagram estar configurado na Meta para a conta selecionada, com mensagem de ajuda inline no modal
- Verificacao de webhook usa endpoint app-level /{app_id}/subscriptions da Meta Graph API (com app_id|app_secret) — unica forma confiavel de ler o estado do webhook Instagram no modelo de Casos de uso. Tentativa anterior via /subscribed_apps falhava porque token de Business Login nao tem capability para esse endpoint
- Webhook Instagram descartava todos os eventos silenciosamente por filtrar value.item === "comment" (campo so existe em Facebook Page feed, nao em Instagram)
- Validacao HMAC do webhook comparava assinatura com JSON re-serializado (JSON.stringify) em vez do raw body — toda requisicao era rejeitada com 403 quando FACEBOOK_APP_SECRET estava configurado
- Logs detalhados adicionados no webhook handler para facilitar debug de eventos recebidos
- Acao "Responder comentario" postava novo comentario top-level no post em vez de resposta threaded — agora usa POST /{comment_id}/replies conforme docs da Meta
- Acao "Enviar DM" usava /me/messages com recipient.id (so funciona dentro da janela de 24h de mensagem ativa) — trocado para private_replies API (POST /{ig_id}/messages com recipient.comment_id) que eh a unica forma oficial de DM um comentador (janela de 7 dias)
- Toaster cortava mensagens longas (altura fixa 56px) — agora cresce verticalmente com quebra de linha automatica e tempo de exibicao proporcional ao tamanho
- Mensagem de webhook nao configurado mostrava "Detalhe:" vazio quando Meta retornava success=false sem erro
- No de Atraso tinha texto branco sobre fundo branco e borda invisivel no tema claro — agora usa tema laranja consistente com identidade visual
- Inscricao de webhook Instagram usava subscribed_fields=feed (Facebook Page) em vez de comments,messages na conta IG — causava zero eventos chegando mesmo com automacao ativa
- Arestas (conexoes) do flow builder nao podiam ser removidas — agora clique na aresta pede confirmacao para remover; tecla Backspace/Delete tambem remove
- Botao Historico no editor de automacao para visualizar execucoes sem sair da tela
- UX de remocao de arestas estilo n8n — lixeira aparece no hover sobre a conexao
- No de Condicao mostra "Verdadeiro"/"Falso" em vez de "Match"/"Sem match" (mais claro)
- Ao arrastar novo no para o canvas, ele conecta automaticamente ao ultimo no (fluxo linear)

### Alterado

- Renomeacao na interface de automacoes: "Wizard/Assistente" passa a se chamar "Automacao Rapida" (PT) e "Quick Automation" (EN); modo "Avancado/Comecar do zero" passa a se chamar "Flow Builder" (mesmo termo em pt e en) — alinhado com a nomenclatura ManyChat que os usuarios brasileiros ja reconhecem

### Removido

- Botao "Configurar webhook Instagram na Meta" em Credenciais — no modelo de Casos de Uso da Meta (2024) o webhook tem que ser configurado manualmente dentro de cada caso de uso, nao ha endpoint API publico que preencha esses campos

## [0.3.0] - 2026-04-03

### Corrigido

- Login/registro com Google via Generic OAuth falhava com redirect_uri_mismatch — middleware detectava provider incorreto devido a "googleapis.com" nos parametros de scope
- Contagem de integracoes por perfil incluia canais deletados

### Adicionado

- Link de convite Late — botao "Enviar link de convite" abre modal com selecao de perfil e plataforma, gerando link OAuth direto via Late platform-invites
- Credenciais OAuth independentes por perfil — cada perfil pode ter suas proprias credenciais de redes sociais
- Webhooks, Auto Post e Sets isolados por perfil ativo
- Late API key configuravel por perfil — cada perfil pode conectar sua propria conta Late
- Preferencia de Shortlink independente por perfil
- Script de migracao SQL para mover dados existentes de credenciais, webhooks, autopost, sets, late e shortlink para o perfil default
- Migracao automatica no startup do backend — usuarios Docker nao precisam rodar SQL manualmente ao atualizar
- Multi-tenancy com perfis: suporte a 3 niveis (Workspace > Perfil > Recursos) para agencias gerenciarem multiplos clientes
- Modelo Profile e ProfileMember no banco de dados com roles (OWNER, MANAGER, EDITOR, VIEWER)
- API REST para CRUD de perfis e gerenciamento de membros (`/profiles`)
- Troca de perfil ativo via cookie/header (`POST /user/change-profile`)
- Filtragem de integracoes, posts, midia, tags e assinaturas por perfil ativo
- Script de migracao SQL para criar perfis default e migrar dados existentes
- Seletor de perfil no top bar para trocar entre perfis da organizacao
- Tab "Perfis" nas configuracoes para criar, editar, excluir perfis e gerenciar membros (ADMIN+)
- Cookie `showprofile` enviado como header nas requests para manter perfil ativo no frontend
- Documentacao do fluxo de client login para fase futura
- Provider Late unificado — botao unico "Late" no modal de adicionar canal abre selecao de contas ja conectadas no Late
- Suporte a 13 plataformas via Late: Twitter, Instagram, TikTok, YouTube, Facebook, LinkedIn, Pinterest, Reddit, Bluesky, Threads, Google Business, Telegram, Snapchat
- Modal de selecao Late com 2 etapas: escolher perfil Late e depois selecionar conta (agrupada por plataforma)
- Opcao de conectar nova conta via OAuth do Late diretamente no modal
- Badge visual Late (asterisco oficial) nos icones de canais conectados via Late para diferenciar de conexoes nativas
- Icone de plataforma como foto de perfil para canais Late (Late SDK nao fornece fotos de perfil)
- Suporte a pre-release (RC) no workflow de release — permite lancar versoes de teste sem afetar `:latest`
- Workflow `promote-release.yml` para promover RC para estavel sem rebuild da imagem Docker
- Opcoes `rc` e `promote` no skill `/new-release`
- Changelog incremental — Claude Code preenche `[Unreleased]` conforme trabalha

### Corrigido

- Isolamento completo de dados por perfil — posts, tags, analytics, agents e canais agora respeitam o perfil ativo em todas as operacoes de escrita e leitura
- Posts criados em um perfil nao aparecem mais em outros perfis
- Find-slot calcula horarios livres considerando apenas posts do perfil ativo
- Analytics valida que a integracao/post pertence ao perfil antes de retornar dados
- Agent (copilot) lista apenas canais do perfil ativo e nao consegue postar em canais de outro perfil
- Threads do agent isoladas por perfil — cada perfil tem suas proprias conversas
- Upload de midia pelo agent agora salva com profileId correto
- Enable/disable/delete de canal valida que a integracao pertence ao perfil ativo
- Integracoes sem perfil (org-level) agora aparecem em todos os perfis corretamente
- Edit/delete de tags agora respeita o perfil ativo
- Autopost busca apenas integracoes do perfil correto ao disparar
- Conexao OAuth agora salva profileId atomicamente na integracao (sem race condition)
- Late connect salva profileId diretamente no createOrUpdateIntegration (eliminada race condition)
- Webhooks preservam profileId ao ser atualizados
- API publica aceita profileId como query param para filtrar integracoes
- Quota de canais conta apenas canais do perfil ativo ao habilitar canal
- Migracao automatica no startup associa posts e midias orfaos ao perfil default
- Conexao de canais Late (TikTok/Pinterest) falhava com erro de sessao expirada ao adicionar canal
- Upload de midia nao aparecia na listagem quando perfil ativo estava selecionado (profileId nao era salvo no registro de midia)
- Midia sem perfil associado (upload anterior ao recurso de perfis) agora aparece para todos os perfis
- Icones de plataforma Late quebravam em 15+ componentes por usar identificador `late-xxx` como path de icone (centralizado via `PlatformIconBadge`)
- Componente `PlatformIconBadge` reutilizavel com badge Late (asterisco SVG oficial) substitui codigo duplicado em 8+ componentes
- Canais Late agora mostram icone da plataforma como foto de perfil (Late SDK nao fornece fotos)
- Layout do frontend falhava ao renderizar `PlausibleProvider` como Fragment (corrigido com renderizacao condicional)
- Rota de uploads retornava erro 500 para arquivos inexistentes (agora retorna 404 com verificacao de existencia)
- Rota de uploads falhava quando `UPLOAD_DIRECTORY` nao estava configurado (agora retorna erro 500 descritivo)
- Adicionada traducao `select_or_upload_pictures_max_1gb` em pt e en
- Agendamento de posts via canais Late falhava silenciosamente — providers `late-*` nao estavam registrados no discriminator de validacao do DTO
- Perfis sem Late configurado conseguiam acessar contas Late de outros perfis — fallback para API key da organizacao removido quando perfil ativo esta selecionado
- Toggle "Compartilhar Late com Perfis" nas Configuracoes Globais — permite controlar se perfis sem chave Late propria usam a chave do workspace padrao
- Mesma conta Late pode ser conectada em perfis diferentes sem conflito (internalId composto com profileId)
- Conexao de canais Late falhava com erro ao tentar fazer upload de icone local como URL (`/icons/platforms/tiktok.png`)
- Posts via Late agora verificam o status real da publicacao na plataforma — se o Late aceita mas a rede social rejeita (ex: token expirado), o erro eh detectado e reportado em vez de marcar como sucesso
- Posts via Late ficavam presos na fila porque a task queue `late` nao tinha worker — Late providers agora usam a task queue `main`

### Alterado

- Skill `/changelog` reescrito para consolidar rascunho incremental em vez de gerar do zero
- CI/CD detecta pre-releases e nao atualiza `:latest` para versoes RC/beta

### Upstream

- Sincronizado com Postiz upstream ate commit f55cca51 (2026-04-03)
- Security: validacao de URLs em webhooks (IsSafeWebhookUrl), protecao contra acesso interno via upload URL
- Upgrade Next.js para versao 16.2.1 com Turbopack
- Novo provider ReelFarm para geracao de video
- Import de posts existentes
- Pinterest: exibicao de mais boards
- MCP com suporte a OAuth
- Sentry metrics e health check para Temporal
- Language switch no frontend com suporte a direcao RTL
- Sistema de announcements (banners de aviso)
- Componente SafeImage substituindo next/image direto
- Sincronizado com Postiz upstream ate commit e20565fb (2026-03-13)
- Novo provider MeWe para publicacao em grupos
- Sistema OAuth Apps — apps de terceiros podem autenticar via Postiz OAuth
- Refactor de auth providers (factory → manager pattern)
- MCP com suporte a OAuth2
- Fixes de Stripe (skip subscription check sem Stripe, require API key)
- Facebook/Instagram connect melhorado, Google My Business com paginacao
- Telegram fix, X API update
- AppSumo webhook handling melhorado
- PR Quality workflow no CI

### Documentacao

- Secao Multi-Tenancy adicionada ao README com link para guia completo
- Guia detalhado de multi-tenancy em `docs/multi-tenancy.md`
- Documentacao reorganizada em estrutura `docs/` (architecture, development, operations, planning)

## [0.2.0] - 2026-02-28

### Adicionado

- Rebranding: Postiz renomeado para "DestinyPost" em todo o frontend
- Traducoes pt-BR para componentes principais (canais, email, erros, midia)
- Documentacao de build e publicacao Docker (guia-docker-release.md)
- CLAUDE.md e AGENTS.md com instrucoes para agentes IA
- Source maps para estilos CSS globais
- Skills Claude Code: /sync-upstream, /new-release, /changelog, /fork-status

### Corrigido

- Interpolacao i18next no fluxo de conexao de canais
- Logo no README usando asset local em vez de fonte externa
- Registry CI/CD: corrigido de ghcr.io/gitroomhq para ghcr.io/destinyai-dev

### Alterado

- README reescrito para comunidade brasileira (Automacao Sem Limites)
- Traducoes de notificacoes por email atualizadas
- Versao do fork definida como 0.1.0 (independente do upstream)

### Upstream

- Baseado no Postiz upstream em commit ccd66571 (era ~v2.16.0)
- Sincronizado com Postiz upstream ate commit `6c39e810` (2026-02-28)
  - feat: URL separada para MCP server (`mcpUrl`)
  - feat: Redis throttler para rate limiting
  - feat: Suporte a TLS e API key no Temporal
  - feat: Melhor tratamento de erros no TikTok
  - feat: Funcionalidade de logout
  - feat: Compressao HTTP e melhores indices no banco
  - feat: Cobrancas apenas para sucesso + reembolso e cancelamento
  - fix: Colaboradores em carrossel do Instagram
